# Agent Observability Lab — PRD

This is the Product Requirements Document. It owns three things: the project's **What**, the project's **Why**, and the open and closed **Tasks**. End-user-facing surface lives in `README.md`; internal structure lives under `SPEC/`; coding style lives in `CONVENTIONS.md`.

## What

Agent Observability Lab is a hands-on environment for evaluating what telemetry an AI coding agent emits and how an observability backend ingests, stores, and visualizes it. It is organized as a collection of self-contained **stacks**, each living under `stacks/<name>/` and runnable on its own with `docker compose up`. The first stack is **`claude-code-elastic`**: Elasticsearch + Kibana + APM Server, receiving Claude Code's OpenTelemetry signals over Elastic's native OTLP endpoint and surfacing the collected **metrics** (sessions, tokens, cost, code edits, commits/PRs) and **events** (prompts, tool results, API requests) in Kibana. The lab is a reproducible demo and reference, not a production deployment.

## Why

AI coding agents are increasingly part of real workflows, and it is hard to reason about cost, audit, and behavior without seeing what is actually observable about them. Vendor docs describe the signals in the abstract; this lab makes them concrete — a stack anyone can `docker compose up` to watch real agent telemetry flow into a backend, inspect the exact fields, and judge what is useful, what is sensitive, and what is missing. Keeping each stack self-contained under `stacks/` lets new agent/backend combinations be added and compared over time without entangling them.

## Tasks

Each task is one concern. Tasks are processed in order subject to their dependencies. Completed tasks (`- [x]`) are immutable history — corrections become new tasks, never edits to old ones.

- [x] Establish the `claude-code-elastic` Elastic Stack base: `stacks/claude-code-elastic/docker-compose.yml` running Elasticsearch (single-node, security disabled) and Kibana, both with healthchecks, pinned to Elastic Stack **9.4.2** (all services share this version). `docker compose up -d` from the stack directory brings both to a healthy state and Kibana is reachable on its HTTP port. Record the chosen tech stack and the lint/test commands in `CONVENTIONS.md`.
- [x] Add APM Server to `stacks/claude-code-elastic/` as the Elastic-native OTLP receiver: a service that accepts OTLP/HTTP and OTLP/gRPC, writes to Elasticsearch, and is reachable on its OTLP port. Its config lives under `stacks/claude-code-elastic/config/`. Verify the OTLP endpoint responds.
- [x] Provide Claude Code telemetry configuration in `stacks/claude-code-elastic/`: a documented `.env.example` (and matching docs) that enables `CLAUDE_CODE_ENABLE_TELEMETRY` with the `OTEL_*` exporters for **both metrics and events**, pointed at the APM Server OTLP endpoint. Document exactly how to run a Claude Code session that emits into this stack.
- [x] Add a smoke/verification script under `stacks/claude-code-elastic/scripts/` that brings the stack up, waits for health, and asserts that Claude Code telemetry has landed by querying the relevant Elasticsearch indices. Document the expected indices/fields.
- [x] Add a Kibana view for `claude-code-elastic` (data views / saved objects as importable NDJSON, or documented Discover + APM UI steps) so a user can open Kibana and see the collected Claude Code metrics and events. Write the stack's `stacks/claude-code-elastic/README.md` Quick Tour against this working path.
- [x] Add two Kibana **Saved Searches** (saved-object type `search`, importable NDJSON) under `stacks/claude-code-elastic/kibana/`, built on the existing **Claude Code — Events** data view (id `cce-claude-code-events`) — do NOT create new data views. Each sorts by `@timestamp` desc and filters on its `message` value AND defensively on `service.name: claude-code`. The curated columns below were chosen against live data (see the stack README "Working notes"); high-cardinality correlation IDs (`request_id`, `tool_use_id`), constant/PII fields (`user_*`, `message`, `service.name`), and the `service_summary` stream are intentionally excluded.
  1. **Claude Code — API Requests** — filter `message: claude_code.api_request`; columns: `labels.model`, `labels.query_source`, `labels.speed`, `labels.effort`, `numeric_labels.duration_ms`, `numeric_labels.cost_usd`, `numeric_labels.input_tokens`, `numeric_labels.output_tokens`, `numeric_labels.cache_read_tokens`, `numeric_labels.cache_creation_tokens`.
  2. **Claude Code — Tool Results** — filter `message: claude_code.tool_result`; columns: `labels.tool_name`, `labels.decision_type`, `labels.decision_source`, `labels.success`, `labels.duration_ms`, `labels.tool_input_size_bytes`, `labels.tool_result_size_bytes`.

  Ship them as their own importable NDJSON (alongside `kibana/claude-code-data-views.ndjson`, importing the data view first or together), and extend the stack `README.md` Quick Tour / Saved Objects import step so a user can open either saved search from Discover. Verify both import cleanly and open against real `claude-code` telemetry.
- [x] Extend the Claude Code Saved Searches in `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson` (same conventions as the task above — `search` objects on the `cce-claude-code-events` data view, `@timestamp` desc). **Express the constraints as filter pills, not query-bar text:** put the `message` and `service.name: claude-code` constraints in `searchSourceJSON.filter[]` as `phrase` filters so Discover shows them as removable, labelled filter *pills* (readable for non-experts), with an empty query bar — and **rework the two existing saved searches (API Requests, Tool Results) the same way**. Each filter's `meta.index` must reference `cce-claude-code-events` via a `references[]` entry (name like `kibanaSavedObjectMeta.searchSourceJSON.filter[0].meta.index`). The new correlation key `labels.prompt_id` (groups events within one prompt; present on all three event types) is added as the **last** column of each:
  1. **New — Claude Code — Tool Decisions** — filter `message: claude_code.tool_decision`; columns: `labels.tool_name`, `labels.decision` (accept/reject), `labels.source` (config/user_temporary), `labels.prompt_id`. This is the canonical, always-present record of the permit decision (unlike `tool_result`'s `decision_*`, which is interactive-session-only — see the stack README telemetry notes).
  2. **Update — Claude Code — API Requests** — append `labels.prompt_id` as the final column.
  3. **Update — Claude Code — Tool Results** — append `labels.prompt_id` as the final column.

  Update the stack `README.md` Quick Tour saved-searches table to list Tool Decisions and the added `prompt_id` columns. Verify the file imports cleanly and that every saved search opens with its constraints shown as **filter pills (empty query bar)** against real `claude-code` telemetry; if the hand-authored filter/reference JSON does not render as pills on this Stack version, seed the correct shape by building the filters once in the Discover UI and re-exporting the saved objects.
- [x] Replace the `.env`-and-source telemetry workflow with the agent-side methods documented in `SPEC/SPEC.md` ("Enabling telemetry"). Claude Code does not read `.env` files, so **remove `stacks/claude-code-elastic/.env.example`** and rewrite the stack `README.md` Quick Tour "Point a Claude Code session at the stack" step to present three options, primary first:
  1. **Shell env (one-off, primary)** — a copy-paste `export` block of the same vars (`CLAUDE_CODE_ENABLE_TELEMETRY=1`, the `OTEL_*` exporters, endpoint `http://localhost:8200`, the lowered export intervals), with **both** a bash/zsh form and a fish form; run `claude` from any directory afterward. Emphasize it is ephemeral and creates no files (side-effect-free), so a user can fire their own work's telemetry into the local demo from wherever they like.
  2. **`settings.json` `env` block (persistent)** — the same vars as a JSON `env` block; note project settings load only from the launch directory (so a stack-local `stacks/claude-code-elastic/.claude/settings.local.json` is self-contained, while `~/.claude/settings.json` is global). If a stack-local example is shipped, gitignore `.claude/settings.local.json` via a stack-local `.gitignore`.
  3. **`managed-settings.json` (org enforcement)** — a short note that managed settings have highest precedence and cannot be overridden by users, i.e. the way to force telemetry on for all users across an organization; link the Claude Code monitoring docs rather than duplicating details.

  Keep the existing safety warning (events can carry prompt/tool content). Update the README Layout and any `.env.example` references accordingly. Verify a one-off shell-env session still lands telemetry end to end.
- [x] Add three more Claude Code Saved Searches to `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson`, same conventions as the shipped ones (`search` objects on the `cce-claude-code-events` data view, `@timestamp` desc, constraints as **filter pills** in `searchSourceJSON.filter[]` — `message` + `service.name: claude-code`, empty query bar, `meta.index` wired through `references[]`; each ends in `labels.prompt_id`). Column choices were made against live data — see the stack README telemetry notes.
  1. **Claude Code — User Prompts** — pill `message: claude_code.user_prompt`; columns: `labels.prompt_length`, `labels.prompt` (`<REDACTED>` unless `OTEL_LOG_USER_PROMPTS=1`), `labels.prompt_id`. Keep the `prompt` column — a saved object stores only column *names*, not data; the prompt is redacted by default and is ingested regardless of the column choice (column choice is not a PII control — see SPEC "Identity & PII").
  2. **Claude Code — Hook Executions** — pill `message: claude_code.hook_execution_complete` (the outcome-bearing event; `hook_execution_start` is just an announcement); columns: `labels.hook_event`, `labels.hook_name`, `labels.num_hooks`, `labels.num_success`, `labels.num_blocking`, `labels.total_duration_ms`, `labels.prompt_id`. Excluded on purpose: `hook_source` / `managed_only` — on execution events these collapse to a redundant 2-value pair (`merged` vs `policySettings` ⇄ `managed_only`); granular origins live only on the (uncaptured) `hook_registered` event. Note these `num_*` / `total_duration_ms` are **string** labels (not aggregatable without a runtime-field cast).
  3. **Claude Code — Subagent Completions** — pill `message: claude_code.subagent_completed`; columns: `labels.agent_type`, `labels.agent_source`, `labels.model`, `labels.is_async`, `numeric_labels.duration_ms`, `numeric_labels.total_tokens`, `numeric_labels.total_tool_uses`, `labels.prompt_id`. Excluded on purpose: `is_built_in` (redundant boolean form of `agent_source`).

  Deferred (do NOT add now): `hook_plugin_metrics` (plugin-specific, 20+ numeric fields — a dedicated view later) and `feedback_survey` (noise). Update the stack `README.md` Quick Tour saved-searches table to list the three new searches, and move them from "Still open / later" into the shipped section. Verify the file imports cleanly and all six saved searches open with filter-pill constraints against real `claude-code` telemetry.
- [x] Add a Kibana saved-objects import helper under `stacks/claude-code-elastic/scripts/` so users don't hand-type shell-specific curl loops (the import currently differs between bash `for…do…done` and fish `for…end`). Ship **both** `import-kibana-objects.sh` (POSIX/bash, per CONVENTIONS — `curl` + `jq`, locates its own stack dir like `smoke-test.sh`, `KIBANA_URL` overridable, default `http://localhost:5601`) and `import-kibana-objects.ps1` (PowerShell, mirroring the repo's existing `ralph.sh`/`ralph.ps1` pairing). Each imports the NDJSON in `kibana/` via the Saved Objects `_import?overwrite=true` API, **data views before saved searches** (so the `cce-claude-code-events` reference resolves), and prints the per-file import result. Replace the manual bash/fish curl one-liners in the README Quick Tour step 3 with a one-line invocation of the script (keep a brief note that the manual API call still works). Verify both scripts import all objects cleanly against a live Kibana.
- [x] Wire the shipped dashboard into the import helpers. A starter dashboard NDJSON already exists at `stacks/claude-code-elastic/kibana/claude-code-dashboard.ndjson` (a single by-value dashboard, id `cce-claude-code-overview`, title "Claude Code — Overview (claude-code-elastic)", referencing the `cce-claude-code-metrics` / `cce-claude-code-events` data views — it does NOT bundle them). Add it as the **last** entry of the `FILES` list in **both** `scripts/import-kibana-objects.sh` and `scripts/import-kibana-objects.ps1` (order: data views → saved searches → dashboard, so the data-view references resolve). The stack `README.md` already documents it (Quick Tour step 3 table, Layout, Telemetry reference); do not re-edit those beyond what's needed. Verify all three files import cleanly against a live Kibana and the **Claude Code — Overview** dashboard opens with its panels populated.
- [x] Add a **Claude Code — Permission Mode Changes** saved search to `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson` (seventh search), same conventions as the shipped ones: a `search` object on the `cce-claude-code-events` data view, `@timestamp` desc, constraints as **filter pills** in `searchSourceJSON.filter[]` (`message: claude_code.permission_mode_changed` AND `service.name: claude-code`, empty query bar, each filter's `meta.index` wired through `references[]`), leading with `labels.user_email` (the universal "who" column on every saved search — see the all-searches rework task below) and ending in `labels.prompt_id`. Columns (chosen against live data): `labels.user_email`, `labels.from_mode`, `labels.to_mode`, `labels.trigger`, `labels.prompt_id` — the permission-posture audit view (observed: modes `default`/`acceptEdits`/`plan`/`auto`, `trigger` = `shift_tab`). **The stack `README.md` is already updated for this (Events table, Quick Tour step-3 list, Layout count, Telemetry reference "Saved-search columns" table, counts bumped to seven) — do NOT edit the README.** Verify the file imports cleanly and the search opens with its constraints as filter pills (empty query bar) over real `claude-code` telemetry.
- [x] Add a **Claude Code — MCP Server Connections** saved search to `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson`, same conventions as the shipped ones: a `search` object on the `cce-claude-code-events` data view, `@timestamp` desc, constraints as **filter pills** in `searchSourceJSON.filter[]` (`message: claude_code.mcp_server_connection` AND `service.name: claude-code`, empty query bar, each filter's `meta.index` wired through `references[]`). Columns (chosen against live data), leading with `labels.user_email` (the universal "who" column on every saved search — see the all-searches rework task below): `labels.user_email`, `labels.status`, `labels.transport_type`, `labels.server_scope`, `labels.is_plugin`, `labels.duration_ms`. **Exception to the usual convention: this event has NO `prompt_id` (it fires at session startup, before any prompt) — do NOT add a `prompt_id` column.** The stack `README.md` is already updated (Events table, Quick Tour list, Telemetry reference "Saved-search columns" table) — do NOT edit the README. Verify the file imports cleanly and the search opens with its constraints as filter pills over real `claude-code` telemetry.
- [x] Add a **Claude Code — Auth Events** saved search to `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson` (ninth search), same conventions as the shipped ones: a `search` object on the `cce-claude-code-events` data view, `@timestamp` desc, constraints as **filter pills** in `searchSourceJSON.filter[]` (`message: claude_code.auth` AND `service.name: claude-code`, empty query bar, each filter's `meta.index` wired through `references[]`). Columns, in this order (chosen with the human — identity first, then the event triple, so it reads as an audit line *who | what | outcome | method* and keeps `action`/`success`/`auth_method` adjacent): `labels.user_email`, `labels.action` (`login`/`logout`), `labels.success`, `labels.auth_method` (`oauth`). **One deliberate, documented departure from the usual saved-search conventions:** **no `labels.prompt_id`** — the auth event *does* carry one, but login/logout are account/session-level, not prompt-level, so correlating to a prompt has no audit value. This is the **second `prompt_id`-less search** (after MCP Server Connections, which simply has none) — here it is *present but intentionally omitted*. Do NOT add it. (Leading with `labels.user_email` is **not** a departure — it is the universal "who" column on every saved search; identity is especially apt here since it *varies* on an in-session `/login` account switch.) Still **exclude** the other identity/PII labels (`user_id`, `user_account_id`, `user_account_uuid`, `organization_id`) and the failure-only `error_category` / `status_code` (absent on the `success: true` events that dominate).

  **Behaviour note (already documented in the README, do NOT re-derive):** this event is *logout-biased* — `action: logout` reliably lands but a normal cold-start login is never captured; only an in-session `/login` (account switch) produces an `action: login` event. The stack `README.md` is already updated (Events table + "logout-biased" note, Quick Tour saved-objects list, Telemetry reference "Saved-search columns" table, "Generating events…" note) — do NOT edit the README. Verify the file imports cleanly and the search opens with its constraints as filter pills (empty query bar) over real `claude-code` telemetry.
- [x] Add a **Claude Code — Event Overview** saved search to `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson` (tenth search) — the cross-event-type timeline that the per-`message` searches are filtered slices of. A `search` object on the `cce-claude-code-events` data view, `@timestamp` desc. **Unlike every other saved search it does NOT filter on `message`:** the only filter pill is `service.name: claude-code` (empty query bar, its `meta.index` wired through `references[]`), and `message` becomes a visible *column* (the "what") instead of a constraint. Columns, in this order — *when | who | what | which session | which prompt*: `labels.user_email`, `message`, `session.id`, `labels.prompt_id` (with `@timestamp` as Discover's leftmost time column). Field paths verified against live data: `session.id` (top-level, not under `labels`) and `labels.user_email` are present on **every** event type; `labels.prompt_id` is present on most but blank on session/startup events (`mcp_server_connection`, `hook_registered`, `plugin_loaded`) — keep the column, the blank is informative (the event fired outside any prompt). **`labels.user_email` leads** as the universal "who" column on every saved search (see the all-searches rework task below) — judge it by its real multi-user meaning, not its constant value in this single-user lab. The stack `README.md` is already updated (Quick Tour saved-objects list, Telemetry reference "Saved-search columns" table + its intro paragraph) — do NOT edit the README. Verify the file imports cleanly and that the search opens with only the `service.name` pill (empty query bar), all event types interleaved by `@timestamp`, over real `claude-code` telemetry.
- [x] Make `labels.user_email` the **leading column on every Claude Code saved search**, reworking the six already-shipped ones in `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson` — **API Requests**, **Tool Results**, **Tool Decisions**, **User Prompts**, **Hook Executions**, **Subagent Completions** — to prepend `labels.user_email` as their first column (the other open saved-search tasks above already specify it, so this task only changes the six shipped ones). Rationale: `user_email` is the "who" axis of an audit/observability view and is present on every event type; it was originally excluded as "constant PII" in this single-user lab, but that reasoning is rejected — a column is judged by its real multi-user/multi-org meaning, not its cardinality in the demo (column choice is not a PII control per SPEC "Identity & PII"). Touch **only** the column order: prepend `labels.user_email`, leave each search's existing columns, filter pills, `references[]`, and `@timestamp` desc sort unchanged (those that end in `labels.prompt_id` still do). **The stack `README.md` is already updated** (Telemetry reference "Saved-search columns" intro now states every search leads with `user_email`, and the per-row column lists treat it as the universal lead) — do NOT edit the README. Verify the file imports cleanly and that all six reworked searches open with `user_email` as the first column over real `claude-code` telemetry.
- [x] Add a **Claude Code — Hook Registered** saved search to `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson`, same conventions as the shipped ones: a `search` object on the `cce-claude-code-events` data view, `@timestamp` desc, constraints as **filter pills** in `searchSourceJSON.filter[]` (`message: claude_code.hook_registered` AND `service.name: claude-code`, empty query bar, each filter's `meta.index` wired through `references[]`). Columns (chosen against live data), leading with the universal `labels.user_email`: `labels.user_email`, `labels.hook_event`, `labels.hook_type`, `labels.hook_source`, `labels.plugin_name`. This is the hook **provenance / inventory** view — the companion to **Hook Executions** (runtime outcomes): `hook_registered` fires once per configured hook at session start and is the **only** event carrying the granular `hook_source` origin (`userSettings`/`projectSettings`/`localSettings`/`flagSettings`/`pluginHook`/`policySettings`); `plugin_name` is the supplying plugin when `hook_source: pluginHook`. Keep `hook_source` even though it is all `pluginHook` in this single-user lab (judged by real multi-source meaning, per the all-searches convention). **Exception to the usual convention: this event has NO `prompt_id` (fires at session startup, before any prompt) — do NOT add a `prompt_id` column** (the third `prompt_id`-less search, after MCP Server Connections and Auth Events). **Excluded on purpose:** `hook_name` (not emitted on this event — it lives on the `hook_execution_*` events), `plugin_id_hash` (opaque), `managed_only` (not present on `hook_registered` in this data). The stack `README.md` is already updated (Events table row, Quick Tour saved-objects list, Telemetry reference "Saved-search columns" table + the `prompt_id`-exception intro) — do NOT edit the README. Verify the file imports cleanly and the search opens with its constraints as filter pills (empty query bar) over real `claude-code` telemetry.
- [x] Fix `stacks/claude-code-elastic/scripts/smoke-test.sh`: its **Act** step sends synthetic OTLP/**JSON** to `/v1/metrics` and `/v1/logs`, but this stack's APM Server only decodes **protobuf** — it ignores `Content-Type` entirely and always protobuf-decodes the body, so both POSTs fail with `HTTP 400` / `failed to unmarshal request body: proto: illegal wireType` (the script fails fast on `/v1/metrics`, which is why the gate is red). Root cause confirmed live: tried `Content-Type: application/json`, `application/json; charset=utf-8`, `application/x-protobuf`, and none — all 400; there is no OTLP/JSON toggle in `config/apm-server.yml`. Real Claude Code telemetry is unaffected (it exports `http/protobuf`, and lands fine). **Fix: send OTLP/protobuf instead of JSON.** Replace the two JSON heredocs + `Content-Type: application/json` with the precomputed protobuf payloads below, POSTed with `Content-Type: application/x-protobuf` via `printf '%s' "$B64" | base64 -d | curl --data-binary @- …`. This keeps the "no protobuf tooling" property — it only adds `base64` (coreutils), so add a `command -v base64 … || skip` precondition next to the existing `curl`/`jq` checks. The two payloads were built and **verified live** (each returned `HTTP 200` and landed one doc under `service.name=cce-smoke-test` in `metrics-apm*` / `logs-apm*`):
  - metrics (`ExportMetricsServiceRequest`, one `cce.smoke.counter` sum point): `ClUKIgogCgxzZXJ2aWNlLm5hbWUSEAoOY2NlLXNtb2tlLXRlc3QSLxItChFjY2Uuc21va2UuY291bnRlcjoYChIZAAAytJHUsxgxAQAAAAAAAAAQAhgB`
  - logs (`ExportLogsServiceRequest`, one `cce.smoke.event` record): `CmcKIgogCgxzZXJ2aWNlLm5hbWUSEAoOY2NlLXNtb2tlLXRlc3QSQRI/CQAAMrSR1LMYEAkqEQoPY2NlIHNtb2tlIGV2ZW50Mh8KCmV2ZW50Lm5hbWUSEQoPY2NlLnNtb2tlLmV2ZW50`

  Both bake a **fixed** `timeUnixNano` (≈2026-05-28); that is fine because `assert_landed` queries `_count` by `service.name` with no time filter, so the existing assertion logic and the DISCOVER step are unchanged. (Optional, not required: refresh the 8-byte little-endian `timeUnixNano` at runtime if a current `@timestamp` is wanted.) Keep the `post_otlp` HTTP-200 check and both `assert_landed` calls as-is. Also correct the script's header comment and any `scripts/README.md` line that says the probe is OTLP/JSON "for portability" to describe the protobuf probe. Verify end to end on the live stack: `scripts/smoke-test.sh` reaches **PASS** — both POSTs return `200` and both `assert_landed` (metrics + logs) succeed.
- [x] Add a **traces** data view to `stacks/claude-code-elastic/kibana/claude-code-data-views.ndjson` (a third `index-pattern` object alongside the existing metrics and events data views) so Claude Code's beta trace spans are browsable in Discover. Pattern `traces-apm*` (the data stream OTLP spans land in — confirmed live: a synthetic OTLP/protobuf span POSTed to `/v1/traces` landed in `traces-apm*`; confirm the exact name against live data and scope it as the metrics/events views are if an `.app`-style stream exists), `@timestamp` time field, id `cce-claude-code-traces`, title `Claude Code — Traces`. Bump the file's export-summary `exportedCount` accordingly. The import helper already imports this NDJSON file, so **no `scripts/import-*` change is needed**. The stack `README.md` is already updated (Layout note "(metrics, events, traces)", the three-data-streams bullet, and the Telemetry reference "Traces (beta)" subsection) — do NOT edit the README. Verify the file imports cleanly and the data view opens in Discover; with tracing on (`CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1` + `OTEL_TRACES_EXPORTER=otlp`) and a real session, the `claude_code.interaction` / `llm_request` / `tool` spans show under `service.name: claude-code`. (Traces are best viewed in the APM UI; this data view is the Discover companion.)
- [x] Extend `stacks/claude-code-elastic/scripts/smoke-test.sh` (now protobuf-based) to also verify the **traces** signal, mirroring the existing metrics/logs probes. In the **Act** step add a third synthetic OTLP/protobuf probe to `/v1/traces`, and in **Assert** add a third `assert_landed "traces" "traces-apm*"` (same poll-by-`service.name=cce-smoke-test` logic). Reuse the existing `base64 -d | curl --data-binary @- … -H 'Content-Type: application/x-protobuf'` mechanism. The payload was built and **verified live** (returned `HTTP 200` and landed one span under `service.name=cce-smoke-test` in `traces-apm*`) — an `ExportTraceServiceRequest` with one `cce.smoke.span` span (fixed `timeUnixNano` ≈2026-05-28, same rationale as the metrics/logs payloads): `CmgKIgogCgxzZXJ2aWNlLm5hbWUSEAoOY2NlLXNtb2tlLXRlc3QSQhJAChAREREREREREREREREREREREggiIiIiIiIiIioOY2NlLnNtb2tlLnNwYW4wATkAADK0kdSzGEFAQkG0kdSzGA==`. Update the script's header comment (the 3A Act/Assert description) and the DISCOVER step to include traces / `traces-apm*`. Verify end to end on the live stack: `scripts/smoke-test.sh` reaches **PASS** with all three POSTs returning `200` and all three `assert_landed` (metrics + logs + traces) succeeding.
- [x] **Physically isolate Claude Code's trace spans** into their own data stream and point the traces data view at it. APM writes all OTLP spans to the service-agnostic `traces-apm-default` (no per-service dataset, unlike metrics/events), so isolation must be done by **routing**. Add `scripts/setup-trace-routing.sh` (+ a `.ps1` mirror, same pairing/conventions as `import-kibana-objects.*`: POSIX/bash with `curl`+`jq`, locates its own stack dir, `ES_URL` overridable default `http://localhost:9200`, idempotent, prints the result, fails loudly on non-2xx) that `PUT`s the **`traces-apm@custom`** ingest pipeline:
  ```
  PUT _ingest/pipeline/traces-apm@custom
  { "processors": [ { "reroute": { "if": "ctx.service?.name == 'claude-code'", "namespace": "claudecode" } } ] }
  ```
  This is the sanctioned hook: `traces-apm@default-pipeline` already calls `traces-apm@custom` with `ignore_missing_pipeline: true` (verified live), and `_simulate` confirmed a `service.name: claude-code` span reroutes to **`traces-apm-claudecode`** while other producers (e.g. `cce-smoke-test`) stay in `traces-apm-default`. The target still matches the `traces-apm-*` index template, so it inherits the full APM trace mappings; reroute to the same namespace is a no-op, so no pipeline loop. Then **re-point the traces data view** in `stacks/claude-code-elastic/kibana/claude-code-data-views.ndjson` (`cce-claude-code-traces`) from `traces-apm*` to **`traces-apm-claudecode*`** — now physically scoped, so no `service.name` filter is needed. (Spans captured before the pipeline is installed stay in `traces-apm-default`; that is expected.) The `smoke-test.sh` traces probe is unaffected — its `service.name: cce-smoke-test` span stays in `traces-apm-default`, and its `traces-apm*` assertion still matches. The stack `README.md` is already updated (Quick Tour step 1 routing-setup step, Layout entries for both scripts, the three-data-streams bullet, and the Traces "Trace data model" section describing the reroute) — do NOT edit the README. Verify on the live stack: running `scripts/setup-trace-routing.sh` creates the pipeline, then with tracing on a real session's `claude_code.*` spans land in `traces-apm-claudecode`, and the re-pointed data view opens on them in Discover.
- [x] Rename the trace-isolation namespace from `claudecode` to **`claude_code`** (consistency with the `claude_code` token in the metrics/events datasets, e.g. `metrics-apm.app.claude_code-default`). In `scripts/setup-trace-routing.sh` **and** `scripts/setup-trace-routing.ps1`, change the reroute `namespace` to `claude_code` so `service.name: claude-code` spans land in **`traces-apm-claude_code`** (verified via `_simulate`: underscore is a valid namespace char and the name still matches the `traces-apm-*` template, so APM trace mappings are inherited — keep the namespace approach, NOT a `traces-apm.claude_code-*` dataset, which has no matching APM template and so can't be created as a data stream). Re-point the `cce-claude-code-traces` data view in `stacks/claude-code-elastic/kibana/claude-code-data-views.ndjson` from `traces-apm-claudecode*` to **`traces-apm-claude_code*`**. **Migration:** the existing `traces-apm@custom` pipeline already routes to `traces-apm-claudecode` (~13 throwaway demo spans live there) — after editing, re-run `setup-trace-routing.sh` to overwrite the pipeline, then **delete the stale data stream** (`DELETE /_data_stream/traces-apm-claudecode`; its pre-rename spans have no value). The stack `README.md` is already updated (every `traces-apm-claudecode` reference changed to `traces-apm-claude_code`) — do NOT edit the README. The `smoke-test.sh` traces probe is unaffected (its `cce-smoke-test` span stays in `traces-apm-default`). Verify on the live stack: after re-running the script a new session's spans land in `traces-apm-claude_code`, the re-pointed data view opens on them, and `traces-apm-claudecode` no longer exists.
- [x] Switch the trace-isolation namespace to **per-agent** `agents_<agent>` — for this stack, `claude_code` → **`agents_claude_code`** (data stream `traces-apm-agents_claude_code`). **Rationale (documented in the README "Trace data model" section — do NOT re-derive):** agent trace spans can carry **PII** (prompt / tool I/O / code content once the `OTEL_LOG_TOOL_CONTENT` / `OTEL_LOG_RAW_API_BODIES` gates are on), so they must be isolated from any co-tenant production traces in `traces-apm-default` with **independent deletion / ILM / RBAC**; agent tracing is also **experimental and high-churn** (every `llm_request` + tool call), which shouldn't pollute a production trace store's sampling budget / cardinality. Per-agent (not just a single `agents` category) so each agent gets its own droppable stream **and** the `agents_` prefix yields a cross-agent glob `traces-apm-agents_*` (claude_code, codex, …) that still excludes non-agent traces. In `scripts/setup-trace-routing.sh` **and** `.ps1`, change the reroute so `service.name: claude-code` → `namespace: agents_claude_code` (verified via `_simulate`: lands in `traces-apm-agents_claude_code`, still matches the `traces-apm-*` template). Rework the traces data views in `claude-code-data-views.ndjson` into **two**: (1) re-point the existing `cce-claude-code-traces` (title `Claude Code — Traces`) to **`traces-apm-agents_claude_code*`** (this agent), and (2) **add a second** `index-pattern` object `cce-agents-traces` (title `AI Agents — Traces`) on **`traces-apm-agents_*`** (cross-agent — claude_code, codex, …; excludes non-agent traces), `@timestamp` time field; bump the file's export-summary `exportedCount`. The import helper already covers this NDJSON file, so no `scripts/import-*` change is needed. **`metrics` and `logs` stay on the defaults** (already per-service via `*-apm.app.claude_code-default`; not co-mingled, nothing to route). **Migration:** re-running `setup-trace-routing.sh` overwrites the pipeline so new spans route to `traces-apm-agents_claude_code`. Cleaning up the now-stale `traces-apm-claude_code` data stream is a **manual operator step, out of scope for this task** — do NOT script or invoke a data-stream delete. The stack `README.md` is already updated (every `traces-apm-claude_code` → `traces-apm-agents_claude_code`, plus the isolation rationale) — do NOT edit the README. The `smoke-test.sh` traces probe is unaffected (`cce-smoke-test` stays in `traces-apm-default`). Verify on the live stack: a new session's spans land in `traces-apm-agents_claude_code` and the re-pointed data view opens on them.
- [x] Add a **Claude Code — Interactions** saved search (title `Claude Code — Interactions (claude-code-elastic)`, id `cce-claude-code-interactions`) to `stacks/claude-code-elastic/kibana/claude-code-saved-searches.ndjson` — a `search` object on the **`cce-claude-code-traces`** data view (per-agent traces, `traces-apm-agents_claude_code*`), `@timestamp` desc, giving **one row per interactive turn** (the `claude_code.interaction` root span). **Filter pill `labels.span_type: interaction`** (NOT a `message` filter — traces have no `message`; `span_type` catches the interaction whether it surfaced as a `transaction` or a `span`); **no `service.name` pill** (the data view is physically scoped to the agent). Columns (leading with `labels.user_email`): `labels.user_email`, `labels.user_prompt` (`<REDACTED>` unless `OTEL_LOG_USER_PROMPTS=1`), `numeric_labels.user_prompt_length`, `numeric_labels.interaction_sequence`, `numeric_labels.interaction_duration_ms` (ms; APM-native `transaction.duration.us` is the µs equivalent), `event.outcome`, `trace.id` (drill-down + bridge to that turn's events via `events.trace.id`; the interaction carries no `prompt_id` — 0 trace docs have it). **Interactive-only by nature:** the `interaction` root is emitted only in interactive REPL sessions; **headless / `-p` sessions (e.g. Ralph) have no `interaction` span (and no `user_prompt` event)** — their traces are rooted at `llm_request`/`tool` and appear in **Claude Code — Traces** (below), not here. (Mirrors **User Prompts**, which is likewise interactive-only.) The stack `README.md` is already updated (saved-objects table, Traces notes) — do NOT edit the README. Verify the file imports cleanly and the search opens with the `span_type: interaction` pill (empty query bar), one row per interaction, over real interactive `claude-code` traces.
- [x] Add a **Claude Code — Traces** saved search (title `Claude Code — Traces (claude-code-elastic)`, id `cce-claude-code-traces-overview` — deliberately distinct from the `cce-claude-code-traces` *data-view* id) to `claude-code-saved-searches.ndjson` — a `search` object on the **`cce-claude-code-traces`** data view, `@timestamp` desc, giving **one row per trace across all session types**. **Filter pill `processor.event: transaction`** (the trace root — verified 1:1 live: `transactions == COUNT_DISTINCT(trace.id)`); no `service.name` pill (data view scoped). Columns (leading with `labels.user_email`, common to every root type): `labels.user_email`, `transaction.name`, `transaction.duration.us`, `event.outcome`, `trace.id`. **`transaction.name` is the key column** — it shows the trace's root type: `claude_code.interaction` (interactive turns) vs `claude_code.llm_request` / `claude_code.tool` (headless/`-p` sessions like Ralph, which have no interaction). Use `transaction.duration.us` (µs; present on every transaction — render via the data view's Duration formatter) rather than `interaction_duration_ms` (interaction-only). Interaction-specific fields (`user_prompt`, `interaction_sequence`, …) are intentionally **not** columns here (they populate only on interaction roots) — use **Claude Code — Interactions** for that rich turn view. **Granularity caveat (also documented in the README):** a "trace" is a whole prompt *turn* in interactive sessions (rooted at `interaction`) but a single *API call or tool execution* in headless sessions (each `llm_request`/`tool` is its own trace); `transaction.name` makes this visible. The stack `README.md` is already updated (saved-objects table row + Traces granularity note) — do NOT edit the README. Verify the file imports cleanly, opens with the `processor.event: transaction` pill (empty query bar), one row per trace including Ralph's headless traces.
- [x] Reorder the existing **Claude Code — User Prompts** saved search columns in `claude-code-saved-searches.ndjson` from `labels.prompt_length, labels.prompt` to **`labels.prompt, labels.prompt_length`** (content first, size second) — keep the `user_email` lead, the `message`/`service.name` filter pills, the `prompt_id` trailer, and everything else unchanged; only the order of those two columns changes. (Correction to a shipped saved search → new task per Ralph Loop.) The stack `README.md` is already updated (Saved-search columns table row shows `prompt`, `prompt_length`) — do NOT edit the README. Verify the file imports cleanly and the search opens with `prompt` before `prompt_length`.
- [x] Append top-level **`trace.id`** as the **final column** of every existing **event** saved search in `claude-code-saved-searches.ndjson` (API Requests, Tool Results, Tool Decisions, User Prompts, Hook Registered, Hook Executions, Subagent Completions, Permission Mode Changes, MCP Server Connections, Auth Events, Event Overview) — after the existing trailing `labels.prompt_id` where present. `trace.id` is populated only while tracing is on (events emitted during a trace carry it; startup events like MCP Server Connections may not) and is the cross-signal bridge to the trace spans. Leave all filters / `user_email` lead / other columns unchanged; this only appends one column each. (Correction/extension to shipped saved searches → new task.) The stack `README.md` is already updated (Saved-search columns intro notes the `trace.id` trailer) — do NOT edit the README. Verify the file imports cleanly and `trace.id` appears as the last column on each event saved search.
- [x] Make **`trace.id` a click-through to the APM UI trace view** by adding a URL **field formatter** to the data views that surface it, in `claude-code-data-views.ndjson` — set each affected data view's `fieldFormatMap` so `trace.id` uses `{ "id": "url", "params": { "urlTemplate": "/app/apm/link-to/trace/{{value}}", "labelTemplate": "{{value}}" } }`. Apply to **`cce-claude-code-traces`**, **`cce-agents-traces`**, and the **events** data view **`cce-claude-code-events`** (so the appended `trace.id` event column is clickable too). The import helper already covers this NDJSON file. The stack `README.md` is already updated (Traces "Where to view it" + Saved-search columns intro mention the click-through). **Verify in the running Kibana** that `trace.id` renders as a link and lands on the correct trace in the APM UI — and if `/app/apm/link-to/trace/{{value}}` is not the right deep link on this Stack version, fall back to `/app/apm/traces/{{value}}` (record which worked).
- [x] **Externalize the `traces-apm@custom` pipeline body out of the scripts.** Right now the reroute pipeline JSON is hand-embedded as a heredoc in `scripts/setup-trace-routing.sh` **and** duplicated as a here-string in `scripts/setup-trace-routing.ps1` — two copies, low readability, drift-prone. Move the pipeline body **verbatim** (no behavior change — same `description` + the `reroute` processor) into a single reviewable file **`elasticsearch/trace-routing.pipeline.json`** (new `elasticsearch/` directory, parallel to `kibana/`, for ES-side bootstrap objects). Rework both scripts to **read that file and PUT it** to `_ingest/pipeline/traces-apm@custom` instead of carrying inline JSON — `setup-trace-routing.sh` via `curl … --data @elasticsearch/trace-routing.pipeline.json` (it already `cd`s to the stack dir), `setup-trace-routing.ps1` via `Get-Content -Raw elasticsearch/trace-routing.pipeline.json`. Add the file's existence to each script's preconditions (fail clearly if missing). Keep everything else (idempotent PUT, 2xx/acknowledged check, `ES_URL` override, SKIP on missing `curl`/`jq`) unchanged — this is a pure refactor to a single source of truth. The stack `README.md` is already updated (Layout shows `elasticsearch/trace-routing.pipeline.json` and the scripts' one-line descriptions point at it) — do NOT edit the README. Verify: `scripts/setup-trace-routing.sh` (and the `.ps1`) still install the identical pipeline (the resulting `_ingest/pipeline/traces-apm@custom` is byte-equivalent to before) and `_simulate` still reroutes a `service.name: claude-code` span to `traces-apm-agents_claude_code`.
- [x] **Scope the metrics & events data views to the agent's data stream and drop the redundant `service.name` filter pills.** The `*-apm.app.<service>-default` data streams are keyed by `service.name` (the dataset segment *is* the sanitized service name — verified live: `logs-apm.app.claude_code*` is 100% `service.name: claude-code`, and the smoke-test probe / any other producer land in their own `*-apm.app.<service>-default` streams, e.g. `*.app.cce_smoke_test-default`). So physically scoping the data views makes the `service.name: claude-code` filter pill redundant. In `stacks/claude-code-elastic/kibana/claude-code-data-views.ndjson`, re-point **`cce-claude-code-events`** from `logs-apm.app*` to **`logs-apm.app.claude_code*`** and **`cce-claude-code-metrics`** from `metrics-apm.app*` to **`metrics-apm.app.claude_code*`** (ids unchanged, so the saved searches' and dashboard's references keep resolving). Then in `claude-code-saved-searches.ndjson`, **remove the `service.name: claude-code` phrase filter from every event saved search** (API Requests, Tool Results, Tool Decisions, User Prompts, Hook Registered, Hook Executions, Subagent Completions, Permission Mode Changes, MCP Server Connections, Auth Events, Event Overview): for the per-`message` searches keep the `message` phrase filter and drop the `service.name` filter **and its `references[]` entry** (re-index the remaining filter to `filter[0]` and fix the matching `meta.index` reference name); for **Event Overview** (which has *only* the `service.name` filter) remove it entirely so it has **no filter pill** (the scoped data view is its all-events scope). Keep every column, the `user_email` lead, the `trace.id` trailer, the `@timestamp` desc sort, and the index reference. The **Interactions / Traces** searches (on the traces data view) are unaffected. The stack `README.md` is already updated (data-view rows now show `*.app.claude_code*`; the Saved-search columns intro now says the view is physically scoped and uses a single `message` pill, with Event Overview pill-less) — do NOT edit the README. Verify on the live stack: the two data views resolve only `claude-code` docs (smoke-test excluded); each per-`message` saved search opens with a single `message` filter pill (no `service.name`); Event Overview opens with no filter pill; the Overview dashboard still populates.
- [x] **Externalize Elasticsearch and Kibana configuration into `config/` files**, matching the APM Server pattern already used in this stack. Right now `stacks/claude-code-elastic/docker-compose.yml` carries ES and Kibana settings as `environment:` blocks (`xpack.security.*`, `discovery.type`, `bootstrap.memory_lock` on ES; `ELASTICSEARCH_HOSTS`, `SERVER_HOST` on Kibana) while `apm-server` mounts `./config/apm-server.yml` — three services, two patterns. Move the ES and Kibana settings into **`config/elasticsearch.yml`** and **`config/kibana.yml`**, bind-mounted read-only at `/usr/share/elasticsearch/config/elasticsearch.yml` and `/usr/share/kibana/config/kibana.yml`. The YAML files give a place to comment *why* each setting is set (demo posture: security disabled, single-node) and lift configuration discussion out of compose. **Leave in `environment:`**: `ES_JAVA_OPTS=-Xms1g -Xmx1g` (process env read by the ES startup script — not a YAML setting; the equivalent `jvm.options` file is out of scope, and keeping heap sizing visible in compose is intentional). The Kibana `environment:` block goes away entirely (both keys move to `kibana.yml`, including `server.host: 0.0.0.0` — a deliberate call to keep all Kibana settings in one place even though it is Docker-port-adjacent); the ES `environment:` keeps only `ES_JAVA_OPTS`. The Docker-level `ulimits.memlock` block stays — it is a container concern, not an ES setting. **Bind-mount ownership caveat:** APM uses `--strict.perms=false` because its image is picky about config-file ownership; ES and Kibana images tolerate world-readable bind mounts, so no equivalent flag is needed (host-side `0644` is fine). **Do NOT edit the stack `README.md`** — the Layout block is illustrative and the new files do not need to be enumerated there. Verify the stack: `docker compose config -q` passes; `docker compose up -d` brings all three services healthy; `scripts/smoke-test.sh` reaches PASS unchanged.
- [x] **Phase 1 of the components refactor — extract the Elasticsearch backend into `components/backends/elastic/`**, leaving `stacks/claude-code-elastic/` as a thin composition that `include:`s it. Per `SPEC/SPEC.md` "Repository shape", reusable building blocks live under `components/{backends,paths,agents}/<name>/`; each stack under `stacks/<combo>/` is one composition expressed as a `docker compose include:` plus per-stack glue. This task does the **Backend extraction only** — Agent assets stay in place (they move in a later Phase 2 task). **Behaviour must not change**: the smoke test and Kibana import must produce byte-equivalent results before and after.

  **Moves** (rename only — file contents unchanged):

  - `stacks/claude-code-elastic/docker-compose.yml` (the existing ES + Kibana + APM Server service definitions) → `components/backends/elastic/docker-compose.yml`
  - `stacks/claude-code-elastic/config/` (`apm-server.yml`, `elasticsearch.yml`, `kibana.yml`) → `components/backends/elastic/config/`
  - `stacks/claude-code-elastic/elasticsearch/` (`trace-routing.pipeline.json`) → `components/backends/elastic/elasticsearch/`
  - `stacks/claude-code-elastic/kibana/` (all NDJSON) → `components/backends/elastic/kibana/` (Phase 2 will further split agent-specific NDJSON into `components/agents/claude-code/kibana/`; Phase 1 just relocates wholesale)
  - `stacks/claude-code-elastic/scripts/import-kibana-objects.sh` → `components/backends/elastic/scripts/import-kibana-objects.sh`
  - `stacks/claude-code-elastic/scripts/import-kibana-objects.ps1` → `components/backends/elastic/scripts/import-kibana-objects.ps1`
  - `stacks/claude-code-elastic/scripts/setup-trace-routing.sh` → `components/backends/elastic/scripts/setup-trace-routing.sh`
  - `stacks/claude-code-elastic/scripts/setup-trace-routing.ps1` → `components/backends/elastic/scripts/setup-trace-routing.ps1`

  Bind-mount paths inside the component compose stay relative (`./config/apm-server.yml` etc.); Docker Compose 2.20+ resolves `include:`-d compose files' relative paths against the **included file's directory**, so they continue to resolve correctly after the move. The Backend scripts already self-locate (`SCRIPT_DIR=$(cd "$(dirname -- "$0")" && pwd)`, `STACK_DIR=$(cd "$SCRIPT_DIR/.." && pwd)`), so they continue to find their NDJSON / pipeline files from `components/backends/elastic/`. **Rename the variable / comments** from `STACK_DIR` to `COMPONENT_DIR` (and the header comments' "stack root" → "component root") so the language matches reality; no behavioural change.

  **New `stacks/claude-code-elastic/docker-compose.yml`** — a thin composition. **Preserve `name: claude-code-elastic` explicitly** (so the Compose project name does not get derived from the included file's `name:`):

  ```yaml
  name: claude-code-elastic
  include:
    - ../../components/backends/elastic/docker-compose.yml
  ```

  **New `stacks/claude-code-elastic/scripts/` routing shims** — keep the same script *names* the user is accustomed to. Each (except `smoke-test.sh`) becomes a thin forwarder to the corresponding component script, so `cd stacks/claude-code-elastic && scripts/import-kibana-objects.sh` keeps working and the stack remains genuinely self-contained from the user's command-line perspective (a `ls stacks/claude-code-elastic/scripts/` shows what the stack offers):

  - `scripts/import-kibana-objects.sh` — POSIX/bash one-liner: `exec ../../components/backends/elastic/scripts/import-kibana-objects.sh "$@"` (with `#!/usr/bin/env bash` + a header comment naming the real script). Use `exec` so exit code and signals propagate identically.
  - `scripts/import-kibana-objects.ps1` — PowerShell forwarder: `& "$PSScriptRoot/../../components/backends/elastic/scripts/import-kibana-objects.ps1" @args; exit $LASTEXITCODE` (or equivalent argument-passthrough form). Mirror the `[CmdletBinding()]` + `param(...)` signature so `-KibanaUrl` etc. forward through.
  - `scripts/setup-trace-routing.sh` — POSIX/bash forwarder, same pattern.
  - `scripts/setup-trace-routing.ps1` — PowerShell forwarder, same pattern.

  **Stays in `stacks/claude-code-elastic/scripts/`**: `smoke-test.sh` (no shim, no move). It is **stack property, not Backend property** — the probe target (`http://localhost:8200`) and the assertions (docs landing in `metrics-apm.app.claude_code-default` etc.) are specific to this composition (direct path + Elastic backend); future `*-otelcol-*` stacks will own their own `smoke-test.sh` with a different probe target. The script continues to `cd` to its stack root and runs `docker compose up -d` — which now uses the new thin compose file and pulls in the included backend services.

  **Stack `README.md` — Layout block only**: rewrite the `claude-code-elastic/` tree to reflect the new physical shape — `docker-compose.yml` (thin include), `scripts/` containing `smoke-test.sh` plus the four routing shims — and append a single line noting that services / config / Kibana NDJSON / Backend scripts live in `../../components/backends/elastic/`. The Quick Tour command paths are unchanged (still `scripts/import-kibana-objects.sh`, `scripts/setup-trace-routing.sh`, `scripts/smoke-test.sh`) because the shims preserve them, so do not touch any other section of the README. Do not write a `components/backends/elastic/README.md` yet — the component-level README is deferred until the component has more than one consumer.

  **Verification** (every gate must pass before marking complete):

  1. `for d in stacks/*/; do (cd "$d" && docker compose config -q); done` passes.
  2. `cd stacks/claude-code-elastic && docker compose up -d` brings `cce-elasticsearch`, `cce-kibana`, and `cce-apm-server` to healthy state with the **same container names and host port bindings** as before.
  3. `cd stacks/claude-code-elastic && scripts/import-kibana-objects.sh` imports all three NDJSON files cleanly into a fresh Kibana (`success: true` for each file; `successCount` matches the pre-refactor run).
  4. `cd stacks/claude-code-elastic && scripts/setup-trace-routing.sh` PUTs the `traces-apm@custom` pipeline with `acknowledged: true`; `_simulate` still reroutes a `service.name: claude-code` span to `traces-apm-agents_claude_code`.
  5. `cd stacks/claude-code-elastic && scripts/smoke-test.sh` reaches **PASS** with all three OTLP probes (metrics / logs / traces) landing in the APM data streams.
  6. On a PowerShell host the `.ps1` shims behave equivalently: `scripts/import-kibana-objects.ps1` and `scripts/setup-trace-routing.ps1` exit `0` and produce identical effects.

  If Compose's relative-path resolution around `include:` behaves differently from the assumption above on this version, **fix it by adjusting the bind-mount paths inside the component compose so they resolve unambiguously component-relative** rather than by parameterizing through env vars — keep the composition explainable from its files. Note in the report whatever was needed.
- [x] **Phase 2a of the components refactor — extract the Claude Code agent component into `components/agents/claude-code/`**, moving agent-specific Kibana assets out of the Backend and refactoring the import orchestration. **IDs stay as-is** (`cce-claude-code-*` / `cce-agents-traces`); the ID rename is its own task (Phase 2b). Tree must remain green: from a fresh `docker compose down -v && up -d`, the full Kibana import must succeed and `scripts/smoke-test.sh` must reach PASS.

  **Data-views NDJSON split.** `components/backends/elastic/kibana/claude-code-data-views.ndjson` currently holds four data views — three agent-specific, one cross-agent. Split them by ownership:

  - **Backend keeps** (new file `components/backends/elastic/kibana/agents-data-views.ndjson`):
    - `cce-agents-traces` — the AI Agents cross-agent traces data view (`traces-apm-agents_*`)
    - trailing summary line `{"exportedCount":1,"missingRefCount":0,"missingReferences":[]}`
  - **Agent gets** (new file `components/agents/claude-code/kibana/data-views.ndjson`):
    - `cce-claude-code-metrics`
    - `cce-claude-code-events`
    - `cce-claude-code-traces`
    - trailing summary line `{"exportedCount":3,"missingRefCount":0,"missingReferences":[]}`
  - **Delete** the original `components/backends/elastic/kibana/claude-code-data-views.ndjson` after the split.

  **Whole-file moves** (rename to drop the redundant `claude-code-` prefix — the agent name is now in the directory path):

  - `components/backends/elastic/kibana/claude-code-saved-searches.ndjson` → `components/agents/claude-code/kibana/saved-searches.ndjson` (content unchanged)
  - `components/backends/elastic/kibana/claude-code-dashboard.ndjson` → `components/agents/claude-code/kibana/dashboard.ndjson` (content unchanged)

  **Import-script refactor.** Three layers, each owning what it can see:

  - **Backend `components/backends/elastic/scripts/import-kibana-objects.{sh,ps1}`** — change the `FILES` list to import only `kibana/agents-data-views.ndjson` (the cross-agent data view; what the backend now owns). Update the header comment to describe the new scope: "cross-agent backend assets only — agent-specific assets are imported by each agent's own import script". Keep the `KIBANA_URL` override, the `success: true` gate, and the existing self-locating behaviour unchanged.
  - **New `components/agents/claude-code/scripts/import-kibana-objects.sh` and `.ps1`** — copy the structure of the backend's import scripts (POSIX/bash with `curl` + `jq` for `.sh`, PowerShell 7+ with `Invoke-RestMethod` for `.ps1`), self-locating its own component dir (`COMPONENT_DIR=$(cd "$SCRIPT_DIR/.." && pwd)` and PowerShell equivalent), `FILES` = `kibana/data-views.ndjson`, `kibana/saved-searches.ndjson`, `kibana/dashboard.ndjson` in that dependency order, same `KIBANA_URL` / `-KibanaUrl` override semantics. Header comment names the agent and notes that the data view references resolve because data-views.ndjson is imported first.
  - **Stack `stacks/claude-code-elastic/scripts/import-kibana-objects.{sh,ps1}`** — rewrite the routing shim from a single forward into an **orchestrator** that runs (1) the Backend's import script and then (2) `components/agents/claude-code/scripts/import-kibana-objects.{sh,ps1}` in that order. Forward `$@` / `@args` to both sub-scripts (so `KIBANA_URL=…` / `-KibanaUrl …` still propagates), and fail fast on the first non-zero exit. Keep the script name unchanged so the stack-local UX is preserved (`scripts/import-kibana-objects.sh` still works the same way from a user's perspective).

  **Untouched in this task**: trace-routing scripts (`setup-trace-routing.{sh,ps1}` at both levels), `smoke-test.sh`, the `traces-apm@custom` pipeline body, the stack `docker-compose.yml`, all `cce-*` IDs (those move in Phase 2b), and any README. Do not create `components/agents/claude-code/README.md` yet — defer until there is a second agent for symmetry.

  **Verification** (every gate must pass before marking complete):

  1. `for d in stacks/*/; do (cd "$d" && docker compose config -q); done` passes.
  2. From a fresh state (`cd stacks/claude-code-elastic && docker compose down -v && docker compose up -d`):
     - `scripts/import-kibana-objects.sh` runs the backend's import (one file: `agents-data-views.ndjson`, 1 object) **then** the agent's import (three files: `data-views.ndjson` → 3 objects, `saved-searches.ndjson` → 13 objects, `dashboard.ndjson` → 1 object). All sub-imports report `success: true`; the orchestrator exits 0.
     - `scripts/setup-trace-routing.sh` still PUTs the pipeline (unchanged).
     - `scripts/smoke-test.sh` reaches **PASS**.
  3. Kibana UI: Stack Management → Data Views shows all four (Metrics / Events / Traces / AI Agents — Traces). Discover Open menu shows all 13 saved searches with their filter pills intact. The Claude Code — Overview dashboard opens and its panels populate (real `claude-code` telemetry is flowing into this stack already).
  4. The PowerShell orchestrator `scripts/import-kibana-objects.ps1` is functionally equivalent: exits `0`, produces identical Kibana state.
- [x] **Phase 2b of the components refactor — rename saved-object IDs to drop the `cce-` (claude-code-elastic) stack prefix.** Now that Kibana assets live under `components/{backends,agents}/<name>/kibana/` (not under any one stack), the `cce-` prefix in their IDs and the ` (claude-code-elastic)` suffix in their display names are stale — they encode a stack name on assets the stack no longer owns. This task is a **pure content edit**: rename IDs and display names per the rule below, update every `references[]` block so cross-references still resolve, and update the one matching ID reference in the stack `README.md`. **Container names** (`cce-elasticsearch`, `cce-kibana`, `cce-apm-server` in the Backend compose) are NOT in scope — they are a separate convention and stay as-is until a future task addresses them deliberately.

  **Rename rule** (mechanical — applies uniformly to every saved-object across all NDJSON in `components/{agents,backends}/*/kibana/`):

  - **ID**: strip the leading `cce-` prefix. So `cce-claude-code-events` → `claude-code-events`; `cce-claude-code-overview` → `claude-code-overview`; `cce-agents-traces` → `agents-traces`. The 13 saved searches under `components/agents/claude-code/kibana/saved-searches.ndjson`, the 3 agent data views under `components/agents/claude-code/kibana/data-views.ndjson`, the 1 dashboard under `components/agents/claude-code/kibana/dashboard.ndjson`, and the 1 cross-agent data view under `components/backends/elastic/kibana/agents-data-views.ndjson` all rename this way — every `cce-` prefixed `"id":` field plus every `references[].id` entry that targets a renamed object.
  - **Display name** (`attributes.name` / `attributes.title` on dashboards): strip the trailing ` (claude-code-elastic)` suffix where present. So `"Claude Code — Events (claude-code-elastic)"` → `"Claude Code — Events"`; `"AI Agents — Traces (claude-code-elastic)"` → `"AI Agents — Traces"`. Same reasoning — the stack-name suffix is now stale on component-owned assets.

  **`stacks/claude-code-elastic/README.md` — single update**: in the "Regenerating the Kibana saved objects" section, the curl example references `"id":"cce-claude-code-overview"` in its `-d` JSON payload; update to `"id":"claude-code-overview"`. That is the only saved-object ID reference in the README (verified via grep); no other README changes.

  **Out of scope** (do NOT touch):

  - Container names in `components/backends/elastic/docker-compose.yml` (`cce-elasticsearch`, etc.) — separate convention.
  - The Compose project `name: claude-code-elastic` in `stacks/claude-code-elastic/docker-compose.yml` — this is the stack's identity, unrelated to saved-object IDs.
  - Past PRD task text — completed tasks are immutable history and refer to the old IDs by their then-current names; do not edit them.
  - `SPEC/` files, `CONVENTIONS.md`, the top-level `README.md` — no saved-object IDs are mentioned there.

  **Migration note**: a Kibana instance with the old `cce-*` IDs already imported will end up with orphaned old objects after re-importing the renamed NDJSON (Kibana imports by `id`; the renamed objects have new IDs, so the old ones are not overwritten). The standard lab cleanup is `docker compose down -v && docker compose up -d` before re-running the import. This is acceptable for a demo stack; no in-place migration script is needed.

  **Verification** (every gate must pass before marking complete):

  1. `grep -r "cce-claude-code\|cce-agents" stacks/ components/ README.md` returns **nothing** (the only legitimate `cce-` references left in the working tree are the container names in `components/backends/elastic/docker-compose.yml`, the `cce-` healthcheck output, and any `cce-` in `PRD.md` history). Verify with the precise grep above.
  2. `for d in stacks/*/; do (cd "$d" && docker compose config -q); done` passes.
  3. From a fresh state (`cd stacks/claude-code-elastic && docker compose down -v && docker compose up -d`):
     - `scripts/import-kibana-objects.sh` imports every file with `success: true` and **`missingRefCount: 0`** in each `_import` response (references resolve after rename).
     - `scripts/setup-trace-routing.sh` still PUTs the pipeline (unchanged).
     - `scripts/smoke-test.sh` reaches PASS.
  4. Kibana UI: every data view, saved search, and dashboard opens under its new (no-suffix) display name; the Claude Code — Overview dashboard's panels populate against the live `claude-code` telemetry already flowing.
  5. The `.ps1` orchestrator is functionally equivalent.
- [x] **Phase 3a of the components refactor — add the `otelcol-sidecar` path and the `claude-code-otelcol-elastic` stack (basic relay).** Per `SPEC/SPEC.md`, a Path component adds a transport-layer hop between agent and backend; this introduces a local OpenTelemetry Collector that the agent points at instead of talking to the APM Server directly. Motivation is in `SPEC/threat-model.md` (the (A) network-reliability case) — but **do NOT edit any `SPEC/` file or the top-level docs; they are owned by the human + conversational LLM.** This task stands up the composition and a working relay **without** the durable queue (that is Phase 3b) and **without** Collector self-telemetry (Phase 3c).

  **New `components/paths/otelcol-sidecar/`:**
  - `docker-compose.yml` — one service `otel-collector` on `otel/opentelemetry-collector-contrib:<pin a current version>` (use the **contrib** image, not core — Phase 3b needs its `file_storage` extension). Receives OTLP on `0.0.0.0:4317` (gRPC) + `0.0.0.0:4318` (HTTP), published to `127.0.0.1:4317`/`127.0.0.1:4318` (demo posture). Bind-mounts its config read-only. `depends_on: apm-server` is not expressible across components by default — instead make the exporter resilient to startup ordering (the Collector retries). Pick a `container_name` or leave it Compose-default; do not collide with the `cce-*` backend names.
  - `config/otel-collector.yaml` — minimal pipeline: `otlp` receiver (http+grpc) → `batch` processor → `otlphttp` exporter with `endpoint: http://apm-server:8200`. The Collector reaches the APM Server by its **compose service name** `apm-server` because `include:` merges both components into one Compose project on a shared default network (verify this resolves; if not, declare an explicit shared network in the path component). No `sending_queue`/`file_storage` yet.

  **New `stacks/claude-code-otelcol-elastic/`:**
  - `docker-compose.yml` — `name: claude-code-otelcol-elastic`, `include:` BOTH `../../components/backends/elastic/docker-compose.yml` and `../../components/paths/otelcol-sidecar/docker-compose.yml`.
  - `scripts/` — mirror the claude-code-elastic shims (they forward to the *same* component scripts via the identical `../../components/...` relative paths): `import-kibana-objects.{sh,ps1}` (orchestrate backend + agent imports) and `setup-trace-routing.{sh,ps1}` (backend). Plus its **own** `smoke-test.sh` (stack property): same 3A shape and the same precomputed OTLP/protobuf payloads as `claude-code-elastic`'s, but POSTed to the **Collector** endpoint `http://localhost:4318/v1/{metrics,logs,traces}` (override default via `OTEL_COLLECTOR_URL`), asserting the docs land in the APM data streams in ES (proving agent → Collector → APM → ES). Duplicating the assertion logic with the other stack's smoke-test is acceptable per the per-stack-smoke-test convention; extracting a shared `assert-landed` helper into the backend component is an optional judgment call — note it if done.
  - `README.md` — a Quick Tour for this composition, **self-contained and NOT linking into `SPEC/`** (per the README/SPEC layering: stack READMEs stay self-contained). It must (a) point a Claude Code session at the **Collector** — `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318` (otherwise the same `OTEL_*` block as claude-code-elastic), (b) explain this stack inserts a local Collector between agent and APM, and (c) note the **container-name caveat**: this stack reuses the Elastic backend's fixed `cce-*` container names, so it and `claude-code-elastic` **cannot run at the same time** — `docker compose down` one before bringing up the other.

  **Collector healthcheck gotcha:** the contrib image is minimal (likely no `curl`/shell), like the APM Server image. Either add the `health_check` extension (`:13133`) with a probe that works in that image, or omit the Collector healthcheck and rely on its retry-on-startup. Record what you chose.

  **Verification:** `for d in stacks/*/; do (cd "$d" && docker compose config -q); done` passes (both stacks). With `claude-code-elastic` down, `cd stacks/claude-code-otelcol-elastic && docker compose up -d` brings ES + Kibana + APM Server + Collector healthy; `scripts/smoke-test.sh` reaches **PASS** with all three probes landing in ES via the Collector; `scripts/import-kibana-objects.sh` and `scripts/setup-trace-routing.sh` work (same backend/agent as the other stack). `.ps1` shims equivalent.
- [x] **Phase 3b — give the `otelcol-sidecar` Collector a durable on-disk queue (VPN-outage resilience).** This is the reason the sidecar exists (`SPEC/threat-model.md` (A) case): when the link to the central backend is down, the agent keeps working against the always-reachable local Collector, which **persists** undelivered telemetry to disk and drains it on recovery — no permanent loss. Builds on Phase 3a; **do NOT edit `SPEC/`.**

  **Changes to `components/paths/otelcol-sidecar/`:**
  - In `config/otel-collector.yaml`, add the **`file_storage`** extension (a queue directory under e.g. `/var/lib/otelcol/file_storage`), enable it in `service.extensions`, and set the `otlphttp` exporter's `sending_queue: { enabled: true, storage: file_storage/... }` (with `retry_on_failure` enabled). The queue is per-exporter (forward-looking: a future multi-backend fan-out queues each backend independently).
  - In `docker-compose.yml`, mount a **named volume** at the queue directory so the queue survives container restarts.

  **Resilience verification** — add a check (extend `smoke-test.sh` with an outage phase, or ship a dedicated `scripts/resilience-test.sh`; the dedicated script is cleaner): (1) bring the stack up healthy; (2) `docker compose stop apm-server` to simulate the central backend being unreachable; (3) POST a uniquely-tagged OTLP/protobuf probe (e.g. `service.name=cce-resilience-test`) to the Collector `:4318` — it is accepted and queued, not lost; (4) `docker compose start apm-server` and wait for healthy; (5) assert the probe doc **lands in ES** after recovery (proving it was buffered on disk during the outage and drained on reconnect). Fail if it never lands. Keep the Phase 3a `smoke-test.sh` PASS intact.

  **Verification:** `docker compose config -q` passes; from a fresh up, the resilience check reaches **PASS** (probe sent while `apm-server` was stopped still lands after it restarts); `smoke-test.sh` still PASS. If a stack README mention of the durable-queue behaviour helps, add it to the stack README only (self-contained, no `SPEC/` links).
- [x] **Phase 3c — emit the Collector's own telemetry to the Elastic backend (standard `service.telemetry` path).** A silently-dead sidecar is an audit hole, so the Collector must report its own health. Use the **standard** approach: the Collector's `service.telemetry` subsystem is a *separate path* from the data pipelines, so its self-metrics do **not** ride the agent payload's `file_storage` queue. Builds on Phase 3a/3b; **do NOT edit `SPEC/`** (the design rationale, incl. the central-side heartbeat-absence responsibility that is out of lab scope, is already recorded in `SPEC/threat-model.md` "Observing the sidecar itself").

  **Changes to `components/paths/otelcol-sidecar/config/otel-collector.yaml`:** configure `service.telemetry.metrics` to export the Collector's internal metrics via OTLP to the **same APM Server** (`http://apm-server:8200`, `http/protobuf`) — e.g. a `periodic` reader with an `otlp` exporter (match the Collector version's `service.telemetry` schema). Give the self-telemetry a recognizable `service.name` (e.g. `otelcol-sidecar`) so it is distinguishable from `claude-code` and the smoke-test producers in ES. Leave the data pipeline (Phase 3a/3b) unchanged.

  **Verification:** from a fresh up, after the Collector has run a minute, assert that `otelcol_*` self-metrics land in ES under the chosen `service.name` (query `metrics-apm*` for it) — in particular **`otelcol_exporter_queue_size`** (the key health signal: a growing persistent queue means the central link is failing). Confirm the self-metrics arrive on their own `service.name`, separate from `claude-code`. `smoke-test.sh` and the Phase 3b resilience check still PASS. Optionally note in the stack README that the Collector reports its own health (self-contained, no `SPEC/` links). **Out of scope (do not build):** central-side heartbeat-absence alerting — it needs the real fleet roster and lives on the operator's backend, per `SPEC/threat-model.md`.
- [x] **Phase 3d — enrich agent telemetry with host identity at the `otelcol-sidecar` Collector.** Claude Code's native telemetry carries OS/arch resource attributes but **no `host.name` / `host.hostname`** (verified live), so a document can be attributed to a person (`labels.user_email`) and a session (`session.id`) but **not to a device**. A local Collector is the natural place to add device identity to every passing signal — a second concrete payoff of the sidecar path beyond VPN-resilience. Builds on Phase 3a; **do NOT edit `SPEC/`** (the absence and this remedy are already recorded in `SPEC/claude-code-telemetry.md` "No host identity").

  **Changes to `components/paths/otelcol-sidecar/`:**
  - In `config/otel-collector.yaml`, add a **`resourcedetection`** processor with the `system` detector (`hostname_sources: [os]`) and **`override: false`** (so it adds `host.name` only where absent — it must NOT clobber the agent's existing `host.os.*` / `host.architecture` attributes). Add it to **each** of the three pipelines' `processors` list **before `batch`** (`[resourcedetection, batch]`). Optionally also let it set `host.id`.
  - In `docker-compose.yml`, set the Collector service's **`hostname:`** to a readable, deterministic value (e.g. `otelcol-sidecar-demo`) so the detected `host.name` is stable and recognizable in ES rather than a random container ID.

  **Container caveat (document in the stack README only — self-contained, no `SPEC/` links):** because the Collector runs in a *container*, the `system` detector reports the **container's** hostname (the value set above), **not the laptop's**. The dockerized stack demonstrates the *mechanism*; a real fleet runs the Collector as a host service (systemd / launchd), where it picks up the true machine name. Note this explicitly so the demo value isn't mistaken for real device attribution.

  **Verification:** `docker compose config -q` passes. From a fresh up of `claude-code-otelcol-elastic`, run `scripts/smoke-test.sh` (its probes go through the Collector) and then confirm in ES that the smoke-test docs now carry a populated **`host.name`** (= the configured Collector hostname) — i.e. query `metrics-apm*` / `logs-apm*` / `traces-apm*` for `service.name=cce-smoke-test` and check `host.name` is non-empty, where before Phase 3d it was absent. The Phase 3b resilience check and Phase 3c self-telemetry still pass. (If the OTLP `host.name` resource attribute surfaces in ES as `host.hostname` rather than `host.name` on this Stack version, accept either and record which.)
- [x] **Fix: component `.ps1` scripts leak `Set-Location` into the caller's PowerShell session.** The three component scripts — `components/backends/elastic/scripts/import-kibana-objects.ps1`, `components/backends/elastic/scripts/setup-trace-routing.ps1`, and `components/agents/claude-code/scripts/import-kibana-objects.ps1` — run `Set-Location -LiteralPath $ComponentDir` so their relative `kibana/` / `elasticsearch/` file paths resolve, and never restore the original location. A `.ps1` executes **in the caller's PowerShell session** (unlike the `.sh` mirrors, whose `cd` is confined to their own subprocess), so after running any of them — directly or through either stack's `scripts/` shims — the user's shell is left parked in the component directory. Reported live by the user; not the intended behaviour. **Fix by removing the `Set-Location` entirely**: keep the `$ComponentDir` resolution and reference every file **absolutely** (`Join-Path $ComponentDir 'kibana/…'`, `Join-Path $ComponentDir 'elasticsearch/trace-routing.pipeline.json'`) — preferred over `Push-Location`/`Pop-Location` because it has no location side effect on **any** exit path (including `exit 1` after `Write-Error`). The `.sh` scripts and the stack shims need no change. **Verify** from a fixed cwd (e.g. the repo root): run each of the three `.ps1` directly AND via the `stacks/claude-code-elastic` and `stacks/claude-code-otelcol-elastic` shims, on a success path and on a failure path (e.g. Kibana down) — behaviour and exit codes unchanged, and `$PWD` afterwards is identical to before in **every** case; both stacks' smoke-tests still PASS.
- [x] **Rename the sidecar Collector config to dodge a SchemaStore false positive: `components/paths/otelcol-sidecar/config/otel-collector.yaml` → `config/collector.yaml`.** Editors using yaml-language-server with SchemaStore (Zed, VS Code) auto-apply the **OpenTelemetry Declarative Configuration** schema (the *SDK* config format, which requires a top-level `file_format` property) to any file matching its greedy `otel*.yaml` glob — so the *Collector* config gets a bogus `Missing property "file_format"` warning in every contributor's editor. The file itself is a valid Collector config (the running stack proves it); the fix is purely a rename to a basename that does not start with `otel` (`collector.yaml` — the `otelcol-sidecar/config/` directory keeps the context clear; do NOT pick `otelcol.yaml`, which still matches the glob). Update the **three references**, all inside `components/paths/otelcol-sidecar/docker-compose.yml`: the bind-mount source `./config/otel-collector.yaml:/etc/otelcol-contrib/config.yaml:ro` and the two header/inline comments naming the file (the in-container target path and the `--config` flag stay unchanged). No other file references the name (`SPEC/`, READMEs, scripts checked — none; PRD history is immutable and stays as-is). Verify: `git mv` so history follows; `docker compose config -q` passes for both stacks; `cd stacks/claude-code-otelcol-elastic && docker compose up -d` brings the Collector up healthy with the renamed config; `scripts/smoke-test.sh` reaches PASS; and the YAML warning is gone when the renamed file is opened in a SchemaStore-backed editor.
- [x] **De-stack-ify the Elastic backend component's compose identity.** `components/backends/elastic/docker-compose.yml` still opens with the stack-era header (`# claude-code-elastic — Elastic Stack base`, "…receiver Claude Code's telemetry is pointed at") and carries a top-level **`name: claude-code-elastic`** — leftovers from when this file *was* the stack. It is now an agent-agnostic backend component included by two stacks (`include:` ignores an included file's `name:`, so the field is dead weight — but a direct `docker compose up` from the component directory would create a project masquerading as the `claude-code-elastic` stack, and the header misleads readers). **Remove the `name:` line entirely** and rewrite the header comment to describe what the file now is: the reusable Elastic backend component (Elasticsearch + Kibana + APM Server as the OTLP receiver), agent-agnostic, composed into stacks via `include:`. No other file changes. Verify: `for d in stacks/*/; do (cd "$d" && docker compose config -q); done` passes; both stacks still resolve their own project names (`docker compose config | head` from each stack dir shows `name: claude-code-elastic` / `name: claude-code-otelcol-elastic` respectively); the running stack is unaffected (`docker compose up -d` from `stacks/claude-code-otelcol-elastic` is a no-op).
- [x] **Rename the backend container names `cce-*` → `aol-*`.** The `cce-` (claude-code-elastic) prefix on `container_name: cce-elasticsearch` / `cce-kibana` / `cce-apm-server` predates the components refactor: the backend is no longer owned by that stack (or by any one agent), so the prefix misleads. Rename to the repo-scoped **`aol-`** (agent-observability-lab): `aol-elasticsearch`, `aol-kibana`, `aol-apm-server` in `components/backends/elastic/docker-compose.yml`. Update **every reference** (grep `cce-` to enumerate; currently: the wait-for-healthy container lists in `stacks/claude-code-elastic/scripts/smoke-test.sh` and `stacks/claude-code-otelcol-elastic/scripts/smoke-test.sh`, any container references in `stacks/claude-code-otelcol-elastic/scripts/resilience-test.sh`, the header comment of `stacks/claude-code-otelcol-elastic/docker-compose.yml`, and the container-name mentions in both stack `README.md`s). **Out of scope here:** the probe identities `service.name: cce-smoke-test` / `cce-resilience-test` baked into the precomputed OTLP/protobuf payloads — renamed by the **following task** (the same-length `cce`→`aol` swap keeps the protobuf valid). Keep the fixed container names (they don't constrain anything the fixed host ports don't already constrain, and the smoke-tests' `docker inspect <name>` health-waits stay simple). Verify: `grep -rn "cce-" components/ stacks/` afterwards shows **only** the out-of-scope probe service-names (in the smoke/resilience scripts' payload comments and assertions) and immutable history (`PRD.md`, `reports/`); `docker compose config -q` passes for both stacks; from a fresh `docker compose down && up -d` of one stack, its `smoke-test.sh` (and for the otelcol stack also `resilience-test.sh`) reaches PASS against the renamed containers.
- [x] **Rename the probe identities `cce-*` → `aol-*` inside the precomputed OTLP payloads.** Companion to the container-name task above — finishes eradicating the stale `cce-` prefix. Three verification scripts bake probe identities into precomputed base64 OTLP/protobuf payloads: `service.name: cce-smoke-test` plus the inner names `cce.smoke.counter` / `cce smoke event` / `cce.smoke.event` / `cce.smoke.span` (in `stacks/claude-code-elastic/scripts/smoke-test.sh` and `stacks/claude-code-otelcol-elastic/scripts/smoke-test.sh`), and `service.name: cce-resilience-test` plus `cce.resilience.event` (in `stacks/claude-code-otelcol-elastic/scripts/resilience-test.sh`). **No protobuf tooling is needed to regenerate the blobs:** every rename swaps the 3-byte prefix `cce` → `aol` at identical byte length, so the protobuf length prefixes are untouched — base64-decode each payload, replace those exact ASCII strings (python/perl; avoid a blanket binary `sed`, and do NOT touch non-string bytes), re-encode. Update alongside each script's `SERVICE_NAME=` variable and the comments naming the probes. Previously-ingested docs under the old names (and their `*-apm.app.cce_smoke_test-default` data streams) remain in ES until a `down -v` — acceptable demo residue; note it in the report. Verify: each script reaches **PASS** on the live stack (its own assertion proves the regenerated payloads decode and land — `service.name: aol-smoke-test` / `aol-resilience-test` documents present in the APM data streams); afterwards `grep -rn "cce" components/ stacks/` returns **nothing** (full eradication — only `PRD.md` / `reports/` history keeps the old prefix).
- [x] **Add the otelcol-sidecar path component's Kibana data view and import wiring.** The sidecar's self-telemetry (Phase 3c) lands in `metrics-apm.app.otelcol_sidecar-default` but no Kibana asset surfaces it. Following the precedent that components own their Kibana assets (as `components/agents/claude-code/kibana/` does), give the **path component** its own:

  - **New `components/paths/otelcol-sidecar/kibana/data-views.ndjson`** — one `index-pattern` object, id **`otelcol-sidecar-metrics`**, display name **`OTel Collector Sidecar — Metrics`**, title **`metrics-apm.app.otelcol_sidecar*`**, `timeFieldName: @timestamp`, same NDJSON object shape as the agent's `data-views.ndjson` (`managed: false`, empty `references`, `coreMigrationVersion: 9.4.2`, trailing export-summary line with `exportedCount: 1`). Physically scoped like the agent views — no `service.name` filter needed. **No saved searches**: the sidecar emits only metrics, and metric documents don't scan as Discover rows (the same reason the Claude Code metrics view has none) — the visualization surface is the Health dashboard (next task).
  - **New `components/paths/otelcol-sidecar/scripts/import-kibana-objects.sh` / `.ps1`** — modelled on the agent component's import scripts **as amended by the earlier Set-Location-leak task** (the `.ps1` must use absolute `Join-Path $ComponentDir …` paths, no `Set-Location`). `FILES` = `kibana/data-views.ndjson` only for now (the next task appends the dashboard). Same `KIBANA_URL` / `-KibanaUrl` override and `success: true` gate.
  - **Extend the stack orchestrator** `stacks/claude-code-otelcol-elastic/scripts/import-kibana-objects.{sh,ps1}` from two legs to three: backend → agent → **path**, forwarding args, fail-fast. `stacks/claude-code-elastic` is untouched — a stack without the sidecar gets no sidecar view.
  - If the otelcol stack `README.md` enumerates the imported saved objects, add the new data view to that list (self-contained, no `SPEC/` links); otherwise no README change.

  **Verification:** against the live stack, `cd stacks/claude-code-otelcol-elastic && scripts/import-kibana-objects.sh` runs all three legs with `success: true` each and exit 0; the **OTel Collector Sidecar — Metrics** data view opens in Discover and resolves the `otelcol_*` fields; the `.ps1` orchestrator is equivalent and leaves `$PWD` unchanged; `stacks/claude-code-elastic/scripts/import-kibana-objects.sh` still runs its two legs unchanged.
- [x] **Build the "OTel Collector Sidecar — Health" dashboard.** The Discover-side gap for the sidecar's metrics-only telemetry is intentional (no saved searches — see the previous task); the visualization surface is a Lens dashboard on the **`otelcol-sidecar-metrics`** data view, built in the Kibana UI against live data and exported (the same production method as the Claude Code — Overview dashboard).

  **Populate real data first:** run `stacks/claude-code-otelcol-elastic/scripts/resilience-test.sh` once before building — the induced outage both puts a real spike in `otelcol_exporter_queue_size` (so the queue panel is designed against the shape it exists to show) **and** materializes the failure-series fields (`otelcol_exporter_send_failed_*` appear only once a send has failed; refresh the data view's field list afterwards).

  **Panels** (Lens, by-value, all on the sidecar data view — exact aggregations are implementation judgment; field names verified live):

  1. **Queue depth** *(the headline)* — `max(otelcol_exporter_queue_size)` over time, broken down by `labels.data_type` (logs/metrics/traces are separate series). An outage reads as a rising staircase that drains to zero on recovery.
  2. **Relay throughput** — `otelcol_receiver_accepted_{spans,metric_points,log_records}` vs `otelcol_exporter_sent_{…}` as rates; proves the relay is flowing per signal.
  3. **Failures** — `otelcol_exporter_send_failed_*` (now present after the resilience run) and `otelcol_receiver_refused_*`; the panel must tolerate the enqueue-failure fields (`otelcol_exporter_enqueue_failed_*`) not existing yet — same arrives-later posture as `claude_code.pull_request.count` in the Overview dashboard.
  4. **Process** — `otelcol_process_memory_rss`, `otelcol_process_cpu_seconds` (counter → rate), `otelcol_process_uptime` (resets reveal restarts).

  **Export and ship:** export via the saved-objects export API with `includeReferencesDeep: false` (the data view stays a reference), give it the stable id **`otelcol-sidecar-health`** and title **`OTel Collector Sidecar — Health`**, save as `components/paths/otelcol-sidecar/kibana/dashboard.ndjson`, and append it to the path import script's `FILES` (data view first). **Verification:** from a clean Kibana (saved objects cleared or fresh import), the path import brings in data view + dashboard with `success: true`; the dashboard opens with the queue-spike history visible in panel 1 and every panel populated (failures panel may legitimately show zero outside outages); the stack orchestrator imports all three components' assets end to end.
- [x] **Inject an organization-assigned asset ID at the sidecar (`env` detector → `host.id`).** Companion to the Phase 3d host-name enrichment: `host.name` is the *machine's own* identity, but fleet auditing wants the *organization's* identity — the asset-management number. That is injected, not detected, so add the **`env`** detector in front of `system` in `components/paths/otelcol-sidecar/config/otel-collector.yaml` (`detectors: [env, system]` — first-listed wins conflicts, and `override: false` still protects agent-supplied attributes), and feed it from the Collector's environment in `components/paths/otelcol-sidecar/docker-compose.yml`:

  ```yaml
  environment:
    - OTEL_RESOURCE_ATTRIBUTES=host.id=${AOL_ASSET_ID:-aol-demo-asset-0001}
  ```

  Compose interpolates `AOL_ASSET_ID` from the host shell at `up` time (demo default otherwise) — the lab analogue of an MDM writing the per-device asset number into the Collector service definition at install time; the agent is untouched. The attribute key is **`host.id`** deliberately: it is an ECS/APM-modeled field, so it lands as a **top-level, aggregatable `host.id`** (the join axis to an asset inventory) rather than flattening into `labels.*` as a custom key would. Document the pattern briefly in the otelcol stack `README.md` (self-contained, no `SPEC/` links): the env-var knob, the demo default, and the MDM analogy — and that a real fleet would also consider the `system` detector's own `host.id` (machine-id; default-off) as the *intrinsic* alternative, with `[env, system]` order meaning the organizational ID wins when both exist. **Verification:** with the stack up (recreate the Collector so the env applies), relayed telemetry — the smoke-test probes and a real session — carries top-level **`host.id: aol-demo-asset-0001`** in ES alongside the Phase 3d `host.name`; re-`up` with `AOL_ASSET_ID=test-override-42` lands `host.id: test-override-42`; agent-supplied attributes remain unclobbered; `smoke-test.sh` and `resilience-test.sh` still PASS.
- [x] Add a **Claude Code — Tool Output** saved search to `components/agents/claude-code/kibana/saved-searches.ndjson` — the dedicated view for the `OTEL_LOG_TOOL_CONTENT` documents, which match no `message: claude_code.*` filter and are otherwise visible only in Event Overview. A `search` object on the **`cce-claude-code-events`** data view, `@timestamp` desc, filter pill **`message: tool.output`** (empty query bar). Columns: `labels.output` (Bash stdout), `labels.content` (Read file contents), `labels.diff` (Edit structured diff), `labels.file_path` (Edit only), `span.id`, `trace.id`. **Documented convention exception: NO `labels.user_email` lead** — these documents carry no `labels.user_*` / `prompt_id` envelope at all (see `SPEC/claude-code-telemetry.md`, Content-exposure gates). The view is empty unless `OTEL_LOG_TOOL_CONTENT=1` *and* tracing are on — that is expected; it is the standing audit lens for the gate. Verify the file imports cleanly and the search opens with the single pill over real `tool.output` documents (the 2026-06-05 01:31–01:55 window has live ones).
- [x] Add a **Claude Code — Tool I/O (joined)** view — the repo's **first ES|QL-based saved Discover session** (no data view / filter pills; an ES|QL query, saved as a Discover session and shipped in `components/agents/claude-code/kibana/saved-searches.ndjson`). It reconstructs **one row per tool call** (tool name + input + output) by unioning the events and traces streams and joining in two STATS hops — `span.id` merges `tool.output` with its `claude_code.tool` span (which carries `tool_name` / `tool_use_id`), then `tool_use_id` merges in `claude_code.tool_result` (which carries `tool_input`, `user_email`, `prompt_id` — restoring the attribution the raw `tool.output` lacks). Working prototype (verified live; polish the multivalue handling — `input` dropped on some rows — before shipping):
  ```
  FROM logs-apm.app.claude_code*, traces-apm-agents_claude_code*
  | WHERE message == "tool.output" OR message == "claude_code.tool_result" OR labels.span_type == "tool"
  | EVAL body = COALESCE(labels.output, labels.content, labels.diff)
  | STATS content = VALUES(body), tuid = VALUES(labels.tool_use_id), tool = VALUES(labels.tool_name), input = VALUES(labels.tool_input), email = VALUES(labels.user_email), pid = VALUES(labels.prompt_id), tid = VALUES(trace.id), ts = MIN(@timestamp) BY span.id
  | EVAL tuid = MV_FIRST(tuid)
  | STATS ts = MIN(ts), user_email = VALUES(email), tool = VALUES(tool), input = VALUES(input), content = VALUES(content), prompt_id = VALUES(pid), trace_id = VALUES(tid) BY tuid
  | WHERE content IS NOT NULL
  | SORT ts DESC
  ```
  Final columns: `ts`, `user_email`, `tool`, `input`, `content`, `prompt_id`, `trace_id`. Verify against the 2026-06-05 01:31–01:55 live window (Bash/Read/Edit rows; the 01:54 Bash row must show input *and* output together).
- [x] Add a **Claude Code — API Bodies** saved search to `components/agents/claude-code/kibana/saved-searches.ndjson` — low practical utility (the stored body is a ~1 KB head fragment; see the truncation chain in `SPEC/claude-code-telemetry.md`) but kept **as the lab's standing lens on the `OTEL_LOG_RAW_API_BODIES` gate**. A `search` object on **`cce-claude-code-events`**, `@timestamp` desc, one filter pill: **`message` is one of `claude_code.api_request_body`, `claude_code.api_response_body`** (a `phrases` filter). Columns: `labels.user_email`, `message`, `labels.model`, `labels.query_source`, `labels.body_length`, `labels.body_truncated`, `labels.prompt_id`, `trace.id`. **Deliberately excluded:** `labels.request_id` and the `labels.body` itself (the 1 KB fragment is for row-expand, not a column). Verify import + opens over the live body events (2026-06-05 02:09+ window).
- [x] Surface the `OTEL_LOG_TOOL_DETAILS`-gated fields as columns on three shipped saved searches in `components/agents/claude-code/kibana/saved-searches.ndjson` (gate-aware columns, same precedent as User Prompts' `prompt`: they simply stay empty while the gate is `0`): (1) **Tool Results** — append `labels.tool_input` then `labels.tool_parameters` (keep the existing `tool_*_size_bytes` columns); (2) **Tool Decisions** — append `labels.tool_parameters` (shows *what* was accepted/rejected); (3) **MCP Server Connections** — insert `labels.server_name` immediately after the `labels.user_email` lead (server identity is the primary scan column; the field exists only when the gate is on). Everything else (filters, sorts, other columns, references) unchanged. Verify the file imports cleanly and each search opens with the new columns placed as specified.
- [x] Add a **Claude Code — API Errors** saved search to `components/agents/claude-code/kibana/saved-searches.ndjson` — the API failure lens, and a base signal for future alerting. A `search` object on **`cce-claude-code-events`**, `@timestamp` desc, one filter pill: **`message: claude_code.api_error`**. Columns: `labels.user_email`, `labels.model`, `labels.error`, `numeric_labels.status_code`, `numeric_labels.attempt`, `numeric_labels.duration_ms`, `labels.prompt_id`, `trace.id`. **Deliberately excluded:** `labels.request_id` (opaque, matching API Requests) and the `claude_code.internal_error` events (carry only `error_name: "Error"` — no standalone value; their substance is the paired 500-class `api_error`). The event shape is in the Events table of `SPEC/claude-code-telemetry.md`. Verify the file imports cleanly and the search opens over the live failure window (2026-06-05 03:01–03:32 UTC holds 7 `api_error` documents from a real 529/500 incident).
- [x] Add a **Claude Code — API Retries Exhausted** saved search to `components/agents/claude-code/kibana/saved-searches.ndjson` — companion to API Errors above (same file, same data view, the two can land together). A `search` object on **`cce-claude-code-events`**, `@timestamp` desc, one filter pill: **`message: claude_code.api_retries_exhausted`**. Columns: `labels.user_email`, `labels.model`, `labels.error`, `numeric_labels.status_code`, `numeric_labels.total_attempts`, `numeric_labels.total_retry_duration_ms`, `labels.prompt_id`, `trace.id`. The event's content duplicates its paired terminal `api_error` (see the Events table of `SPEC/claude-code-telemetry.md`) — this view exists because "retries fully exhausted" is the **distinct signal future alerting hangs off**, and it should be observable on its own; an empty view in normal operation is expected. Verify the file imports cleanly and the search opens over the live failure window (2026-06-05 03:01–03:19 UTC holds 3 `api_retries_exhausted` documents: 529×2, 500×1).
- [x] **P1 prompt-audit, plaintext-ingest vertical slice — capture prompts at the source and ship them to a dedicated Elasticsearch index on a path independent of OTLP.** This is the first build increment of the `SPEC/pii-capture-verification-plan.md` P1 pattern and the start of the `SPEC/IDEAS.md` audit-trail feature ("when / who / what prompt"). Decisions locked with the human in conversation: a **prompt-specific** `prompts-audit` index (not a generalized `agent-audit`); **ES direct POST only** (no local JSONL buffer this phase); **prompt scope first** (tool results / LLM info are later increments); **no server-side reshaping** (an ingest-pipeline approach was rejected — it would send the plaintext prompt over the wire before any chance to seal it; the endpoint must build *and eventually seal* the document); **zero-install hook** on Windows + mac/Linux; **`cwd` excluded** as PII. Encryption is explicitly deferred — plaintext now, CMS `prompt_cipher` sealing later (the mapping reserves the field so sealing needs no mapping change).

  **Built (component layout per `SPEC/IDEAS.md` "Lab structure — CONFIRMED"):**
  - `components/backends/elastic/elasticsearch/prompts-audit.index.json` — the index mapping (single source of truth): `dynamic: strict` (a hook bug fails loud), a keyword **envelope** (`agent`, `user_email`, `organization`, `session_id`, `hostname`), the `prompt` **text**, and a reserved `prompt_cipher` (keyword, `index`/`doc_values` off) for the sealing phase. `agent` keyword added so the store stays agent-agnostic (codex/opencode would land here too — it is a **backend** property, not the agent's). `cwd` deliberately omitted (working-directory paths are PII).
  - `components/backends/elastic/scripts/setup-prompt-audit.{sh,ps1}` — idempotent index create (checks existence first, since a create PUT is not idempotent), mirroring the `setup-trace-routing.*` precedent (`ES_URL`/`-EsUrl` override, curl+jq / Invoke-RestMethod, self-locating, the `.ps1` uses absolute `Join-Path` with no `Set-Location`). (These are operator scripts, so curl+jq per CONVENTIONS is fine — unlike the hook.)
  - `components/agents/claude-code/hooks/capture-prompt.{sh,ps1}` — the `UserPromptSubmit` hook (new `hooks/` dir beside `kibana/`/`scripts/`): reads the event JSON from stdin, resolves identity locally from `~/.claude.json` (`.oauthAccount.emailAddress`/`.organizationName`), builds the document **entirely on the endpoint** (so a later phase can seal `prompt` → `prompt_cipher` with `openssl cms` before anything leaves the box), and POSTs it straight to `prompts-audit`. **Zero install for fleet rollout:** the `.sh` depends only on `curl` + `awk` (POSIX base — `awk` lifts JSON string values verbatim, no `jq`; the doc is assembled with `printf`); the `.ps1` uses only built-in PowerShell and sends a **UTF-8 byte body** (Windows PowerShell 5.1, the fleet default, otherwise mangles Japanese). **Contract verified against the hook docs:** writes **nothing to stdout** (UserPromptSubmit exit-0 stdout is injected into the model context), **always exits 0** (best-effort — ES down never blocks the user's prompt), and curl/`Invoke-RestMethod` use a short timeout to fail fast.
  - `stacks/claude-code-elastic/scripts/setup-prompt-audit.{sh,ps1}` and `stacks/claude-code-otelcol-elastic/scripts/setup-prompt-audit.{sh,ps1}` — forwarding shims (the audit store is a backend property; identical in both stacks — in the otelcol stack the audit path bypasses the Collector and talks straight to ES).
  - Both stack `README.md`s gained a self-contained **"Prompt audit trail (independent of OTLP)"** section (index-create step + hook-registration `settings.json` snippet + plaintext caveat) and a Layout entry for the new files.

  **Verified live** against the running otelcol stack: `setup-prompt-audit.sh` creates `prompts-audit` (`acknowledged: true`) and is idempotent on re-run; a **real interactive session's** prompt landed with identity resolved from `~/.claude.json` and `session_id` matching that session's OTLP `session.id` (306 analytics events — proving the audit↔analytics join, and that OTLP's `labels.prompt` is `<REDACTED>` while the audit store has the full text); both hooks round-trip arbitrary content (quotes / backslash / newline / Japanese / braces) **exactly**, compared ES-side via a `equals` script field, including under **stock Windows PowerShell 5.1**; the `dynamic: strict` mapping rejects a stray field with HTTP 400; `docker compose config -q` passes for both stacks. A verification gotcha was recorded: Windows `jq`/`curl` translate LF→CRLF on stdout at read-back, so round-trip checks must compare ES-side, not via piped client output (the stored bytes are clean LF). Synthetic verification documents were deleted afterward. **Next increments (separate tasks):** CMS sealing (`prompt` → `prompt_cipher`) with a plaintext/sealed switch; tool-result capture (`PostToolUse`); LLM-info capture (`Stop` → transcript); a Kibana data view + saved search for `prompts-audit`.
- [x] **Stand up the `codex-cli-elastic` stack skeleton — OpenAI Codex CLI as the second agent on the existing Elastic backend, proven to the OTLP-path level.** First increment of extending the lab to Codex CLI: the composition stands up and the agent-independent OTLP → APM Server → Elasticsearch path is proven by the synthetic smoke probe. The agent's telemetry config and all Kibana assets are **out of scope** here — they depend on a live characterization of Codex's telemetry and are authored later with the human. **Naming is settled:** `codex-cli` mirrors Codex's emitted `service.name: codex-cli` (as `claude-code` does its own, and disambiguates from Codex cloud / IDE / the legacy model); later tasks use trace namespace `agents_codex_cli` and data streams `*-apm.app.codex_cli-default`.

  Reuse `components/backends/elastic/` unchanged; model the stack on `stacks/claude-code-elastic/` (direct-path precedent) and `stacks/claude-code-otelcol-elastic/` (the existing second stack on the same backend — incl. its `aol-*` container/port collision caveat). Ship: a thin `docker-compose.yml` (`name: codex-cli-elastic`, `include:` the Elastic backend, no inline services); the stack's own `scripts/smoke-test.sh` (the probe is agent-independent, so it follows `claude-code-elastic`'s — synthetic `aol-smoke-test` OTLP/protobuf to `:8200`, asserting `metrics/logs/traces-apm*`); and a self-contained `README.md` Quick Tour (up → smoke → teardown, demo-posture + collision caveats, plus a brief "not yet wired" note that pointing a Codex session and the Kibana views come in later increments). Do **not** add a `config.toml`, a `setup.{sh,ps1}`, or Kibana NDJSON.

  **Verify:** `docker compose config -q` passes for all stacks; with the other two stacks down, `codex-cli-elastic` brings the three `aol-*` services healthy; `scripts/smoke-test.sh` reaches PASS (all three probes land).
- [x] **Add the Codex CLI trace-isolation reroute to the shared backend pipeline.** Companion plumbing to the skeleton above. Add a second `reroute` processor to `components/backends/elastic/elasticsearch/trace-routing.pipeline.json` so `service.name: codex-cli` spans land in **`traces-apm-agents_codex_cli`** (same per-agent `agents_<agent>` convention as the existing `claude-code` reroute, which stays unchanged), and generalize the `description` from stack-specific to backend-level covering both agents. No script changes — the `setup-trace-routing.{sh,ps1}` already PUT this file verbatim, so every stack that installs it benefits.

  **Verify:** valid JSON, PUTs with `acknowledged: true`; `_simulate` reroutes a `codex-cli` span to `traces-apm-agents_codex_cli`, still routes `claude-code` to `traces-apm-agents_claude_code`, and leaves other `service.name`s in `traces-apm-default`; existing Claude Code smoke tests still PASS.
- [x] **Provide the `codex-cli-elastic` setup so a real Codex session can emit into the stack (no Kibana yet):** add the `components/agents/codex-cli/` telemetry config template (`[otel]` → APM OTLP `http://localhost:8200`, `otlp-http` `protocol = "binary"` for logs `/v1/logs` + traces `/v1/traces` + metrics `/v1/metrics` with `metrics_exporter` set explicitly off Codex's `statsig` default, `log_user_prompt = false`) and a stack `setup.{sh,ps1}` (modeled on claude-code-elastic's) that installs trace routing and renders a self-contained, non-invasive session config (prefer a stack-local `.codex/config.toml` via project precedence or `CODEX_HOME` over touching `~/.codex`, gitignored), plus a README "point a Codex session" step — Kibana import stays deferred; Ralph verifies pipeline install + valid `[otel]` TOML + `docker compose config -q` + smoke PASS, while confirming a real session's telemetry lands is a human step feeding later characterization.
- [x] **Fix the Codex trace-isolation reroute — the shipped predicate never matches the real `service.name`.** Live characterization showed Codex emits `service.name: codex_cli_rs` (Rust impl; datasets `*-apm.app.codex_cli_rs-default`, version 0.137.0), not the assumed `codex-cli` — so the shipped reroute (`ctx.service?.name == 'codex-cli'` → `agents_codex_cli`) never fires and all Codex trace spans leak into `traces-apm-default` unisolated. In `components/backends/elastic/elasticsearch/trace-routing.pipeline.json` change the Codex processor to `ctx.service?.name == 'codex_cli_rs'` → namespace **`agents_codex_cli_rs`** (carrying the `rs` token so traces match the APM-assigned `codex_cli_rs` logs/metrics datasets; the cross-agent glob `traces-apm-agents_*` still catches it; human-facing agent/stack names stay `codex-cli`). Update the `description` accordingly; the `claude-code` reroute is unchanged. **Verify:** `_simulate` reroutes a `codex_cli_rs` span to `traces-apm-agents_codex_cli_rs`, still routes `claude-code`, leaves others in `traces-apm-default`; after re-running the stack's trace-routing setup a fresh Codex session's spans land in `traces-apm-agents_codex_cli_rs`. (Spans already in `traces-apm-default` stay as demo residue until `down -v`.)
- [x] **Add the Codex CLI Kibana data views + the agent/stack import wiring (data-view layer only — saved searches follow per event type).** Mirror `components/agents/claude-code/kibana/data-views.ndjson` for Codex, scoped to the real `codex_cli_rs` datasets; the log_only/trace_safe and stream-noise calls belong to the later saved-search tasks, not here. New `components/agents/codex-cli/kibana/data-views.ndjson` with three `index-pattern` objects (human-facing names `Codex CLI — …`, ids `codex-cli-…`, trailing `exportedCount:3`): `codex-cli-metrics` (`metrics-apm.app.codex_cli_rs*`); `codex-cli-events` (`logs-apm.app.codex_cli_rs*`, with the same `trace.id` URL field formatter as claude's events view); `codex-cli-traces` (`traces-apm-agents_codex_cli_rs*`, same formatter — depends on the reroute-fix task above, so empty until a session lands post-fix). Add `components/agents/codex-cli/scripts/import-kibana-objects.{sh,ps1}` modeled on the claude agent's (self-locating `COMPONENT_DIR`, `.ps1` uses absolute `Join-Path` with no `Set-Location`, `KIBANA_URL`/`-KibanaUrl` override, `FILES=kibana/data-views.ndjson`, `success:true` gate). Wire a Kibana-import step into the stack `stacks/codex-cli-elastic/scripts/setup.{sh,ps1}` — run the backend import (the cross-agent AI Agents — Traces view, which already covers codex) then the codex agent import, fail-fast — so setup becomes trace-routing + render-config + kibana-import; update the stack README Quick Tour. **Verify:** both import legs report `success:true`; the three data views open in Discover (Events resolves live `codex_cli_rs` logs, Metrics resolves the metric docs, Traces empty pre-fix and populating after); the `.ps1` path leaves `$PWD` unchanged.
- [x] **Codex saved search — Tool Results (the first Codex saved search; sets the filter-pill conventions the rest reuse).** A `search` object on the `codex-cli-events` data view (`logs-apm.app.codex_cli_rs*`), `@timestamp` desc. Establishes the Codex filter-pill conventions: a single filter **pill on `labels.event_name`** (Codex's event-type field — NOT claude's `message`), empty query bar, the pill's `meta.index` wired to `codex-cli-events` via `references[]`. **Documented departure from the claude convention: NO `labels.user_email` lead** — on Codex the `event_name` + structured fields live on the `trace_safe` doc family, which carries no identity (`user_email` is on the `log_only` twin; the joined ES|QL views restore it). Filter pill `labels.event_name: codex.tool_result`; columns: `labels.tool_name`, `labels.tool_origin`, `labels.mcp_tool`, `labels.success`, `labels.duration_ms`, `numeric_labels.arguments_length`, `numeric_labels.output_length`, `numeric_labels.output_line_count`, `labels.call_id`, `trace.id`. Ship in a new `components/agents/codex-cli/kibana/saved-searches.ndjson` and append that file to the codex agent import script's `FILES` (after `data-views.ndjson`, so the data-view reference resolves). **Verify:** imports `success:true`, opens with the single `event_name` pill (empty query bar) over live `codex.tool_result` docs (35 in the 2026-06-09 window).
- [x] **Codex saved searches — API Requests, Conversation Starts, User Prompts (three filter-pill `search` objects appended to `components/agents/codex-cli/kibana/saved-searches.ndjson`, same conventions as Tool Results — `codex-cli-events` view, `@timestamp` desc, single `labels.event_name` pill, no `user_email` lead):** (a) **API Requests** `labels.event_name: codex.api_request` → cols `labels.endpoint`, `http.request.method`, `labels.http_response_status_code`, `labels.attempt`, `labels.duration_ms`, `labels.success`, `labels.auth_mode`, `trace.id` (HTTP-REST lens, e.g. `/models`; model-stream calls go over WebSocket, intentionally not curated); (b) **Conversation Starts** `codex.conversation_starts` → `labels.model`, `labels.provider_name`, `labels.approval_policy`, `labels.sandbox_policy`, `labels.reasoning_summary`, `numeric_labels.mcp_server_count`, `labels.conversation_id`, `trace.id` (one per `conversation_id`, session-config audit line); (c) **User Prompts** `codex.user_prompt` → `labels.prompt_length`, `numeric_labels.text_input_count`, `numeric_labels.image_input_count`, `labels.conversation_id`, `trace.id` (one per user turn; prompt text rides the log_only twin only when `log_user_prompt=true` → joined view). No import-script change; verify each imports `success:true` and opens with its pill over live docs.
- [x] **Codex saved search — Event Overview (cross-event timeline, stream noise excluded):** a `search` object appended to `saved-searches.ndjson` on `codex-cli-events`, `@timestamp` desc, scoping to the trace_safe family with noise removed via two pills — `service.framework.name: codex_cli_rs` (docs carrying `labels.event_name`) AND negated `NOT labels.event_name: codex.websocket_event` (drops the ~894 per-delta wrappers); cols (`@timestamp` leftmost) `labels.event_name`, `labels.model`, `labels.duration_ms`, `labels.success`, `labels.conversation_id`, `span.id`, `trace.id`, no `user_email` lead; verify it opens with both pills, all event types interleaved and `codex.websocket_event` absent.
- [x] **Codex saved search — Tool I/O (joined), first Codex ES|QL view:** an ES|QL saved Discover session (no data view/pills) appended to `saved-searches.ndjson`, mirroring claude's "Tool I/O (joined)", reconstructing one row per tool call by merging the `trace_safe` `codex.tool_result` (tool_name/success/duration/sizes) with its `log_only` twin (`user_email` + `arguments` command + `output` stdout) on their shared `labels.call_id`/`span.id` — final columns ≈ `ts`, `user_email`, `tool`, `input`, `output`, `success`, `duration_ms`, `trace_id` (exact ES|QL is Ralph's to write/polish, incl. multivalue handling); verify against the 2026-06-09 12:53 window (shell_command rows show command + stdout + `user_email` together).
- [x] **Codex saved search — User Prompts (joined):** companion ES|QL saved Discover session appended to `saved-searches.ndjson`, merging the `trace_safe` `codex.user_prompt` (prompt_length/input counts) with its `log_only` twin (`user_email` + prompt text when `log_user_prompt=true`) on their shared join key (Ralph confirms it live — likely `span.id`, else `conversation_id`+`@timestamp`) — final columns ≈ `ts`, `user_email`, `prompt`, `prompt_length`, `conversation_id`, `trace_id`; `prompt` is empty unless `log_user_prompt=true` (lab default false), the standing lens on that gate; verify it imports and opens (populated-text check is a gated human step).
- [x] **Replace Codex CLI saved searches with the characterized four-view set.** Live characterization of Codex CLI telemetry showed that the earlier per-event saved-search set is less useful than the four curated ES|QL views now captured in `SPEC/kibana-codex-saved-searches.ndjson`. Replace the existing Codex CLI saved searches with only these four saved searches:

  1. **Codex CLI — Conversations**
  2. **Codex CLI — Turns**
  3. **Codex CLI — Turn Timeline (Events) (Deprecated)**
  4. **Codex CLI — Turn Timeline (Traces)**

  Use `SPEC/kibana-codex-saved-searches.ndjson` as the source artifact. Preserve the four views' ES|QL semantics, columns, sort order, and display intent, but normalize the implementation to match the existing `components/agents/codex-cli/kibana/` conventions.

  **Requirements:**
  - Replace the current `components/agents/codex-cli/kibana/saved-searches.ndjson` contents with these four searches only. Remove the earlier Codex saved searches from that file rather than keeping both generations.
  - Keep the existing Codex data views and import scripts, but ensure setup still imports data views before saved searches.
  - Use stable, human-readable saved object IDs, for example: `codex-cli-conversations`, `codex-cli-turns`, `codex-cli-turn-timeline-events-deprecated`, `codex-cli-turn-timeline-traces`.
  - Add useful non-empty descriptions explaining each view's purpose and source: Conversations is a logs-based usage summary; Turns is a traces-based turn summary; Turn Timeline Events is a deprecated/lab-reference logs timeline; Turn Timeline Traces is the primary execution timeline.
  - In `Codex CLI — Turn Timeline (Traces)`, make the mixed `WHERE` condition explicit with parentheses: `(labels.turn_id IS NOT NULL AND labels.turn_id != "") OR labels.submission_id IS NOT NULL`.
  - Normalize log index patterns to the broader `logs-apm.app.codex_cli_rs*` unless there is a concrete reason to keep `logs-apm.app.codex_cli_rs-default*`.
  - Do not add extra columns beyond the exported view intent for this task.
  - Do not add a dashboard.
  - Do not add ingest pipelines, filtering/drop rules, or normalized summary indices in this task.
  - Ignore the Kibana export summary line from the SPEC artifact when producing the final saved-searches NDJSON.

  **Acceptance criteria:**
  - `scripts/setup.sh` and `scripts/setup.ps1` import the Codex data views and exactly these four Codex saved searches.
  - The saved objects import with `success:true` on a fresh stack.
  - All four saved searches open in Kibana Discover.
  - Existing Codex data views remain available.
  - README deferred/saved-search wording is updated to say these four curated searches now ship, while dashboards, ingest filtering, TTFT integration, and normalized summary indices remain deferred.
- [x] **Replace the Codex Turn Timeline saved searches with Classic Discover versions.** The shipped Turn Timeline searches are ES|QL-based, but ES|QL results do not use the Codex data-view `trace.id` URL formatter, so users cannot click through to the Elastic APM trace view. The timeline searches are investigation entry points, so preserving that drilldown is more important than ES|QL-derived convenience columns.

  Use the newly exported Classic Discover objects in `SPEC/kibana-codex-saved-searches.ndjson` as the source for the two timeline searches only:
  - **Codex CLI — Turn Timeline (Events) (Deprecated)**
  - **Codex CLI — Turn Timeline (Traces)**

  Keep **Codex CLI — Conversations** and **Codex CLI — Turns** as the existing ES|QL aggregate searches. Keep the stable saved-object IDs already used by the component NDJSON, and keep the existing Codex / cross-agent data-view references rather than importing duplicate data views from the export. The traces timeline should stay broad enough to show the useful turn spans, including local `shell_command` spans and `op.dispatch.user_input`, which do not reliably carry `labels.turn_id`; the intended drilldown key is `trace.id`.

  After the component saved-searches NDJSON is updated, delete the temporary export file `SPEC/kibana-codex-saved-searches.ndjson`.

  **Acceptance criteria:** the Codex setup imports exactly four saved searches; the two timeline searches are Classic Discover searches backed by data views; `trace.id` is clickable to the APM trace view in those timeline searches; Conversations and Turns still open as ES|QL aggregates; the temporary SPEC NDJSON is gone.
- [x] **Tighten the Codex Conversations aggregate to the current LLM request signal.** Live data now shows normal Codex LLM calls as `labels.event_name: codex.api_request` with `labels.endpoint: /responses`, not `codex.websocket_request`; `/models` requests should not count as LLM requests. Update **Codex CLI — Conversations** so `llm_requests` counts trace-safe `codex.api_request` documents scoped to `/responses`. Also rename the distinct-trace count from `traces` to **`turns`** and place it before `user_prompts`, reflecting the normal Codex session shape where one turn maps to one trace for this aggregate.

  **Acceptance criteria:** Conversations still imports as an ES|QL saved search; the output columns include `turns` before `user_prompts` and no longer include `traces`; `llm_requests` excludes `/models` and counts `/responses` API requests; the saved search opens successfully after setup.
- [x] **Update the Codex Turns aggregate to group by trace and include local tool/user-prompt spans.** Live traces showed that useful turn children such as `op.dispatch.user_input` and `shell_command` do not reliably carry `labels.turn_id`, so the current `BY labels.turn_id` aggregate drops them. Update **Codex CLI — Turns** to use this ES|QL, allowing currently incomplete traces to remain visible:

  ```esql
  FROM traces-apm-agents_codex_cli_rs*
  | WHERE transaction.name == "turn/start"
    OR span.name IN (
      "session_task.turn",
      "op.dispatch.user_input",
      "run_sampling_request",
      "shell_command",
      "mcp.tools.call"
    )
  | EVAL is_session_turn = span.name == "session_task.turn"
  | EVAL is_user_prompt = span.name == "op.dispatch.user_input"
  | EVAL is_llm_request = span.name == "run_sampling_request"
  | EVAL is_shell_command = span.name == "shell_command"
  | EVAL is_mcp_call = span.name == "mcp.tools.call"
  | EVAL is_error = event.outcome == "failure"
  | STATS
      first_seen = MIN(@timestamp),
      last_seen = MAX(@timestamp),
      duration_sec = MAX(CASE(is_session_turn, span.duration.us / 1000000, null)),
      docs = COUNT(*),
      user_prompts = COUNT_DISTINCT(CASE(is_user_prompt, span.id, null)),
      llm_requests = COUNT_DISTINCT(CASE(is_llm_request, span.id, null)),
      shell_commands = COUNT_DISTINCT(CASE(is_shell_command, span.id, null)),
      mcp_calls = COUNT_DISTINCT(CASE(is_mcp_call, span.id, null)),
      cached_input_tokens = SUM(numeric_labels.codex_turn_token_usage_cached_input_tokens),
      non_cached_input_tokens = SUM(numeric_labels.codex_turn_token_usage_non_cached_input_tokens),
      output_tokens = SUM(numeric_labels.codex_turn_token_usage_output_tokens),
      errors = SUM(CASE(is_error, 1, 0)),
      conversation_id = VALUES(labels.thread_id)
    BY trace_id = trace.id
  | SORT last_seen DESC
  ```

  **Acceptance criteria:** Turns still imports as an ES|QL saved search; each row is keyed by `trace_id`; the view includes `user_prompts` and `shell_commands`; token totals remain sourced from the turn-level token fields without double counting; the saved search opens successfully after setup.
- [x] **Refine the Codex Turn Timeline (Traces) Classic Discover view.** Update the shipped Classic Discover saved search **Codex CLI — Turn Timeline (Traces)** in `components/agents/codex-cli/kibana/saved-searches.ndjson` with two small presentation fixes from live trace review: add `labels.model` as a visible column immediately after `span.duration.us` and before `labels.tool_name`, and replace the selected LLM span from `run_sampling_request` to `try_run_sampling_request` in the `span.name` phrases filter. Keep the rest of the saved search, data-view references, title, and stable id unchanged.

  **Acceptance criteria:** the timeline imports as a Classic Discover saved search; visible columns include `span.duration.us`, then `labels.model`, then `labels.tool_name`; the span filter includes `try_run_sampling_request` and no longer includes `run_sampling_request`; `trace.id` remains clickable to the APM trace view.
- [x] **Refine the Codex Turn Timeline (Events) Classic Discover view.** Update the shipped Classic Discover saved search **Codex CLI — Turn Timeline (Events) (Deprecated)** in `components/agents/codex-cli/kibana/saved-searches.ndjson` to add `labels.model` as a visible column immediately after `labels.prompt_length` and before `labels.tool_name`. Keep the rest of the saved search, data-view references, title, stable id, and deprecated/lab-reference positioning unchanged.

  **Acceptance criteria:** the timeline imports as a Classic Discover saved search; visible columns include `labels.prompt_length`, then `labels.model`, then `labels.tool_name`; `trace.id` remains clickable to the APM trace view.
- [x] **Finalize the Codex Turns aggregate columns for execution-shape analysis.** Update **Codex CLI — Turns** to count `try_run_sampling_request` as `llm_requests`, expose `built_tools` as `tool_catalogues`, and collapse local shell plus MCP executions into one `tool_calls` column. Use this ES|QL:

  ```esql
  FROM traces-apm-agents_codex_cli_rs*
  | WHERE transaction.name == "turn/start" OR span.name IN ("session_task.turn", "op.dispatch.user_input", "try_run_sampling_request", "built_tools", "shell_command", "mcp.tools.call")
  | EVAL is_session_turn = span.name == "session_task.turn"
  | EVAL is_user_prompt = span.name == "op.dispatch.user_input"
  | EVAL is_llm_request = span.name == "try_run_sampling_request"
  | EVAL is_tool_catalogue = span.name == "built_tools"
  | EVAL is_shell_command = span.name == "shell_command"
  | EVAL is_mcp_call = span.name == "mcp.tools.call"
  | EVAL is_tool_call = is_shell_command OR is_mcp_call
  | EVAL is_error = event.outcome == "failure"
  | STATS
      first_seen = MIN(@timestamp),
      last_seen = MAX(@timestamp),
      duration_sec = MAX(CASE(is_session_turn, span.duration.us / 1000000, null)),
      docs = COUNT(*),
      user_prompts = COUNT_DISTINCT(CASE(is_user_prompt, span.id, null)),
      llm_requests = COUNT_DISTINCT(CASE(is_llm_request, span.id, null)),
      tool_catalogues = COUNT_DISTINCT(CASE(is_tool_catalogue, span.id, null)),
      tool_calls = COUNT_DISTINCT(CASE(is_tool_call, span.id, null)),
      cached_input_tokens = SUM(numeric_labels.codex_turn_token_usage_cached_input_tokens),
      non_cached_input_tokens = SUM(numeric_labels.codex_turn_token_usage_non_cached_input_tokens),
      output_tokens = SUM(numeric_labels.codex_turn_token_usage_output_tokens),
      errors = SUM(CASE(is_error, 1, 0)),
      conversation_id = VALUES(labels.thread_id)
    BY trace_id = trace.id
  | SORT last_seen DESC
  ```

  **Acceptance criteria:** Turns imports as an ES|QL saved search; output columns include `tool_catalogues` and `tool_calls`; `llm_requests` is based on `try_run_sampling_request`; separate `shell_commands` and `mcp_calls` columns are not shown; the saved search opens successfully after setup.
- [x] **Rename the Codex Conversations tool count from `tool_results` to `tool_calls`.** In **Codex CLI — Conversations**, keep the existing count source (`codex.tool_result` / call id) but rename the output column to `tool_calls` so the aggregate terminology matches Turns and reads as "number of tool executions" rather than "number of result documents." Update the saved-search description and visible column list accordingly.

  **Acceptance criteria:** Conversations imports as an ES|QL saved search; the output columns include `tool_calls` and no longer include `tool_results`; the count value remains based on distinct tool call ids; the saved search opens successfully after setup.
- [x] **Add Codex User Prompts saved searches for the log_only and trace_safe families.** Add two Classic Discover saved searches to `components/agents/codex-cli/kibana/saved-searches.ndjson`, both on the existing **Codex CLI — Events** data view so `trace.id` keeps its APM trace link. Name both views with their telemetry-family source because future Codex comparisons will use the same convention:
  1. **Codex CLI — User Prompts (Log Only)** — filter pills: `service.framework.name: codex_otel.log_only` and `labels.prompt` exists.
  2. **Codex CLI — User Prompts (Trace Safe) (Deprecated)** — filter pills: `labels.target: codex_otel.trace_safe` and `labels.event_name: codex.user_prompt`.

  Both searches should use the same visible columns, accepting that some columns are intentionally blank on one family or the other: `@timestamp`, `labels.model`, `labels.prompt`, `labels.prompt_length`, `numeric_labels.text_input_count`, `numeric_labels.image_input_count`, `labels.conversation_id`, `trace.id`.

  **Acceptance criteria:** both saved searches import as Classic Discover searches with filter pills and an empty query bar; the log_only view opens over prompt documents; the trace_safe view is clearly marked deprecated in title/description; `trace.id` remains clickable to the APM trace view; setup still imports all Codex saved searches successfully.
- [x] **Add Codex LLM Request saved searches for duration, token, and communication analysis.** Add three Classic Discover saved searches that intentionally expose the three physical views of Codex LLM requests. Name each view with both its telemetry-family source and its primary analysis purpose.

  1. **Codex CLI — LLM Requests (Traces / Duration)** — data view **AI Agents — Traces**; filter pill `span.name: try_run_sampling_request`; columns: `@timestamp`, `event.outcome`, `span.duration.us`, `labels.model`, `trace.id`.
  2. **Codex CLI — LLM Requests (Log Only / Tokens)** — data view **Codex CLI — Events**; filter pills `service.framework.name: codex_otel.log_only` and `labels.event_kind: response.completed`; columns: `@timestamp`, `labels.model`, `labels.input_token_count`, `numeric_labels.cached_token_count`, `labels.output_token_count`, `labels.conversation_id`, `trace.id`.
  3. **Codex CLI — LLM Requests (Trace Safe / Communication)** — data view **Codex CLI — Events**; filter pills `labels.target: codex_otel.trace_safe` plus a combined OR filter matching `labels.event_name: codex.websocket_request` OR (`labels.event_name: codex.api_request` AND `labels.endpoint: /responses`); columns: `@timestamp`, `labels.success`, `labels.duration_ms`, `labels.endpoint`, `labels.http_response_status_code`, `labels.error_message`, `labels.conversation_id`, `trace.id`.

  These views are intentionally separate: Traces is the duration/execution view, Log Only is the token view, and Trace Safe is the communication-attempt/error view. Do not merge them in this task.

  **Acceptance criteria:** all three saved searches import as Classic Discover searches with filter pills and empty query bars; the Trace Safe OR condition is represented as a usable filter-pill shape in Discover; `trace.id` remains clickable where present; setup still imports all Codex saved searches successfully.
- [x] **Add TTFT to the Codex Turns aggregate.** Update **Codex CLI — Turns** so each turn row includes time-to-first-token as `ttft_sec`. TTFT is emitted as a trace-safe log event (`labels.target: codex_otel.trace_safe`, `labels.event_name: codex.turn_ttft`) and should be associated to the turn by `trace.id`, alongside the existing trace-derived turn fields. Use this ES|QL:

  ```esql
  FROM traces-apm-agents_codex_cli_rs*, logs-apm.app.codex_cli_rs*
  | WHERE transaction.name == "turn/start"
     OR span.name IN (
       "session_task.turn",
       "op.dispatch.user_input",
       "try_run_sampling_request",
       "built_tools",
       "shell_command",
       "mcp.tools.call"
     )
     OR (
       labels.target == "codex_otel.trace_safe"
       AND labels.event_name == "codex.turn_ttft"
     )
  | EVAL is_session_turn = span.name == "session_task.turn"
  | EVAL is_ttft = labels.target == "codex_otel.trace_safe" AND labels.event_name == "codex.turn_ttft"
  | EVAL is_user_prompt = span.name == "op.dispatch.user_input"
  | EVAL is_llm_request = span.name == "try_run_sampling_request"
  | EVAL is_tool_catalogue = span.name == "built_tools"
  | EVAL is_shell_command = span.name == "shell_command"
  | EVAL is_mcp_call = span.name == "mcp.tools.call"
  | EVAL is_tool_call = is_shell_command OR is_mcp_call
  | EVAL is_error = event.outcome == "failure"
  | STATS
      first_seen = MIN(@timestamp),
      last_seen = MAX(@timestamp),
      duration_sec = MAX(CASE(is_session_turn, span.duration.us / 1000000.0, null)),
      ttft_sec = MAX(CASE(is_ttft, TO_LONG(labels.duration_ms) / 1000.0, null)),
      user_prompts = COUNT_DISTINCT(CASE(is_user_prompt, span.id, null)),
      llm_requests = COUNT_DISTINCT(CASE(is_llm_request, span.id, null)),
      tool_catalogues = COUNT_DISTINCT(CASE(is_tool_catalogue, span.id, null)),
      tool_calls = COUNT_DISTINCT(CASE(is_tool_call, span.id, null)),
      cached_input_tokens = SUM(numeric_labels.codex_turn_token_usage_cached_input_tokens),
      non_cached_input_tokens = SUM(numeric_labels.codex_turn_token_usage_non_cached_input_tokens),
      output_tokens = SUM(numeric_labels.codex_turn_token_usage_output_tokens),
      errors = SUM(CASE(is_error, 1, 0)),
      conversation_id = VALUES(labels.thread_id)
    BY trace_id = trace.id
  | SORT last_seen DESC
  ```

  Keep `docs` out of the output columns; once TTFT logs are included, a raw document count mixes trace documents and supporting log documents and is not useful as a primary analysis column.

  **Acceptance criteria:** Turns imports as an ES|QL saved search; output columns include `ttft_sec` after `duration_sec`; `docs` is no longer shown; TTFT values are populated from `codex.turn_ttft` where present and remain null where the event is absent; existing turn duration, execution counts, token totals, errors, `conversation_id`, and `trace_id` behavior remains unchanged.
- [x] **Show fractional seconds in the Codex Conversations duration.** Update **Codex CLI — Conversations** so `duration_sec` divides by `1000.0` rather than `1000`, matching the fractional-second convention now used by Turns.

  The intended expression is:

  ```esql
  duration_sec = (MAX(ts_ms) - MIN(ts_ms)) / 1000.0,
  ```

  **Acceptance criteria:** Conversations imports as an ES|QL saved search; `duration_sec` can display fractional seconds; the rest of the Conversations query, columns, and counts remain unchanged.
- [x] **Add Codex Tool Calls saved searches across traces, log_only, and trace_safe.** Add three Classic Discover saved searches that expose the same broad Tool Calls concept from three physical telemetry sources. These are intentionally overlapping lab views: traces show execution shape, log_only shows I/O content, and trace_safe shows safe metadata.

  1. **Codex CLI — Tool Calls (Traces / Execution)** — data view **AI Agents — Traces**; filter pill for `span.name` limited to `shell_command`, `apply_patch`, and `mcp.tools.call`; columns: `@timestamp`, `event.outcome`, `span.duration.us`, `span.name`, `labels.tool_name`, `labels.mcp_server_name`, `labels.conversation_id`, `trace.id`.
  2. **Codex CLI — Tool Calls (Log Only / I/O)** — data view **Codex CLI — Events**; filter pills `service.framework.name: codex_otel.log_only` and `labels.tool_name` exists; columns: `@timestamp`, `labels.success`, `labels.duration_ms`, `labels.tool_name`, `labels.mcp_server`, `labels.arguments`, `labels.output`, `labels.conversation_id`, `trace.id`.
  3. **Codex CLI — Tool Calls (Trace Safe / Metadata)** — data view **Codex CLI — Events**; filter pills `labels.target: codex_otel.trace_safe` and `labels.event_name: codex.tool_result`; columns: `@timestamp`, `labels.success`, `labels.duration_ms`, `labels.tool_origin`, `labels.tool_name`, `numeric_labels.arguments_length`, `numeric_labels.output_length`, `numeric_labels.output_line_count`, `labels.conversation_id`, `trace.id`.

  Keep the three views separate rather than merging them: the lab needs the side-by-side comparison of execution spans, sensitive I/O logs, and safe result metadata. Do not add hosted/platform tool events such as `response.web_search_call.*` to these views in this task; those are a separate event-kind family.

  **Acceptance criteria:** all three saved searches import as Classic Discover searches with filter pills and empty query bars; `trace.id` remains clickable where present; the Traces view does not include `mcp__...` wrapper spans; the Log Only view opens over tool documents with I/O fields where present; the Trace Safe view opens over `codex.tool_result` metadata; setup still imports all Codex saved searches successfully.
- [x] **Add Codex trace-safe error saved searches for LLM requests and tool calls.** Add two Classic Discover saved searches on the existing **Codex CLI — Events** data view. These are intentionally lab-oriented complements to the broader LLM Request and Tool Calls views: they are filter variants, but useful for comparing how communication failures and tool failures appear in trace-safe telemetry.

  1. **Codex CLI — LLM Request Errors (Trace Safe)** — filter pills `labels.target: codex_otel.trace_safe`, `labels.event_name` matching `codex.api_request` OR `codex.websocket_request`, and an error condition matching `labels.success: false` OR `labels.error_message` exists. Do **not** filter on `labels.http_response_status_code`: it is a string label in the observed data and has no numeric-label equivalent, so range comparisons are not reliable. Columns: `@timestamp`, `labels.event_name`, `labels.success`, `labels.duration_ms`, `labels.model`, `labels.endpoint`, `labels.http_response_status_code`, `labels.error_message`, `labels.conversation_id`, `trace.id`.
  2. **Codex CLI — Tool Call Errors (Trace Safe)** — filter pills `labels.target: codex_otel.trace_safe`, `labels.event_name: codex.tool_result`, and `labels.success: false`. Columns: `@timestamp`, `labels.success`, `labels.duration_ms`, `labels.tool_origin`, `labels.tool_name`, `numeric_labels.arguments_length`, `numeric_labels.output_length`, `numeric_labels.output_line_count`, `labels.conversation_id`, `trace.id`.

  **Acceptance criteria:** both saved searches import as Classic Discover searches with filter pills and empty query bars; the LLM Request Errors view includes failed `codex.api_request` and `codex.websocket_request` documents without status-code range filtering; the Tool Call Errors view opens over failed `codex.tool_result` documents; `trace.id` remains clickable where present; setup still imports all Codex saved searches successfully.
- [x] **Characterize Codex `UserPromptSubmit` hook payload for prompt audit feasibility.** Add a stack-local verification hook for Codex CLI, analogous in shape to the existing stack-local `.codex/config.toml` telemetry setup but explicitly for hook characterization, not production audit storage yet.

  Requirements:
  - Add the hook script under `components/agents/codex-cli/hooks/` (POSIX shell plus PowerShell mirror, following the repo's existing script-pair convention).
  - The hook must run on Codex `UserPromptSubmit` and capture the raw stdin payload plus a best-effort extracted prompt field into a stack-local file, for example `.codex/hook-captures/user-prompt-submit.ndjson`.
  - Do **not** write the prompt to stdout. The verification hook must avoid changing the model context or disturbing the user's prompt; diagnostics should go to stderr and the hook should exit 0 on all non-fatal paths.
  - Extend the Codex setup flow so running `stacks/codex-cli-elastic/scripts/setup.{sh,ps1}` creates the stack-local hook registration under `<stack>/.codex/`, preferably as `.codex/hooks.json` so it can coexist with the generated `.codex/config.toml`.
  - Keep this as a characterization task only: do not write to `prompts-audit`, do not encrypt/seal content, and do not change the backend mapping yet.

  **Acceptance criteria:** after setup, a Codex session launched with `CODEX_HOME=<stack>/.codex` has a trusted/reviewable `UserPromptSubmit` hook registered; submitting a prompt causes one local capture record to be appended under the stack `.codex/` directory; the captured record is sufficient to identify the actual Codex hook payload keys for prompt text and any available session/conversation/turn correlation fields; the hook never blocks normal prompt submission if capture fails.
- [x] Extend the current `codex-cli-elastic` setup so it creates the local `logs-agent_audit.user_prompt-default` data stream and matching index template with the strict mapping defined in `SPEC/agent-audit.md`; do not change hooks, config, Kibana objects, or external/production clusters in this task.
- [x] Update the current Codex `UserPromptSubmit` characterization hook so it writes the canonical `agent_audit.user_prompt` JSON shape from `SPEC/agent-audit.md` to the existing local capture file instead of the raw hook payload; do not send to Elasticsearch in this task.
- [x] Extend the current `codex-cli-elastic` setup so it generates stack-local `.codex/agent-audit.toml` with local Elasticsearch defaults from `SPEC/agent-audit.md`; do not change hook delivery in this task.
- [x] Update the Codex `UserPromptSubmit` hook to read `.codex/agent-audit.toml` and send the canonical user-prompt audit document to the local `logs-agent_audit.user_prompt-default` data stream; keep the hook fail-open and stop writing the default local capture file.
- [x] Add `stacks/codex-cli-elastic/scripts/verify-agent-audit.{sh,ps1}` as siblings of the existing smoke test to feed a synthetic `UserPromptSubmit` payload through the configured hook and assert the resulting document exists in the local `logs-agent_audit.user_prompt-default` data stream; do not rename or change the existing smoke tests in this task.
- [x] Add an importable Kibana data view and Classic Discover saved search named `Agent Audit — User Prompts` for `logs-agent_audit.user_prompt-*`, with no filter pills, sorted by `@timestamp` desc, and columns `user.id`, `user.name`, `user.email`, `agent_audit.agent.provider`, `agent_audit.agent.name`, `agent_audit.agent.model`, `agent_audit.prompt.text`, `agent_audit.prompt.length`, `agent_audit.conversation_id`, `agent_audit.turn_id`.
- [x] Apply the `SPEC/agent-audit.md` addition of ECS `host.name` and `host.hostname` to the Agent Audit user-prompt mapping, Codex hook document generation, verification, and `Agent Audit — User Prompts` saved-search columns.
- [x] Apply the `SPEC/agent-audit.md` identity schema update to Agent Audit: remove `user.email`, keep `user.id`/`user.name` as workstation login identity, and add optional `agent_audit.agent.account.*` plus parallel `agent_audit.agent.organization.*` fields to the mapping, hook document generation, verification, and `Agent Audit — User Prompts` saved-search columns.
- [x] Implement `SPEC/agent-audit.md` "Identity derivation" in the Codex `UserPromptSubmit` hook `components/agents/codex-cli/hooks/capture-user-prompt.{sh,ps1}`: set `user.id` to the domain-qualified `whoami` login and populate `agent_audit.agent.account.*` / `organization.*` from `$CODEX_HOME/auth.json` (account.id ← `tokens.account_id`; account.email/name ← `id_token` `email`/`name` claims; organization.id/name ← the `is_default` `organizations` entry's `.id`/`.title`), keeping the hook fail-open, local-only (no network), and exit-0 with every field defaulting to `null` on any failure; extend `stacks/codex-cli-elastic/scripts/verify-agent-audit.{sh,ps1}` to assert `user.id` (and `account.id` when `auth.json` is present) is populated, touching neither the strict mapping nor the Kibana objects (those columns already exist).
- [x] Trim the `Agent Audit — User Prompts` saved search (`agent-audit-user-prompts-search` in `components/backends/elastic/kibana/agent-audit.ndjson`) to exactly these columns, in order — `user.id`, `host.name`, `agent_audit.agent.name`, `agent_audit.agent.model`, `agent_audit.agent.account.name`, `agent_audit.agent.account.email`, `agent_audit.agent.organization.name`, `agent_audit.prompt.text`, `agent_audit.prompt.length`, `agent_audit.conversation_id`, `agent_audit.turn_id` — dropping `user.name`, `host.hostname`, `agent_audit.agent.provider`, `agent_audit.agent.account.id`, and `agent_audit.agent.organization.id` from the view only (the hook keeps indexing all of them, `host.hostname` included — column choice is not a data control); leave the `@timestamp` desc sort, the no-filter-pill scope, and the data-view reference unchanged.
- [x] Add a **Codex CLI — Hooks** Classic Discover saved search on **Codex CLI — Metrics** following `SPEC/codex-cli-telemetry.md`: filter pill `codex.hooks.run` exists, `@timestamp` desc sort, and columns `@timestamp`, `labels.hook_name`, `labels.status`, `codex.hooks.run`, `labels.source`.
- [x] **Shorten every Codex CLI saved-search `description` in `components/agents/codex-cli/kibana/saved-searches.ndjson`** — the current descriptions (462–1041 chars) crowd the Discover saved-objects list and the search header. Replace each search's `attributes.description` with the concise one-liner below (description lives only on the top-level `attributes`; the `tabs[].attributes` carry none — leave them, and leave every `title`, `id`, `columns`, filter pill, sort, `references`, and ES|QL query untouched). The goal is to drop the repeated boilerplate ("Backed by a data view so trace.id click-through to the APM trace view works", "One of three intentionally separate physical views…", the long filter/field recitations) while keeping the essential signal: telemetry family (traces / log_only / trace_safe / metrics), the **Deprecated** marker where present, and the `log_user_prompt` gate note on User Prompts (Log Only).
  1. **Codex CLI — Conversations** → `Per-conversation usage summary (ES|QL over logs): turns, user prompts, LLM requests, tool calls, token sums, errors. Session-level companion to Turns.`
  2. **Codex CLI — Turns** → `Per-turn summary (ES|QL over traces, by trace.id): duration, TTFT, prompts, LLM requests, tool calls, token usage, errors. Turn-level companion to Conversations.`
  3. **Codex CLI — Turn Timeline (Events) (Deprecated)** → `Deprecated — logs-stream timeline of a turn; superseded by Turn Timeline (Traces), kept for reference.`
  4. **Codex CLI — Turn Timeline (Traces)** → `Primary execution timeline: one row per turn/tool span (traces). trace.id clicks through to the APM trace view.`
  5. **Codex CLI — User Prompts (Log Only)** → `User-prompt text lens (log_only family). Populated only when log_user_prompt=true (lab default off); the standing lens on that gate.`
  6. **Codex CLI — User Prompts (Trace Safe) (Deprecated)** → `Deprecated — trace_safe user-prompt view: prompt envelope only, never the text. Use User Prompts (Log Only) for the text.`
  7. **Codex CLI — LLM Requests (Traces / Duration)** → `LLM-request latency lens (traces, try_run_sampling_request): request duration and outcome per model.`
  8. **Codex CLI — LLM Requests (Log Only / Tokens)** → `LLM-request token lens (log_only, response.completed): input / cached / output token counts per model.`
  9. **Codex CLI — LLM Requests (Trace Safe / Communication)** → `LLM-request communication lens (trace_safe): the websocket / /responses attempts to reach the model — success, duration, status, error.`
  10. **Codex CLI — Tool Calls (Traces / Execution)** → `Tool-call execution lens (traces): one row per tool span (shell_command, apply_patch, mcp.tools.call) with duration and outcome.`
  11. **Codex CLI — Tool Calls (Log Only / I/O)** → `Tool-call I/O lens (log_only): the actual tool arguments and output (sensitive content).`
  12. **Codex CLI — Tool Calls (Trace Safe / Metadata)** → `Tool-call metadata lens (trace_safe, codex.tool_result): result envelope (success, duration, origin, sizes) without the content.`
  13. **Codex CLI — LLM Request Errors (Trace Safe)** → `Failed LLM requests (trace_safe api_request / websocket_request, success=false or error_message). Error complement of LLM Requests (Communication).`
  14. **Codex CLI — Tool Call Errors (Trace Safe)** → `Failed tool calls (trace_safe codex.tool_result, success=false). Error complement of Tool Calls (Metadata).`
  15. **Codex CLI — Hooks** → `Hook-run lens (metrics, codex.hooks.run): hook name, status, and source per run.`

  Verify the file still imports cleanly (`success: true`, 15 objects, references resolved) against the live Kibana and each search opens with its shortened description.
- [x] **Restore the dropped `codex.websocket_request` channel in the two Codex LLM-request views that match only `codex.api_request`** in `components/agents/codex-cli/kibana/saved-searches.ndjson` — match **both** model channels like the **LLM Requests (Trace Safe / Communication)** view (`codex.websocket_request` OR (`codex.api_request` AND `labels.endpoint: /responses`); keep the `/models` exclusion, change nothing else). In **Conversations** (ES|QL) set `is_llm_request = is_trace_safe AND (labels.event_name == "codex.websocket_request" OR (labels.event_name == "codex.api_request" AND labels.endpoint == "/responses"))`; in **Turn Timeline (Events) (Deprecated)** add `codex.websocket_request` as a third OR branch of the trace_safe-family filter (update both the top-level and `tabs[0]` filter copies + `references[]`). Verify the file imports cleanly (15 objects) and Conversations' `llm_requests` is no longer 0 for the websocket-only conversations.
- [x] Implement the Codex CLI ingest drops specified in `SPEC/codex-cli-telemetry.md` "Volume reduction (ingest drops)": the `service.name: codex_cli_rs`-gated `receiving`+`handle_responses` drop in `components/backends/elastic/elasticsearch/trace-routing.pipeline.json` (first processor, before the reroutes), and a new `logs-apm.app@custom` pipeline + `setup-logs-drop.{sh,ps1}` dropping the three verified `.delta` `event_kind`s; verify both via `_simulate` and a live Codex turn.
- [x] **Shorten the `Agent Audit — User Prompts` saved-search `description`** in `components/backends/elastic/kibana/agent-audit.ndjson` (object id `agent-audit-user-prompts-search`): replace `attributes.description` with a concise one-liner describing the search — `Hook-captured user prompts from the Agent Audit stream (logs-agent_audit.user_prompt-*), one row per submitted prompt.` Touch only `description`; leave `title`, `id`, `columns`, sort, `references`, and the data-view object unchanged. Verify the file imports cleanly and the search opens with the shortened description.
- [x] **Redact Codex CLI tool I/O in `logs-apm.app@custom`** — append two `set` processors to `components/backends/elastic/elasticsearch/logs-drop.pipeline.json` (after the existing `.delta` drop) replacing the raw tool input/output on `codex.tool_result`'s log_only twin with `[REDACTED]`, and extend the file `description` to mention it:
  - `{ "set": { "field": "labels.arguments", "value": "[REDACTED]", "override": true, "if": "ctx.service?.name == 'codex_cli_rs' && ctx.labels?.arguments != null" } }`
  - `{ "set": { "field": "labels.output", "value": "[REDACTED]", "override": true, "if": "ctx.service?.name == 'codex_cli_rs' && ctx.labels?.output != null" } }`

  No script change — the existing `setup-logs-drop.{sh,ps1}` PUTs this file. Verify via `_simulate` (a `codex_cli_rs` tool_result twin → both fields `[REDACTED]`; a doc lacking them → fields not created; a non-`codex_cli_rs` doc → untouched) and a live Codex turn.
- [x] **Redact Codex CLI user-prompt text in `logs-apm.app@custom`** — append a `set` processor to `components/backends/elastic/elasticsearch/logs-drop.pipeline.json` replacing the prompt text on `codex.user_prompt`'s log_only twin with `[REDACTED]` (safety net even with `log_user_prompt` off), and extend the file `description` to mention it:
  - `{ "set": { "field": "labels.prompt", "value": "[REDACTED]", "override": true, "if": "ctx.service?.name == 'codex_cli_rs' && ctx.labels?.prompt != null" } }`

  No script change — the existing `setup-logs-drop.{sh,ps1}` PUTs this file. Verify via `_simulate` (a `codex_cli_rs` doc with `labels.prompt` → `[REDACTED]`; a doc lacking it → field not created; a non-`codex_cli_rs` doc → untouched) and a live Codex turn.
- [x] Characterize the Codex `PostToolUse` hook payload: add a stack-local capture hook (POSIX + PowerShell) under `components/agents/codex-cli/hooks/`, registered via `render-hooks.sh`, appending each raw stdin payload to `.codex/hook-captures/tool-call.ndjson` (no Elasticsearch, no reshaping, fail-open and always exit 0); confirm the shape of `tool_name` / `tool_use_id` / `tool_input` / `tool_response` against both a shell tool and an MCP tool.
- [x] Implement the Codex tool-call audit hook specified in `SPEC/agent-audit.md` "Tool call documents": clone the `UserPromptSubmit` hook onto `PostToolUse` (`capture-tool-call.{sh,ps1}` + `render-hooks.sh`, stream name in the script); add a new strict template `elasticsearch/agent-audit.tool_call.template.json` and extend `setup-agent-audit.{sh,ps1}` to also provision the `logs-agent_audit.tool_call-default` stream and a `create_doc` scope on it; a verify script; and in `kibana/agent-audit.ndjson` (already imported — no import-script change) a Data View **Agent Audit — Tool Calls** (`logs-agent_audit.tool_call-*`) plus a Saved Search of the same name on it (data view before saved search), `@timestamp` desc, columns: `user.id`, `host.name`, `agent_audit.agent.name`, `agent_audit.agent.model`, `agent_audit.agent.account.name`, `agent_audit.agent.account.email`, `agent_audit.agent.organization.name`, `agent_audit.tool_call.tool.name`, `agent_audit.tool_call.input.text`, `agent_audit.tool_call.input.length`, `agent_audit.tool_call.output.text`, `agent_audit.tool_call.output.length`, `agent_audit.conversation_id`, `agent_audit.turn_id`.
- [x] Rename the Agent Audit user-prompt field `agent_audit.prompt.*` → `agent_audit.user_prompt.*` per `SPEC/agent-audit.md` (consistency with the `user_prompt` dataset). Update the strict template `components/backends/elastic/elasticsearch/agent-audit.user_prompt.template.json`, the hook document builders `components/agents/codex-cli/hooks/capture-user-prompt.{sh,ps1}`, the `Agent Audit — User Prompts` saved-search columns in `components/backends/elastic/kibana/agent-audit.ndjson` (`agent_audit.prompt.text` / `.length` → `agent_audit.user_prompt.*`), and the assertions in `stacks/codex-cli-elastic/scripts/verify-agent-audit.{sh,ps1}`. The strict mapping changes, so re-run `setup-agent-audit.{sh,ps1}` and recreate the `logs-agent_audit.user_prompt-default` data stream (pre-rename docs keep the old field — operator step, out of scope).
- [x] **Split the Codex audit concern out of `codex-cli-elastic`** into `components/backends/elastic-audit/` + `stacks/codex-cli-elastic-audit/`, per `SPEC/SPEC.md` "Repository shape" and `SPEC/agent-audit.md` "Where this runs". The stacks are alternatives (not co-run), reusing the same container names / ports.
  - **`components/backends/elastic-audit/`** — copy `backends/elastic`'s compose + `config/` **minus `apm-server`** (the service and `config/apm-server.yml`). Move into it, out of `backends/elastic`: `scripts/setup-agent-audit.{sh,ps1}`, `elasticsearch/agent-audit.{user_prompt,tool_call}.template.json`, `kibana/agent-audit.ndjson`, and an `import-kibana-objects.{sh,ps1}` that imports it.
  - **Slim `backends/elastic/`** — delete the moved assets and drop `agent-audit.ndjson` from its import FILES list; keep `trace-routing` / `logs-drop` + APM Server. (Leave the unrelated `prompts-audit` / `setup-prompt-audit` prototype.)
  - **`stacks/codex-cli-elastic-audit/`** — `name: codex-cli-elastic-audit`, `include` the new backend; `setup.{sh,ps1}` runs only `setup-agent-audit` → `render-agent-audit` → `render-hooks` → import audit Kibana (no `render-config`). Move `verify-agent-audit.{sh,ps1}` and `verify-tool-call-audit.{sh,ps1}` here; add a Quick-Tour `README.md`.
  - **Slim `stacks/codex-cli-elastic/`** — `setup.{sh,ps1}` drops the audit steps (keeps `trace-routing` / `logs-drop` / `render-config` / telemetry Kibana); `.codex` now holds only `config.toml`; update `README.md`. `smoke-test.sh` stays.
  - **Verify:** every stack `docker compose config -q` passes; a session on `codex-cli-elastic` emits only OTLP (no `agent_audit.*`, no `hooks.json`); a session on `codex-cli-elastic-audit` lands `agent_audit.user_prompt` / `tool_call` and both verifiers pass with no `apm-server` in the project; `backends/elastic` references no `agent-audit` asset.
- [x] Format then make our shell scripts clean. With the repo `.editorconfig` in place, run `shfmt -w` (no `-s`) over our `*.sh` — normalizes to 2-space, indented `case`, spaced redirects, and expands the compact `|| { …; }` guards to multi-line. Then fix the remaining shellcheck findings: **SC1007** `CDPATH= cd` → `CDPATH='' cd` (~24) and **SC2015** `… && … || …` → `if … then … else … fi` (~2). Verify `shfmt -d` is empty and `shellcheck` is clean on the touched scripts.
- [x] Format then make our PowerShell scripts clean (mirror the shell task), using the repo's `PSScriptAnalyzerSettings.psd1` (already in place; excludes Write-Host/BOM, pins 4-space). **Format**: run `Invoke-Formatter -Settings PSScriptAnalyzerSettings.psd1` over our `*.ps1` (write back). **Then fix** in code: **PSAvoidUsingPositionalParameters** (~22) — call the internal helpers (`Invoke-Provision`, `Invoke-Step`, …) with named parameters; **PSAvoidUsingEmptyCatchBlock** (~5, the fail-open catches in `capture-*` / `verify-*`) — give each a behavior-neutral body `catch { Write-Verbose "fail-open: $_" }` (keeps the rule live for future accidental empties). Verify `Invoke-ScriptAnalyzer -Settings PSScriptAnalyzerSettings.psd1` is clean on the tracked `*.ps1`.
- [x] **Migrate the Codex hooks from a sidecar `hooks.json` to inline `[hooks]` in `config.toml`, sourced from a new `components/agents/codex-cli/hooks.template.toml`** — prep for the managed-config work (see `SPEC/codex-cli-managed-config.md`): the per-hook table body is identical across `hooks.json`, inline `config.toml`, and `requirements.toml`, so a single TOML source is reusable by the future `codex-cli-requirements` stack and removes the JSON-heredoc duplication across `render-hooks.{sh,ps1}`. The new template carries the two existing Agent Audit hooks (`UserPromptSubmit` → `capture-user-prompt`, `PostToolUse` → `capture-tool-call`; same `command`/`commandWindows`/`timeout`/`statusMessage` as today's `hooks.json`) with the absolute script paths as `@@…@@` placeholders, mirroring `mcp.template.toml` + the `render-config.sh` substitution. **Rewrite `render-hooks.{sh,ps1}`** to substitute those placeholders and **append** the tables into `<target>/.codex/config.toml` on the `render-mcp` append model (idempotency guard: skip if `[[hooks.UserPromptSubmit]]` already present; newline-separate if the file exists), keeping the four-script existence precondition; it no longer writes `hooks.json`. Update the header comments. Order in `stacks/codex-cli-elastic-audit/scripts/setup.{sh,ps1}`: `render-hooks` (the primary `config.toml` content) stays at its step-3 position and `render-mcp` (the add-on) remains **last** — both are create-or-append-safe, so `config.toml` ends up with `[hooks]` then the appended `[mcp_servers]`; fix the step description that says hooks land in `.codex/hooks.json`. **Verify:** `render-hooks.sh` on a fresh target writes no `hooks.json` and appends valid `[[hooks.*]]` (resolved absolute paths) to `config.toml`, second run is a no-op; on a `codex-cli-elastic-audit` session both hooks fire and `verify-agent-audit.{sh,ps1}` + `verify-tool-call-audit.{sh,ps1}` pass (delete any stale `.codex/hooks.json` first — one representation per layer); `shfmt -d` empty + `shellcheck` clean on touched `*.sh`, `Invoke-ScriptAnalyzer` clean on touched `*.ps1`.
- [x] **Rename `components/agents/codex-cli/config.template.toml` → `otel.template.toml` and `render-config.{sh,ps1}` → `render-otel.{sh,ps1}`** (the template is purely the `[otel]` block — the name should say so; separable cleanup from the hooks-TOML migration). Update the internal `template=…/config.template.toml` reference inside the renamed scripts. The only real caller is `stacks/codex-cli-elastic/scripts/setup.{sh,ps1}` (step-3 `render-config` → `render-otel`); update it. Every other `render-config` / `config.template.toml` match is a **comment** (in `stacks/codex-cli-elastic-audit/scripts/setup.{sh,ps1}`, `render-hooks.{sh,ps1}`, `render-agent-audit.{sh,ps1}`) — refresh that wording to the new names; do **not** touch `PRD.md` history or `reports/report.html`. **Verify:** `codex-cli-elastic` `scripts/setup.{sh,ps1}` renders `.codex/config.toml` and `scripts/smoke-test.sh` reaches PASS; `grep -r render-config` / `config.template.toml` finds no live (non-comment, non-history) reference.
- [x] **Point the four Codex audit verifiers at the inline `[hooks]` in `.codex/config.toml` instead of `.codex/hooks.json`** — correction to the hooks-TOML migration: `render-hooks` no longer writes `hooks.json`, but `verify-agent-audit.{sh,ps1}` and `verify-tool-call-audit.{sh,ps1}` still skip-gate on and `jq`/`ConvertFrom-Json`-parse it (so they skip once it's gone, or false-pass while it lingers). In all four, change the missing-file skip from `hooks.json` to `config.toml`, and replace the JSON registration check with a presence check of the table in `config.toml` (`grep -qF` in `.sh`, `Select-String -SimpleMatch` in `.ps1`; no TOML parser): `verify-agent-audit` → `[[hooks.UserPromptSubmit]]`, `verify-tool-call-audit` → `[[hooks.PostToolUse]]`. Update the header comments; leave the ES-landing logic unchanged. **Verify:** with the stack set up and **no `.codex/hooks.json`**, all four reach genuine **PASS** (not skip); `shfmt -d` empty + `shellcheck` clean on touched `*.sh`, `Invoke-ScriptAnalyzer` clean on touched `*.ps1`.
- [x] **Make the Codex capture hooks read the new flat `.codex/agent-audit.conf` (per-stream, zero external deps)** — the template + `render-agent-audit.{sh,ps1}` already emit `agent-audit.conf` (flat `key=value`; schema in `SPEC/agent-audit.md` "Delivery and authorization"), but `hooks/capture-user-prompt.{sh,ps1}` and `capture-tool-call.{sh,ps1}` still read the old `agent-audit.toml` (`[elasticsearch]` + single `data_stream` + `audit.mode`). Update all four to parse the `.conf` with **no external dependency** (`jq` is forbidden — the hooks ship to every workstation): POSIX shells via a pure-`read` `cfg_get` loop (no `jq`/subprocess parser; bash 3.2-safe), PowerShell via the built-in `ConvertFrom-StringData`. Each hook reads **only its own** stream's keys (`capture-user-prompt` → `user_prompt`, `capture-tool-call` → `tool_call`): `capture.<stream>.enabled` (when `false`, exit 0 without delivering — fail-open skip), `capture.<stream>.content` (`plaintext` populates `.text` as today; `encrypted` must **not** write plaintext — leave `.text`/`.encrypted_text` null, since sealing is not built yet), plus shared `elasticsearch.url` / `.api_key` / `.timeout_ms` and the per-stream `elasticsearch.data_stream.<stream>` (replacing `capture-tool-call`'s hardcoded stream name and `capture-user-prompt`'s old single `data_stream`). Also switch the `agent-audit.toml` → `agent-audit.conf` presence/skip reference in `verify-agent-audit.{sh,ps1}` / `verify-tool-call-audit.{sh,ps1}` and the step wording in `stacks/codex-cli-elastic-audit/scripts/setup.{sh,ps1}`. **Verify:** `grep -ri 'jq\|agent-audit\.toml'` over the hooks/verifiers finds nothing; a `codex-cli-elastic-audit` session lands `agent_audit.user_prompt` / `tool_call` docs in the per-stream `data_stream` values from the `.conf`; `capture.tool_call.enabled=false` suppresses only tool-call docs; both verifiers PASS; `shfmt -d` empty + `shellcheck` clean on touched `*.sh`, `Invoke-ScriptAnalyzer` clean on touched `*.ps1`.
- [x] **Move all Kibana saved objects under the backend (namespaced per source) and make the backend the sole importer** — Kibana objects are consumed by Kibana, a backend service, so per-agent / per-path NDJSON belongs with the backend that runs Kibana — alongside the already-backend-located agent-specific ingest pipelines (`logs-drop` / `trace-routing`) — not under `agents/` / `paths/`. This realigns the code to the placement rule in `SPEC/SPEC.md` ("an artifact lives with the runtime that consumes it"). **Moves** (content unchanged; object IDs unchanged):
  - `components/agents/codex-cli/kibana/*.ndjson` → `components/backends/elastic/kibana/codex-cli/`
  - `components/agents/claude-code/kibana/*.ndjson` → `components/backends/elastic/kibana/claude-code/`
  - `components/paths/otelcol-sidecar/kibana/*.ndjson` → `components/backends/elastic/kibana/otelcol-sidecar/`
  - `components/backends/elastic/kibana/agents-data-views.ndjson` → `components/backends/elastic/kibana/cross-agent/agents-data-views.ndjson`
  - `elastic-audit` is unchanged: `kibana/agent-audit.ndjson` is its single cross-agent file (no per-source dirs, no agent assets to move).

  **Delete** the per-source import scripts: `components/agents/codex-cli`, `components/agents/claude-code`, `components/paths/otelcol-sidecar` `scripts/import-kibana-objects.{sh,ps1}` (6 files), and remove the now-empty `kibana/` dirs under `agents/` / `paths/`.

  **Make `components/backends/elastic/scripts/import-kibana-objects.{sh,ps1}` the sole importer.** It takes source-namespace names as arguments and imports `kibana/cross-agent/` first (so cross-agent data views like `cce-agents-traces` resolve), then each named `kibana/<source>/` dir; within every dir it imports in category order **data-views → saved-searches → dashboard** (by filename). Keep the curl/jq presence-skip, fail-loud-on-`success:false`, and per-file result print. `components/backends/elastic-audit/scripts/import-kibana-objects.{sh,ps1}` is unchanged (imports its one cross-agent file).

  **Rewire stack setup** (`.sh` + `.ps1`), replacing the former 2–3 separate import calls with the single backend importer plus the composed source namespaces:
  - `codex-cli-elastic` → `import-kibana-objects … codex-cli`
  - `claude-code-elastic` → `… claude-code`
  - `claude-code-otelcol-elastic` → `… claude-code otelcol-sidecar`
  - `codex-cli-elastic-audit` → unchanged (calls the `elastic-audit` importer).

  Pass the namespaces as positional args in `.sh`; in `.ps1` via the existing `Invoke-Step -StepArgs`. **Verify:** each stack's `setup` imports the same objects as before — same IDs, same `successCount`, references resolved (cross-agent data views before the per-agent saved searches); `find components/agents components/paths -name 'import-kibana-objects.*' -o -type d -name kibana` returns nothing; `shfmt -d` empty + `shellcheck` clean on touched `*.sh`, `Invoke-ScriptAnalyzer` clean on touched `*.ps1`. SPEC is already updated — do not edit `SPEC/`.
- [x] **Group the Codex agent's render-input templates under `components/agents/codex-cli/templates/`** — the component has three lifecycle roles but only two are directories: `scripts/` (the `render-*` build tooling) and `hooks/` (the `capture-*` runtime), while the four render-input templates sit loose at the component root. Move them into a peer `templates/` dir so the root is three single-role directories: `otel.template.toml`, `mcp.template.toml`, `hooks.template.toml`, `agent-audit.template.conf` → `templates/`. **Keep the `.template` infix** so a template never shares a basename with its rendered output (the `agent-audit.template.conf` template vs the rendered `.codex/agent-audit.conf`). Update the `template=…` (`.sh`) / `$Template = Join-Path …` (`.ps1`) path in each render script — `render-otel`, `render-mcp`, `render-hooks`, `render-agent-audit` — to point at `…/templates/<name>` (the render scripts stay in `scripts/`). Nothing else references the templates: render scripts self-locate and stacks only invoke `render-*`, so **no stack change**. **Verify:** each render script writes its target from the new path (e.g. `render-otel.sh <tmp>` → `.codex/config.toml`; `render-agent-audit.sh <url> <tmp>` → `.codex/agent-audit.conf`); a fresh `codex-cli-elastic-audit` setup still yields `.codex/config.toml` (inline `[hooks]` + the Elasticsearch MCP) + `.codex/agent-audit.conf`; `shfmt -d` empty + `shellcheck` clean on touched `*.sh`, `Invoke-ScriptAnalyzer` clean on touched `*.ps1`.
- [x] **Add a `redacted` content mode to the audit capture hooks** — `capture.<stream>.content` currently supports `plaintext` (real body in `.text`) and `encrypted` (null `.text`, reserved `.encrypted_text`, not yet built). Add **`redacted`**: deliver the document but put a fixed `[REDACTED]` marker in the body's `.text` while keeping the **true `.length`** and leaving `.encrypted_text` `null` — so the delivery path (identity, routing, strict mapping, landing) can be verified **without exposing prompt / tool content** (schema in `SPEC/agent-audit.md` "Delivery and authorization"). Implement in all four capture hooks at the existing body-form branch (`plaintext` → real, `redacted` → `[REDACTED]`, `encrypted`/anything-else → `null`): `capture-user-prompt.{sh,ps1}` (`agent_audit.user_prompt.text`) and `capture-tool-call.{sh,ps1}` (**both** `agent_audit.tool_call.input.text` and `.output.text`). `.length` is always the true character count regardless of mode. Update the `content (plaintext|redacted|encrypted)` comment in `components/agents/codex-cli/templates/agent-audit.template.conf`. **Verify:** with `capture.user_prompt.content=redacted`, a delivered `user_prompt` doc has `.text` exactly `[REDACTED]` and `.length` = the real prompt length; with `capture.tool_call.content=redacted`, both `input.text` and `output.text` are `[REDACTED]` with true lengths; `plaintext`/`encrypted` behaviour unchanged; `shfmt -d` empty + `shellcheck` clean on touched `*.sh`, `Invoke-ScriptAnalyzer` clean on touched `*.ps1`.
- [x] **Retire the Claude Code — Overview dashboard** — it predates the saved-search-centric standard the Codex source sets (no dashboard). Delete `components/backends/elastic/kibana/claude-code/dashboard.ndjson` (id `claude-code-overview`); no import-script change — the importer's `dashboard` category stays (the `otelcol-sidecar` source still ships one), it simply globs to nothing for the `claude-code` source and is skipped. **Keep `claude-code-metrics`** even though the dashboard was its only user — it completes the metrics/events/traces trio and will back the Claude metrics saved searches ported from Codex. README + SPEC already updated — do **not** edit them. **Verify:** fresh `claude-code-elastic` and `claude-code-otelcol-elastic` setups import their objects cleanly (Claude ships no dashboard, otelcol-sidecar's dashboard still imports); `claude-code-overview` is gone from Kibana.
- [x] **Drop the cross-agent `agents-traces` data view, re-pointing its only users first.** `agents-traces` (`AI Agents — Traces`, `traces-apm-agents_*`) is used by three Codex trace searches — **Turn Timeline (Traces)**, **LLM Requests (Traces / Duration)**, **Tool Calls (Traces / Execution)** — and by nothing else; Codex's agent-scoped `codex-cli-traces` (`traces-apm-agents_codex_cli_rs*`, same `trace.id` formatter) is the right home for single-agent searches.
  1. In `components/backends/elastic/kibana/codex-cli/saved-searches.ndjson`, change every `"id":"agents-traces"` in those three searches' `references[]` to `"id":"codex-cli-traces"` (6 occurrences; change only the ref `id`s, leave the `name`s, filters, columns, sort untouched).
  2. Delete `components/backends/elastic/kibana/cross-agent/agents-data-views.ndjson` (the empty `cross-agent/` dir goes with it).
  3. In **both** `components/backends/elastic/scripts/import-kibana-objects.{sh,ps1}`, remove the now-dangling `kibana/cross-agent` import call + its comment (it would `fail` "not found" on a fresh clone) and the cross-agent lines in the header doc-comment.

  README + SPEC already updated — do **not** edit them. **Verify:** `grep -rn agents-traces components/` is empty; `codex-cli-elastic` setup imports cleanly and the three searches open on `codex-cli-traces` with `trace.id` click-through intact; `claude-code-elastic` / `claude-code-otelcol-elastic` import cleanly with no cross-agent step; `shfmt -d` empty + `shellcheck` clean on the two `.sh`, `Invoke-ScriptAnalyzer` clean on the two `.ps1`.
- [x] **Delete four out-of-model Claude saved searches** from `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — `claude-code-auth-events`, `claude-code-mcp-server-connections`, `claude-code-permission-mode-changes`, `claude-code-subagent-completions` (session-lifecycle / nested-agent views outside `SPEC/agent-activity-model.md`; events stay in `logs-apm.app.claude_code*`, visible via Event Overview) — and set the trailing `exportedCount` 18 → 14 (leave `missingReferences` as-is). README/SPEC already updated. **Verify:** imports with `successCount: 14`; the four are gone from Discover.
- [x] **Add a Claude Code — Conversations saved search** (ES|QL Discover session, id `claude-code-conversations`, title `Claude Code — Conversations`) to `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — the **session-level usage rollup** (Conversation = `session.id`), Claude's counterpart to **Codex CLI — Conversations**, authored in the **same ES|QL house style** (`EVAL is_*` discriminators, `ts_ms` duration, `docs`, `COUNT_DISTINCT`). The query carries `FROM`, so **no data-view reference**; increment the trailing `exportedCount` 14 → 15. **Events-only for now** — trace-derived columns (precise per-turn duration, TTFT) are added in a later diff once tracing is enabled. Query (verified live against `logs-apm.app.claude_code-default`):
  ```esql
  FROM logs-apm.app.claude_code*
  | EVAL ts_ms = TO_LONG(@timestamp)
  | EVAL is_user_prompt = message == "claude_code.user_prompt"
  | EVAL is_llm_request = message == "claude_code.api_request"
  | EVAL is_tool_call = message == "claude_code.tool_result"
  | EVAL is_hook = message == "claude_code.hook_execution_complete"
  | STATS
      first_seen = MIN(@timestamp),
      last_seen = MAX(@timestamp),
      duration_sec = (MAX(ts_ms) - MIN(ts_ms)) / 1000.0,
      docs = COUNT(*),
      turns = COUNT_DISTINCT(labels.prompt_id),
      user_prompts = COUNT_DISTINCT(CASE(is_user_prompt, labels.prompt_id, null)),
      llm_requests = COUNT_DISTINCT(CASE(is_llm_request, labels.request_id, null)),
      tool_calls = COUNT_DISTINCT(CASE(is_tool_call, labels.tool_use_id, null)),
      hooks = COUNT(CASE(is_hook, 1, null)),
      input_tokens = SUM(CASE(is_llm_request, numeric_labels.input_tokens, null)),
      cache_read_tokens = SUM(CASE(is_llm_request, numeric_labels.cache_read_tokens, null)),
      cache_creation_tokens = SUM(CASE(is_llm_request, numeric_labels.cache_creation_tokens, null)),
      output_tokens = SUM(CASE(is_llm_request, numeric_labels.output_tokens, null)),
      cost_usd = SUM(CASE(is_llm_request, numeric_labels.cost_usd, null))
    BY conversation_id = session.id
  | SORT last_seen DESC
  ```
  Set `attributes.columns` to the output order: `["first_seen","last_seen","duration_sec","docs","turns","user_prompts","llm_requests","tool_calls","hooks","input_tokens","cache_read_tokens","cache_creation_tokens","output_tokens","cost_usd","conversation_id"]`. README/SPEC current (de-enumerated) — do not edit. **Verify:** the file imports cleanly (`exportedCount` 15), the search opens as an ES|QL Discover session, one row per `session.id` with the columns above over real `claude-code` telemetry.
- [x] **Add a Claude Code — Turns saved search** (ES|QL Discover session, id `claude-code-turns`, title `Claude Code — Turns`) to `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — the **per-turn usage rollup** (Turn = `labels.prompt_id`), Claude's counterpart to **Codex CLI — Turns**, same ES|QL house style; the query carries `FROM`, so **no data-view reference**; increment the trailing `exportedCount` by 1 (15 → 16, after the Conversations task). **Events-only for now.** Deferred to the traces diff: `ttft_sec`, a span-based `duration_sec`, a `trace_id` column, and moving the turn key from `prompt_id` to `trace.id`. **N/A for Claude:** `tool_catalogues` (no such event). **Held:** `errors` (pending `api_error` verification). Note `duration_sec` here is the first→last **log-event** span of the turn (an approximation, not span wall-time). Query (verified live against `logs-apm.app.claude_code-default`):
  ```esql
  FROM logs-apm.app.claude_code*
  | WHERE labels.prompt_id IS NOT NULL
  | EVAL ts_ms = TO_LONG(@timestamp)
  | EVAL is_user_prompt = message == "claude_code.user_prompt"
  | EVAL is_llm_request = message == "claude_code.api_request"
  | EVAL is_tool_call = message == "claude_code.tool_result"
  | EVAL is_hook = message == "claude_code.hook_execution_complete"
  | STATS
      first_seen = MIN(@timestamp),
      last_seen = MAX(@timestamp),
      duration_sec = (MAX(ts_ms) - MIN(ts_ms)) / 1000.0,
      docs = COUNT(*),
      user_prompts = COUNT_DISTINCT(CASE(is_user_prompt, labels.prompt_id, null)),
      llm_requests = COUNT_DISTINCT(CASE(is_llm_request, labels.request_id, null)),
      tool_calls = COUNT_DISTINCT(CASE(is_tool_call, labels.tool_use_id, null)),
      hooks = COUNT(CASE(is_hook, 1, null)),
      input_tokens = SUM(CASE(is_llm_request, numeric_labels.input_tokens, null)),
      cache_read_tokens = SUM(CASE(is_llm_request, numeric_labels.cache_read_tokens, null)),
      cache_creation_tokens = SUM(CASE(is_llm_request, numeric_labels.cache_creation_tokens, null)),
      output_tokens = SUM(CASE(is_llm_request, numeric_labels.output_tokens, null)),
      cost_usd = SUM(CASE(is_llm_request, numeric_labels.cost_usd, null)),
      conversation_id = VALUES(session.id)
    BY prompt_id = labels.prompt_id
  | SORT last_seen DESC
  ```
  Set `attributes.columns` to the output order: `["first_seen","last_seen","duration_sec","docs","user_prompts","llm_requests","tool_calls","hooks","input_tokens","cache_read_tokens","cache_creation_tokens","output_tokens","cost_usd","conversation_id","prompt_id"]`. README/SPEC current (de-enumerated) — do not edit. **Verify:** the file imports cleanly (`exportedCount` 16), the search opens as an ES|QL Discover session, one row per `labels.prompt_id` (turn) with the columns above over real `claude-code` telemetry.
- [x] **Rework the Claude Code — User Prompts saved search** (existing id `claude-code-user-prompts`) in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — the User Prompt content view (per `SPEC/agent-activity-model.md`). Keep it a `search` on the `claude-code-events` data view with a **single filter pill `message: claude_code.user_prompt`** (empty query bar; `meta.index` wired through `references[]`). Replace `attributes.columns` with: `["@timestamp","labels.prompt","labels.prompt_length","labels.command_name","labels.command_source","session.id","labels.prompt_id"]`. Notes: `labels.command_name` / `labels.command_source` are populated only when the prompt is a slash-command invocation (empty for free-text) — doc-confirmed `claude_code.user_prompt` attributes; `model` / `text_input_count` / `image_input_count` are **not** emitted on Claude's event (Codex-only), so they're omitted; `trace.id` is deferred to the traces diff. Column-only edit — no `exportedCount` change. README/SPEC current — do not edit. **Verify:** the file imports cleanly, the search opens with the single `message` pill (empty query bar) and exactly the columns above over real `claude-code` telemetry.
- [x] **Add a Claude Code — Turn Timeline (Events) saved search** (id `claude-code-turn-timeline-events`, title `Claude Code — Turn Timeline (Events)`) to `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — the per-activity events timeline (one row per event), Claude's events-only counterpart to **Codex CLI — Turn Timeline (Traces)** (which Claude can't build with tracing off). A `search` on the `claude-code-events` data view, `@timestamp` desc, **no filter pill and empty query bar** — Claude Code is low-noise, so the timeline shows every event type and is scoped to a turn interactively (add a `labels.prompt_id` filter in Discover). Increment the trailing `exportedCount` by 1. Columns (`@timestamp` lead, no identity, per the Codex-aligned convention in `SPEC/agent-activity-model.md`): `["@timestamp","message","labels.prompt","labels.prompt_length","labels.model","labels.tool_name","labels.hook_name","labels.error_type","session.id","labels.prompt_id"]` — `message` is the "what"; the rest are the union of per-event-type discriminators (sparse per row, all field paths verified live). `trace.id` is deferred to the traces diff. README/SPEC current — do not edit. **Verify:** the file imports cleanly, the search opens with an empty query bar / no filter pill, all event types interleaved by `@timestamp` desc, with exactly the columns above over real `claude-code` telemetry.
- [x] **Delete the Claude Code — Event Overview saved search** (id `claude-code-event-overview`) from `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — superseded by **Turn Timeline (Events)** (the same unfiltered all-event timeline, with richer per-activity columns); the events stay in `logs-apm.app.claude_code*`, visible via Turn Timeline / Discover, so nothing is lost. Decrement the trailing `exportedCount` by 1. README/SPEC current — do not edit. **Verify:** the file imports cleanly, `claude-code-event-overview` is gone from Discover, the remaining searches open unchanged.
- [x] **Rework Claude Code — API Requests into Claude Code — LLM Requests** in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — rename the existing search to the abstract LLM Request entity (`SPEC/agent-activity-model.md`): id `claude-code-api-requests` → `claude-code-llm-requests`, title `Claude Code — API Requests` → `Claude Code — LLM Requests`. Keep it a `search` on the `claude-code-events` data view with the single filter pill `message: claude_code.api_request` (empty query bar; `meta.index` via `references[]`). Replace `attributes.columns` with: `["@timestamp","labels.model","numeric_labels.duration_ms","numeric_labels.input_tokens","numeric_labels.cache_read_tokens","numeric_labels.cache_creation_tokens","numeric_labels.output_tokens","session.id","labels.prompt_id"]` (`@timestamp` lead, no identity; dropped `query_source`/`speed`/`effort`/`cost_usd`/`user_email`). The Execution facet (span duration/outcome) and the communication/error view come later from traces and the existing **API Errors** search respectively; `trace.id` is deferred to the traces diff. No `exportedCount` change (rename in place). On an already-populated Kibana, delete the stale `claude-code-api-requests` object (fresh imports are clean). README/SPEC current — do not edit. **Verify:** the file imports cleanly, the search opens as **Claude Code — LLM Requests** with the single `message: claude_code.api_request` pill and exactly the columns above over real `claude-code` telemetry.
- [x] **Delete the Claude Code — Tool Decisions, Tool Output, and Tool I/O (joined) saved searches** (ids `claude-code-tool-decisions`, `claude-code-tool-output`, `claude-code-tool-io`) from `components/backends/elastic/kibana/claude-code/saved-searches.ndjson`, as part of consolidating the Tool Call activity onto a single events-only view. **Tool Decisions** — Codex has no standalone permit-decision view (the decision rides on the tool_result metadata); drop it. **Tool Output** (`tool.output` content/diff) and **Tool I/O (joined)** (the ES|QL content-join) — the actual tool I/O is audit-concern content, not a telemetry view (same reasoning as moving identity to audit), and both need traces + `OTEL_LOG_TOOL_CONTENT` anyway. The underlying events/spans remain in the data streams. Decrement the trailing `exportedCount` by 3. README/SPEC current — do not edit. **Verify:** the file imports cleanly, none of the three appear in Discover, the rest open unchanged.
- [x] **Rework Claude Code — Tool Results into Claude Code — Tool Calls** in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — rename the existing search to the abstract Tool Call entity (`SPEC/agent-activity-model.md`): id `claude-code-tool-results` → `claude-code-tool-calls`, title `Claude Code — Tool Results` → `Claude Code — Tool Calls`. Keep it a `search` on the `claude-code-events` data view with the single filter pill `message: claude_code.tool_result` (empty query bar; `meta.index` via `references[]`). Replace `attributes.columns` with: `["@timestamp","labels.success","labels.duration_ms","labels.tool_name","labels.tool_input_size_bytes","labels.tool_result_size_bytes","labels.mcp_server_scope","session.id","labels.prompt_id"]` (`@timestamp` lead, no identity; dropped `user_email`, `decision_type`/`decision_source`, and the gated `tool_input`/`tool_parameters` — tool arguments are audit-concern content and `OTEL_LOG_TOOL_DETAILS` drags in every tool's args; `mcp_server_scope` is the clean ungated MCP signal — local/project/user). `labels.duration_ms` is a **string** label (display only). This is the Metadata facet; the Execution facet (span duration/outcome) and tool I/O content come from traces and the audit stream respectively; `trace.id` is deferred to the traces diff. No `exportedCount` change (rename in place). On an already-populated Kibana, delete the stale `claude-code-tool-results` object (fresh imports are clean). README/SPEC current — do not edit. **Verify:** the file imports cleanly, the search opens as **Claude Code — Tool Calls** with the single `message: claude_code.tool_result` pill and exactly the columns above over real `claude-code` telemetry.
- [x] **Delete the Claude Code — Hook Registered saved search** (id `claude-code-hook-registered`) from `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — the Hooks node consolidates onto the runtime view (Hook Executions → **Hooks**); hook registration/provenance (`hook_source`) is startup inventory with no Codex counterpart. The `hook_registered` event remains in `logs-apm.app.claude_code*`. Decrement the trailing `exportedCount` by 1. README/SPEC current — do not edit. **Verify:** the file imports cleanly, `claude-code-hook-registered` is gone from Discover, the rest open unchanged.
- [x] **Rework Claude Code — Hook Executions into Claude Code — Hooks** in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — rename the existing runtime view to the Hooks node (`SPEC/agent-activity-model.md`): id `claude-code-hook-executions` → `claude-code-hooks`, title `Claude Code — Hook Executions` → `Claude Code — Hooks`. Keep it a `search` on the `claude-code-events` data view with the single filter pill `message: claude_code.hook_execution_complete` (empty query bar; `meta.index` via `references[]`). Replace `attributes.columns` with: `["@timestamp","labels.hook_event","labels.hook_name","labels.hook_source","labels.num_hooks","labels.num_success","labels.num_blocking","labels.num_non_blocking_error","labels.num_cancelled","labels.total_duration_ms","session.id","labels.prompt_id"]` (`@timestamp` lead, no identity; `hook_source` here is the coarse `merged` / `policySettings` policy flag — granular provenance lived on the now-deleted Hook Registered; added `num_non_blocking_error` / `num_cancelled` for full outcome). `hook_source`, all `num_*`, and `total_duration_ms` are **string** labels (display only). `trace.id` deferred to the traces diff. No `exportedCount` change (rename in place). On an already-populated Kibana, delete the stale `claude-code-hook-executions` object (fresh imports are clean). README/SPEC current — do not edit. **Verify:** the file imports cleanly, the search opens as **Claude Code — Hooks** with the single `message: claude_code.hook_execution_complete` pill and exactly the columns above over real `claude-code` telemetry.
- [x] **Delete four more out-of-scope Claude saved searches** from `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — `claude-code-api-bodies` (**API Bodies** — raw full request/response bodies, `OTEL_LOG_RAW_API_BODIES`-gated content = audit territory, low utility), `claude-code-api-retries-exhausted` (**API Retries Exhausted** — near-duplicate of API Errors, empty in normal operation), `claude-code-interactions` (**Interactions** — trace-based per-turn view, empty with tracing off and largely overlapped by Turn Timeline / Turns), and `claude-code-traces-overview` (**Traces** — trace-overview, empty with tracing off). The traces-based two (Interactions, Traces) are **deferred to the traces diff**, not gone for good. The underlying events/spans remain in the data streams. Decrement the trailing `exportedCount` by 4; since Interactions and Traces were the only searches referencing the `claude-code-traces` data view, also **drop `claude-code-traces` from the summary line's `missingReferences`** (`missingRefCount` 2 → 1) — the `claude-code-traces` data-view object itself stays in `data-views.ndjson` for the traces diff. README/SPEC current — do not edit. **Verify:** the file imports cleanly, none of the four appear in Discover, the rest open unchanged.
- [x] **Rework Claude Code — API Errors into Claude Code — LLM Request Errors** in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — rename to the LLM Request error complement (`SPEC/agent-activity-model.md`; matches the `LLM Requests` rename and Codex's **LLM Request Errors**): id `claude-code-api-errors` → `claude-code-llm-request-errors`, title `Claude Code — API Errors` → `Claude Code — LLM Request Errors`. Keep it a `search` on the `claude-code-events` data view with the single filter pill `message: claude_code.api_error` (the event is itself the failure twin of `api_request`, so no `success` predicate is needed; empty query bar; `meta.index` via `references[]`). Replace `attributes.columns` with (Codex-aligned order — time → duration → model → failure block → correlation last): `["@timestamp","numeric_labels.duration_ms","labels.model","numeric_labels.status_code","numeric_labels.attempt","labels.error","session.id","labels.prompt_id"]` (`@timestamp` lead, no identity; dropped `user_email`; added `session.id`; `trace.id` deferred to the traces diff). `numeric_labels.attempt` is the retry count — `> CLAUDE_CODE_MAX_RETRIES` means retries were exhausted, so this column **absorbs the deleted API Retries Exhausted signal**. No `exportedCount` change (rename in place). On an already-populated Kibana, delete the stale `claude-code-api-errors` object (fresh imports are clean). README/SPEC current — do not edit. **Verify:** the file imports cleanly and the search opens as **Claude Code — LLM Request Errors** with the single `message: claude_code.api_error` pill and exactly the columns above. (No `api_error` in the current sample, so a non-empty result can't be live-verified yet; the field paths are doc/SPEC-confirmed.)
- [x] **Add a Claude Code — Tool Call Errors saved search** (id `claude-code-tool-call-errors`, title `Claude Code — Tool Call Errors`) to `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — the Tool Call error complement (`SPEC/agent-activity-model.md`), Claude's counterpart to **Codex CLI — Tool Call Errors**. A `search` on the `claude-code-events` data view, `@timestamp` desc, with **two filter pills** — `message: claude_code.tool_result` AND `labels.success: false` (empty query bar; each filter's `meta.index` wired through `references[]`). Increment the trailing `exportedCount` by 1. Columns (the **Tool Calls** column set plus `error_type` before the correlation block; `@timestamp` lead, no identity, `trace.id` deferred): `["@timestamp","labels.success","labels.duration_ms","labels.tool_name","labels.tool_input_size_bytes","labels.tool_result_size_bytes","labels.mcp_server_scope","labels.error_type","session.id","labels.prompt_id"]`. `labels.error_type` is the ungated failure category (e.g. `McpError`, `Error:ENOENT`, `ShellError`); `success`/`duration_ms`/sizes are **string** labels. **Caveat (documented behaviour, do not re-litigate):** `success: false` includes benign/expected tool failures (no-match greps, non-zero exits, transient MCP retries), so this is a "failed tool calls" browse view — `error_type` and the row content distinguish real faults from normal trial-and-error. README/SPEC current — do not edit. **Verify:** the file imports cleanly and the search opens with both pills (empty query bar) and exactly the columns above over real `claude-code` telemetry (the current sample has one failed `mcp_tool` / `McpError` row).
- [x] **Swap the turn-correlation column `labels.prompt_id` → `trace.id` in the seven raw Claude events views** in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — with tracing now enabled, every in-turn event carries `trace.id` **1:1 with `prompt_id`** (verified live: within a traced turn, `api_request`/`tool_result`/`tool_decision`/`user_prompt`/`hook_execution_*` all populate it), and `trace.id` is the cross-signal bridge + **APM click-through** (URL `fieldFormatMap` already on the `claude-code-events` data view — confirm it survived the rebuild). In each view replace the trailing `labels.prompt_id` column with `trace.id` (**column change only** — filter pills, `references[]`, sort unchanged): `claude-code-user-prompts`, `claude-code-llm-requests`, `claude-code-llm-request-errors`, `claude-code-tool-calls`, `claude-code-tool-call-errors`, `claude-code-hooks`, `claude-code-turn-timeline-events`. Trade-off accepted: the column is blank for events emitted outside a trace (tracing off / startup events), since the stack now runs events+traces together. README/SPEC current — do not edit. **Verify:** each search imports cleanly, shows `trace.id` as its final column, and `trace.id` renders as a click-through to the APM trace view.
- [x] **Scope the timeline + Conversations to turn-bearing events, and re-key Turns to `trace.id`** in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — with events+traces both on, the tracing channel's `gen_ai.request.attempt` logs co-land in `logs-apm.app.claude_code*` (they carry `trace.id` but **no `prompt_id`**), and startup events (`mcp_server_connection`, `hook_registered`) also lack `prompt_id`. Use **`prompt_id` presence as the uniform turn-scoping guard** (it excludes both gen_ai and startup; a `trace.id`-based guard would wrongly re-admit gen_ai, which carries `trace.id`):
  - **Turn Timeline** (`claude-code-turn-timeline-events`): add a single filter pill **`labels.prompt_id` exists** (it currently has no filter), empty query bar.
  - **Conversations** (`claude-code-conversations`): add **`| WHERE labels.prompt_id IS NOT NULL`** to the ES|QL so `docs` and the rollup count only turn events — symmetric with Turns (same event set, grouped by `session.id`).
  - **Turns** (`claude-code-turns`): change the aggregation key `BY prompt_id = labels.prompt_id` → **`BY trace_id = trace.id`** (1:1 with the turn, Codex-aligned); rename the trailing `attributes.columns` entry `prompt_id` → `trace_id` (the key *is* the column — no separate `trace.id` column); keep `WHERE labels.prompt_id IS NOT NULL` and `conversation_id = VALUES(session.id)`. Assumes tracing stays on (tracing off → prompt-bearing events have null `trace.id` and collapse to one bucket).

  README/SPEC current — do not edit. **Verify:** Conversations / Turns / Turn Timeline show **no `gen_ai.request.attempt` rows**; Conversations `docs` counts only `claude_code.*` turn events; Turns yields one row per `trace.id` (= per turn) with `conversation_id` populated; all import cleanly over real `claude-code` telemetry.
- [x] **Shorten every Claude Code saved-search `description`** in `components/backends/elastic/kibana/claude-code/saved-searches.ndjson` — the current descriptions (293–659 chars) carry implementation rationale that crowds the Discover saved-objects list and search header. Replace each search's `attributes.description` with the concise one-liner below (matching the Codex CLI description style), dropping the boilerplate (`Built on … data view`, `Constraint shown as filter pill`, `@timestamp lead / no identity`, the `SPEC/agent-activity-model.md` reference, the `trace.id` deferred notes, the STRING-label caveats — all of which live in the PRD task history and SPEC). **Touch only `attributes.description`** — leave every `title`, `id`, `columns`, filter pill, sort, `references`, and ES|QL query untouched.
  1. **Conversations** → `Per-conversation usage rollup (ES|QL, one row per session.id): turns, prompts, LLM requests, tool calls, hooks, token sums, cost. Session-level companion to Turns.`
  2. **Turns** → `Per-turn usage rollup (ES|QL, one row per turn): prompts, LLM requests, tool calls, hooks, token sums, cost, duration. Turn-level companion to Conversations.`
  3. **Turn Timeline (Events)** → `Per-activity event timeline (one row per event): message is the "what" plus per-event-type columns; filter to a turn in Discover.`
  4. **User Prompts** → `User-prompt events (claude_code.user_prompt): prompt text (<REDACTED> unless OTEL_LOG_USER_PROMPTS=1), length, and slash-command.`
  5. **LLM Requests** → `LLM-request events (claude_code.api_request): model, latency, and token usage per request.`
  6. **LLM Request Errors** → `Failed LLM requests (claude_code.api_error): model, status code, retry attempt, error. Error complement of LLM Requests.`
  7. **Tool Calls** → `Tool-call metadata (claude_code.tool_result): outcome, duration, tool name, I/O sizes, mcp_server_scope.`
  8. **Tool Call Errors** → `Failed tool calls (claude_code.tool_result, success=false): tool, error_type, duration, sizes. Includes benign failures — error_type triages. Error complement of Tool Calls.`
  9. **Hooks** → `Hook-execution outcomes (claude_code.hook_execution_complete): hook event, run counts (success/blocking/error/cancelled), total duration, source.`

  README/SPEC current — do not edit. **Verify:** the file imports cleanly (9 search objects, references resolved) and each search opens with its shortened description.
- [x] **Replace `render-settings.{sh,ps1}` with `render-otel` — the telemetry stack renders `env` only; the prompt-capture hook leaves the telemetry stack** — `components/agents/claude-code/settings.template.json` was split into `otel.template.json` (`env`) and `hook.template.json` (`hooks`). Per the SPEC stack split, the audit/capture hook belongs to the separate **`claude-code-elastic-audit`** stack (its own initiative below), **not** the telemetry stack — so the telemetry stacks render only `env`. Add `components/agents/claude-code/scripts/render-otel.{sh,ps1}`; delete `render-settings.{sh,ps1}`. (`render-hook` + `hook.template.json` are consumed by the audit stack, not here.)
  - `render-otel` does a **JSON key-merge** of `env` into `<target>/.claude/settings.local.json` (create the file if absent; **add `env` only if absent** — never clobber existing; drop the template's `_comment`). It substitutes `@@OTLP_LOGS_ENDPOINT@@` / `@@OTLP_TRACES_ENDPOINT@@` / `@@OTLP_METRICS_ENDPOINT@@` + `@@OTLP_HEADERS@@` with **no path construction** (the `/v1/<signal>` path is the backend's, supplied by the caller). `jq` is fine here (render is setup tooling, not the fleet hook).
  - **Stack wiring:** `stacks/claude-code-elastic/scripts/setup.{sh,ps1}` and `stacks/claude-code-otelcol-elastic/scripts/setup.{sh,ps1}` currently call `render-settings <base-endpoint> <target>` (which also rendered the prompts-audit hook). Change them to construct the three full per-signal endpoints from the backend's OTLP base (direct APM `http://localhost:8200/v1/{logs,traces,metrics}`; the otelcol collector's receiver) and call `render-otel` only. **Remove the prompt-capture hook registration from the telemetry stacks** — audit moves to `claude-code-elastic-audit`.

  **Verify:** a fresh `claude-code-elastic` setup writes `settings.local.json` with **`env` only** (three per-signal endpoints; **no `hooks` block**); re-running is a no-op; a `claude` session lands logs/traces/metrics; `shfmt -d` empty + `shellcheck` clean on the new `.sh`, `Invoke-ScriptAnalyzer` clean on the new `.ps1`.
- [x] **Add `render-mcp.{sh,ps1}` to materialize `<target>/.mcp.json` from `mcp.template.json`** — `components/agents/claude-code/mcp.template.json` defines the project-scoped Elasticsearch MCP server. Add `components/agents/claude-code/scripts/render-mcp.{sh,ps1}` that writes `mcp.template.json` (dropping `_comment`) to `<target>/.mcp.json` (create-if-absent — leave an existing `.mcp.json` untouched), and call it from the `claude-code-elastic` / `claude-code-otelcol-elastic` `setup.{sh,ps1}`. No placeholder substitution is needed (the template is self-contained). **No settings auto-approve key** — the project-scoped server stays interactive-approval (a user decision). **Verify:** a fresh setup writes `<target>/.mcp.json` with the `elasticsearch` server; launching `claude` from the stack dir lists it as pending approval; after approval the Elasticsearch MCP tools work; `shfmt`/`shellcheck`/`Invoke-ScriptAnalyzer` clean on the new scripts.
- [x] **Remove `agent_audit.agent.model` from the agent_audit schema entirely** — model is an LLM-request concern, not a prompt/tool-call audit concern, and Claude's `UserPromptSubmit` / `PostToolUse` hook payloads don't carry it anyway (doc-confirmed: a `model` field reaches only `SessionStart` hooks). Drop it everywhere. `SPEC/agent-audit.md` is already updated (model removed from both canonical shapes and the tool_call mapping note) — do **not** edit SPEC.
  - **Strict templates** — `components/backends/elastic-audit/elasticsearch/agent-audit.user_prompt.template.json` and `agent-audit.tool_call.template.json`: remove the `model` keyword property under `agent_audit.agent`.
  - **Kibana** — `components/backends/elastic-audit/kibana/agent-audit.ndjson`: remove `agent_audit.agent.model` from the `columns` of both saved searches (**Agent Audit — User Prompts** and **Agent Audit — Tool Calls**).
  - **Codex hooks** — `components/agents/codex-cli/hooks/capture-user-prompt.{sh,ps1}` and `capture-tool-call.{sh,ps1}`: drop the `model` lift and the `"model":%s` field (and its argument) from the assembled record; refresh the field-mapping header comments.
  - **Re-provision** — the strict mapping changes, so re-run `setup-agent-audit.{sh,ps1}` and recreate the `logs-agent_audit.{user_prompt,tool_call}-default` data streams (pre-change docs keep the old field — operator step).

  **Verify:** a `codex-cli-elastic-audit` session lands `user_prompt` + `tool_call` docs with **no `agent_audit.agent.model`**; both Agent Audit saved searches open without the model column; `shfmt -d` empty + `shellcheck` clean on touched `.sh`, `Invoke-ScriptAnalyzer` clean on touched `.ps1`.
- [x] **(claude-code-elastic-audit 1/3) Rewrite `capture-prompt.{sh,ps1}` to emit the canonical `agent_audit.user_prompt` document, and add `render-agent-audit.{sh,ps1}`** — mirror the Codex `capture-user-prompt` + `render-agent-audit` pair (`SPEC/agent-audit.md`). The hook takes its delivery config via **`--config <abs path>`** (injected, never discovered — threat-model requirement), reads `components/agents/claude-code/agent-audit.template.conf`'s rendered `.claude/agent-audit.conf` with **zero external deps** (POSIX pure-`read` loop / PowerShell `ConvertFrom-StringData` — no `jq`, no TOML parser), and uses only the `user_prompt` stream keys (`capture.user_prompt.{enabled,content}`, `elasticsearch.{url,api_key,timeout_ms,data_stream.user_prompt}`). From the Claude `UserPromptSubmit` stdin (`{session_id, …, prompt}`) it builds the canonical doc and POSTs to the conf's `data_stream`, fail-open (no stdout, always exit 0, short timeout). **Claude specifics:** `agent.provider="anthropic"`, `agent.name="claude-code"`; `conversation_id ← session_id`; **`turn_id` = null** (not in the payload); `user.id`/`user.name` ← `whoami`/login, `host.*` ← hostname (same as Codex); provider account/org read **locally from `~/.claude.json`'s `oauthAccount`** (plain JSON, no JWT): `account.id ← accountUuid`, `account.name ← displayName`, `account.email ← emailAddress`, `organization.id ← organizationUuid`, `organization.name ← organizationName`; `content` modes `plaintext`/`redacted`/`encrypted` populate `user_prompt.text` (true `.length` always). `render-agent-audit.{sh,ps1}` renders `agent-audit.template.conf` → `<target>/.claude/agent-audit.conf`, substituting `@@ES_URL@@` (create-if-absent). SPEC already updated — do not edit SPEC. **Verify:** feeding a synthetic `UserPromptSubmit` payload to `capture-prompt.sh --config <conf>` builds a canonical doc and indexes it to `logs-agent_audit.user_prompt-default`; `grep -ri 'jq' ` the hooks finds nothing; `shfmt`/`shellcheck` clean on `.sh`, `Invoke-ScriptAnalyzer` clean on `.ps1`.
- [x] **(claude-code-elastic-audit 2/3) Add `render-hook.{sh,ps1}` that registers the capture hook in the audit stack's `settings.local.json` with an injected `--config`** — `components/agents/claude-code/scripts/render-hook.{sh,ps1}`: **JSON key-merge** the `hooks` block from `hook.template.json` into `<target>/.claude/settings.local.json` (add `hooks` only if absent; drop `_comment`; `jq` ok — setup tooling). Substitute `@@HOOK_COMMAND@@` with **this platform's** capture script absolute path **plus `--config <abs>/.claude/agent-audit.conf`** (the config path is injected into the hook command, per `SPEC/agent-audit.md` "Delivery and authorization"; per-OS 出し分け: render-hook.sh → `capture-prompt.sh`, render-hook.ps1 → `capture-prompt.ps1`). **Verify:** `render-hook` writes a `hooks.UserPromptSubmit` command of the form `<abs capture-prompt.(sh|ps1)> --config <abs agent-audit.conf>`; re-run is a no-op; `shfmt`/`shellcheck`/`Invoke-ScriptAnalyzer` clean.
- [x] **(claude-code-elastic-audit 3/3) Add the `stacks/claude-code-elastic-audit/` stack** — mirror `codex-cli-elastic-audit`, reusing the `elastic-audit` backend wholesale (`SPEC/agent-audit.md` "Where this runs"). `docker-compose.yml`: `name: claude-code-elastic-audit`, `include ../../components/backends/elastic-audit/docker-compose.yml` (no APM Server). `scripts/setup.{sh,ps1}` runs, in order: `setup-agent-audit` (provision the `logs-agent_audit.*` streams + strict templates) → `render-hook` (audit `settings.local.json`) → `render-agent-audit` (`.claude/agent-audit.conf` with this stack's ES base) → `render-mcp` (`.mcp.json`, ES MCP) → the elastic-audit `import-kibana-objects` (the Agent Audit Kibana). **No `render-otel`** (no telemetry in the audit stack). `scripts/verify-agent-audit.{sh,ps1}` feeds a synthetic `UserPromptSubmit` through `capture-prompt` and asserts a doc lands in `logs-agent_audit.user_prompt-default`. Add a Quick-Tour `README.md` and gitignore the `.claude` agent home. Depends on tasks 1/3 + 2/3 and the model-removal task (streams are model-free). **Verify:** `docker compose config -q` passes; `setup` brings the audit backend healthy and provisions the stream; a `claude` session launched from the stack lands `agent_audit.user_prompt` docs; `verify-agent-audit` reaches PASS; the compose project has **no APM Server**; `shfmt`/`shellcheck`/`Invoke-ScriptAnalyzer` clean.
- [x] **Fix `render-hook.ps1` to emit an exec-form PowerShell hook (no shell, `-NoProfile`, Windows PowerShell 5.1)** — the audit hook silently delivers nothing on Windows: Claude runs a `command` hook via **Git Bash by default when present**, which mangles the backslash path and can't execute a `.ps1` → `command not found` / exit 127 → fail-open no-op (confirmed live: `capture-prompt.sh` + the same conf indexes HTTP 201; the rendered `.ps1` via `sh -c` fails 127). **Fix with the exec form** (spawns the executable directly — no shell, so **Git-Bash-independent**, keeping the employee-fleet plan intact): render the `UserPromptSubmit` hook as `command: "powershell"`, `args: ["-NoProfile", "-NonInteractive", "-ExecutionPolicy", "Bypass", "-File", "<abs capture-prompt.ps1>", "--config", "<abs agent-audit.conf>"]` (no `shell` field — ignored when `args` is set). This pins **Windows PowerShell 5.1** (`powershell.exe`, on every Windows box — no `pwsh`/PS7), suppresses **profile loading** (`-NoProfile`), and runs under any **execution policy** (`-ExecutionPolicy Bypass`, for locked-down machines). Verified live: `powershell.exe` 5.1.26100 with these flags + the payload on stdin indexes a canonical doc. `render-hook.sh` is unchanged (its `.sh` runs under the default shell on macOS/Linux). Also extend `verify-agent-audit` to exercise the **rendered hook exactly as Claude spawns it** (not `capture-prompt` directly) so this class of gap is caught. **Verify:** a fresh audit-stack setup renders the exec-form hook; a real `claude` prompt lands an `agent_audit.user_prompt` doc in `logs-agent_audit.user_prompt-default`; `Invoke-ScriptAnalyzer` clean.
- [x] **Ensure both agents' fleet-distributed capture hooks run on Windows PowerShell 5.1 (no PS7 dependency)** — the `capture-*.ps1` hooks ship to every employee workstation, where only **Windows PowerShell 5.1** (`powershell.exe`) is guaranteed (`pwsh`/PS7 may be absent). Two parts:
  - **Invocation** — spawn the hook via `powershell.exe` (not `pwsh`) with `-NoProfile -NonInteractive -ExecutionPolicy Bypass`. Claude: the exec-form `render-hook.ps1` above. **Codex:** check how its `commandWindows` / inline `[hooks]` runs on Windows and apply the same (`powershell.exe` + flags, not `pwsh`).
  - **Script compatibility** — audit `components/agents/claude-code/hooks/capture-prompt.ps1` and `components/agents/codex-cli/hooks/capture-user-prompt.ps1` / `capture-tool-call.ps1` for PS7-only constructs (ternary `? :`, `??` / `??=`, `&&` / `||` pipeline chains, `ConvertFrom-Json -AsHashtable`, `Get-Content -AsByteStream`, …) and make them 5.1-clean. The `#!/usr/bin/env pwsh` shebang is cosmetic under exec-form invocation but shouldn't imply a PS7 requirement. (Claude's `capture-prompt.ps1` is already verified under 5.1.26100; Codex's two are unverified.)

  **Verify:** each `capture-*.ps1`, run under `powershell.exe -NoProfile -File … --config …` with a synthetic payload, indexes its canonical doc; no `pwsh`-only syntax remains; `Invoke-ScriptAnalyzer` clean.

### Propagate the `codex-cli-elastic` setup conventions to the other stacks

`codex-cli-elastic` is now the canonical pattern for a stack's setup surface — read `stacks/codex-cli-elastic/{docker-compose.yml,setup.conf,scripts/setup.sh,scripts/setup.ps1,README.md}` and bring the other four stacks in line with it. Touch only each stack's `docker-compose.yml`, `scripts/setup.{sh,ps1}`, `README.md`, and a new `setup.conf`; **leave the smoke-test / verify scripts unchanged.** Shared rules for every task below:

- **Endpoints → `setup.conf`.** Create `setup.conf` at the stack root holding only that stack's real endpoints as `dotted.key=value`, with a single one-line *what* comment. `setup.{sh,ps1}` read it (positional `$1` / `-Config`, else discover `<stack>/setup.conf`) and fail fast (exit 2) on a missing file or empty key. Bridge each value to the contract the shared component scripts already expect — do **not** change any component script's interface, and do **not** invent a key a stack doesn't use. (sh: `export ES_URL`/`KIBANA_URL`, pass other endpoints as the positional args the component already takes; ps1: pass named params.)
- **Lean.** Strip the prose headers from `setup.{sh,ps1}` and `docker-compose.yml`; keep only non-obvious *why*. Add **no** new comments beyond what `codex-cli-elastic` keeps.
- **Naming.** sh locals lowercase, `components_dir` (not `C`); `ES_URL`/`KIBANA_URL` stay uppercase and exported (the env contract). ps1 stays PascalCase with `$ComponentsDir`.
- **ps1 inline.** No step-runner helper (no `Invoke-Step`); inline each step as `&` + named args and rely on `$ErrorActionPreference = 'Stop'` for failure propagation.
- **Readable output.** A blank line before each step heading (not before the first); indent each component's stdout one level — sh `indent() { sed 's/^/  /'; }` + `| indent`; ps1 `filter Indent { "  $_" }` + `6>&1 | Indent`.
- **README.** Mirror `codex-cli-elastic`'s Quick Tour (edit `setup.conf`, then run setup — drop the env-var-override prose) and Layout (add the `setup.conf` line). Keep it slim.
- **Match `codex-cli-elastic`'s file structure and ordering** wherever the stack's own steps allow.
- **Gates (every task):** `docker compose config -q`; `shfmt`/`shellcheck` clean on `.sh`; `Invoke-Formatter` (repo `PSScriptAnalyzerSettings.psd1`) applied and `Invoke-ScriptAnalyzer` clean on `.ps1`. The four tasks are independent of each other.

- [x] **(setup propagation 1/4) `claude-code-elastic`** — 5-step direct/APM path. `setup.conf` keys: `elasticsearch.url`, `kibana.url`, `apm_server.otlp_endpoint` (default `http://localhost:8200`). Keep the existing call shape: `render-otel.sh <stack_dir> <otlp>/v1/logs <otlp>/v1/traces <otlp>/v1/metrics` (build the three `/v1/*` URLs from the base) and `import-kibana-objects … claude-code`. **Verify:** gates pass; `setup.{sh,ps1}` with no arg discovers `setup.conf` and reproduces today's behavior; an empty key or missing file exits 2; `scripts/smoke-test.sh` still PASSES.
- [x] **(setup propagation 2/4) `claude-code-otelcol-elastic`** — 5-step Collector path. `setup.conf` keys: `elasticsearch.url`, `kibana.url`, and the OTLP endpoint that targets the **Collector** (`http://localhost:4318`) — name the key for the Collector (e.g. `collector.otlp_endpoint`), not `apm_server.*`. Keep `import-kibana-objects … claude-code otelcol-sidecar` and build the three `/v1/*` URLs from the base as today. **Verify:** gates pass; no-arg discovery reproduces current behavior; fail-fast on empty/missing key; `scripts/smoke-test.sh` and `scripts/resilience-test.sh` still PASS.
- [x] **(setup propagation 3/4) `claude-code-elastic-audit`** — 5-step audit path (no OTLP). `setup.conf` keys: `elasticsearch.url`, `kibana.url` only. Keep the step order: `setup-agent-audit` → `render-hook` → `render-agent-audit` (takes the ES base as a positional arg) → `render-mcp` → elastic-audit `import-kibana-objects`. **Verify:** gates pass; no-arg discovery reproduces current behavior; fail-fast on empty/missing key; `scripts/verify-agent-audit.{sh,ps1}` still reaches PASS.
- [x] **(setup propagation 4/4) `codex-cli-elastic-audit`** — 4-step audit path (no OTLP). `setup.conf` keys: `elasticsearch.url`, `kibana.url` only. Keep the step order: `setup-agent-audit` → `render-agent-audit` → `render-hooks` + `render-mcp` → elastic-audit `import-kibana-objects`. **Verify:** gates pass; no-arg discovery reproduces current behavior; fail-fast on empty/missing key; `scripts/verify-agent-audit.sh` and `scripts/verify-tool-call-audit.sh` still PASS.

- **Backends decomposition refactor (re-org into service fragments).** Migrate to the target shape fixed in `SPEC/SPEC.md` ("Repository shape / Component responsibilities / Placement rule / Invariants"): *services own the asset libraries and the generic appliers; a backend holds only an `include:` list plus abstract composition scripts and owns no asset files of its own.* Each phase must leave the tree green on its own. Phases 2 and 3 depend on phase 1 (the fragments existing) but are independent of each other. **Do NOT edit `SPEC/`.**
- **Gates (every task):** `docker compose config -q` on every stack; `shfmt`/`shellcheck` clean on `.sh`; `Invoke-Formatter` (repo `PSScriptAnalyzerSettings.psd1`) applied and `Invoke-ScriptAnalyzer` clean on `.ps1`. From a fresh `docker compose down -v && up -d`, every stack's Kibana import must succeed and `smoke-test.sh` / `verify-*` must reach PASS.

- [x] **(backends decomposition 1/4) Extract the service fragments.** Add `components/backends/services/{elasticsearch,kibana,apm-server}/docker-compose.yml`, moving each service definition, its shared config (`elasticsearch.yml` / `kibana.yml` / `apm-server.yml` — today byte-identical apart from a header comment, so collapse to one copy each), and the `elasticsearch-data` volume into the fragment. Fragments carry no `name:`. Rewrite `backends/elastic/docker-compose.yml` to `include: ../services/{elasticsearch,kibana,apm-server}/docker-compose.yml` and `backends/elastic-audit/docker-compose.yml` to `include: ../services/{elasticsearch,kibana}/docker-compose.yml`. **This phase moves compose structure only — no assets or scripts move yet.** **Verify:** `docker compose config -q` green for every stack; `up -d` brings every service to healthy; all existing smoke/verify still PASS.
- [x] **(backends decomposition 2/4) Consolidate Kibana assets and the importer into `services/kibana/`.** Move `backends/elastic/kibana/{claude-code,codex-cli,otelcol-sidecar}/` and `backends/elastic-audit/kibana/agent-audit.ndjson` into `components/backends/services/kibana/<source>/` (agent-audit becomes one namespaced source). Move the generic `import-kibana-objects.{sh,ps1}` (the `elastic` variant that takes source namespaces as arguments and imports data-views → saved-searches → dashboard in order) into `services/kibana/`, **delete the diverged fixed-file `elastic-audit` variant**, and express agent-audit through the generic importer as `import-kibana-objects agent-audit`. Update every caller (stack setup, backend composition) to the relocated path. **Verify:** full Kibana import succeeds for every stack; saved searches open in Discover; smoke/verify PASS.
- [x] **(backends decomposition 3/4) Consolidate ES assets into `services/elasticsearch/` and replace the per-backend setup scripts with per-component-type composition.** Move `backends/elastic/elasticsearch/{logs-drop.pipeline,trace-routing.pipeline,prompts-audit.index}.json` and `backends/elastic-audit/elasticsearch/agent-audit.*.template.json` into the `services/elasticsearch/` asset library. Provide generic appliers that PUT each ES component type into the running cluster, **split by type** — e.g. `import-ingest-pipelines` (ingest pipelines) and `import-index-templates` (index / component templates) — taking the asset names to apply as arguments. Replace the per-backend `setup-logs-drop` / `setup-trace-routing` / `setup-prompt-audit` (elastic) and `setup-agent-audit` (elastic-audit) scripts with composition that selects the assets intrinsic to each backend and applies them through those appliers; the composition may be one script or several per component type. The backend owns no asset files; the appliers carry no per-backend selection (the caller passes it in). Update each stack's setup to invoke the new scripts. **Verify:** from a fresh start, pipelines / templates / data streams are applied as before; smoke/verify confirm telemetry/audit data land and reach PASS.
- [x] **(backends decomposition 4/4) Full-matrix verification and stack README alignment.** Bring up, import, and run smoke/verify for all five stacks (`claude-code-elastic`, `claude-code-otelcol-elastic`, `claude-code-elastic-audit`, `codex-cli-elastic`, `codex-cli-elastic-audit`) from a fresh `down -v && up -d`, and confirm no asset files remain under any `backends/<backend>/` (only `docker-compose.yml` and composition scripts). Update each stack's `README.md` import steps and Layout to the relocated paths (user-facing READMEs only; do not touch `SPEC/`). **Verify:** all gates green; smoke/verify PASS for all five stacks.

Add Claude Code tool-call audit capture (`PostToolUse` → `logs-agent_audit.tool_call-default`) — the increment deferred in `stacks/claude-code-elastic-audit/README.md`. The `elastic-audit` backend already provisions this strict stream and surfaces it via the cross-agent **Agent Audit — Tool Calls** Kibana objects (Codex uses them), so **no backend change**; the work is in the Claude Code agent component plus the stack's verify/README. `SPEC/agent-audit.md` is already updated (tool-call section made agent-generic; Claude's `PostToolUse` carries `turn_id`/`tool_use_id`, unlike its turn-less `UserPromptSubmit`) — do **not** edit SPEC. Three tasks:

- [x] **(claude tool-call 1/3) Add `components/agents/claude-code/hooks/capture-tool-call.{sh,ps1}`** — mirror `components/agents/codex-cli/hooks/capture-tool-call.{sh,ps1}`, but take the **Claude identity derivation from `capture-user-prompt.{sh,ps1}`** (provider `anthropic`/`claude-code`; account/org from `~/.claude.json`'s `oauthAccount` — plain JSON, so drop Codex's `auth.json`/JWT/`base64`/`AWK_ORG` block). `STREAM=tool_call`, reads only the `tool_call` conf keys, `--config` injected, zero external deps, fail-open. Mapping per SPEC: `conversation_id ← session_id`, `turn_id ← turn_id`, `tool.name ← tool_name`, `tool.call_id ← tool_use_id`, `input ← tool_input` / `output ← tool_response` serialized to a JSON string into `.text` (true `.length` kept); `cwd`/`transcript_path`/`permission_mode` dropped.
- [x] **(claude tool-call 2/3) Register the `PostToolUse` hook** — add a `PostToolUse` block to `components/agents/claude-code/hook.template.json` and rework `render-hook.{sh,ps1}` to render **both** events (`UserPromptSubmit` → `capture-user-prompt`, `PostToolUse` → `capture-tool-call`), each with the injected `--config`; the `.ps1` exec-form (`powershell` + args) applies to both. Add `capture.tool_call.enabled=true` / `capture.tool_call.content=plaintext` / `elasticsearch.data_stream.tool_call=logs-agent_audit.tool_call-default` to `agent-audit.template.conf`. Both renderers stay create-if-absent (a stale local `.claude/` must be regenerated to pick up the new keys/hook).
- [x] **(claude tool-call 3/3) Add `stacks/claude-code-elastic-audit/scripts/verify-tool-call-audit.{sh,ps1}` and update the stack `README.md`** — mirror `stacks/codex-cli-elastic-audit/scripts/verify-tool-call-audit.{sh,ps1}`, driving the **rendered** hook (the Claude registration check is `jq .hooks.PostToolUse` on `settings.local.json`, not a `config.toml` grep) with a synthetic `PostToolUse` payload (object `tool_input`, string `tool_response`, plus the dropped fields). In the README move "Tool-call capture for Claude Code" from **What's deferred** into the wired set, correct the diagram/intro/"Why a separate stack" to `UserPromptSubmit` + `PostToolUse`, update the Data streams bullet, and list the new script in Quick Tour + Layout.
- [x] **Force-redact Claude Code prompt text at ingest, regardless of the client `OTEL_LOG_USER_PROMPTS` gate** — overwrite `labels.prompt` (events `claude_code.user_prompt`) and `labels.user_prompt` (the `claude_code.interaction` trace root) to `<REDACTED>` in the per-agent APM `@custom` sub-pipelines, so prompt text never lands even if a client sets the gate to `1` (this also masks slash-command text like `/exit`, which the agent never redacts). Mirror Codex's `components/backends/services/elasticsearch/codex-cli/logs-apm.app@custom-codex_cli_rs.pipeline.json` (its `set … labels.prompt … "override": true … "if": "ctx.labels?.prompt != null"`). Two changes under `components/backends/services/elasticsearch/claude-code/`: (1) **add** `logs-apm.app@custom-claude-code.pipeline.json` setting `labels.prompt` → `<REDACTED>` (override, if present); (2) **add** the same `set labels.user_prompt` → `<REDACTED>` processor to the existing `traces-apm@custom-claude-code.pipeline.json` (keep its `reroute`). Use the marker **`<REDACTED>`** (angle brackets — matches Claude Code's own client redaction, so ingest- and client-redaction are indistinguishable; deliberately NOT Codex's `[REDACTED]`). The shared `logs-apm.app@custom` / `traces-apm@custom` routers already dispatch on `service.name: claude-code`, and `import-elasticsearch-assets.sh` auto-applies every `*.pipeline.json` in the concern dir, so no wiring change — it takes effect wherever the `claude-code` ES concern is imported (`claude-code-elastic`, `claude-code-otelcol-elastic`). `prompt_length` / `user_prompt_length` are left intact. `SPEC/claude-code-telemetry.md` already updated — do **not** edit SPEC.
- [x] **Drop Claude Code raw API-body events at ingest (`OTEL_LOG_RAW_API_BODIES`)** — `claude_code.api_request_body` and `claude_code.api_response_body` (events stream) expose the **entire conversation** in `labels.body` — full `messages[]`: system prompt, CLAUDE.md, memory, every prior turn, tool I/O, and the prompt verbatim (regardless of `OTEL_LOG_USER_PROMPTS`) — plus the full model response, bypassing the prompt-redaction control. In the per-agent **`logs-apm.app@custom-claude-code`** sub-pipeline (added by the prompt-redaction task above — **this task depends on it**), add a `drop` processor so both message types are discarded **before storage**, regardless of the client gate or mode (inline `labels.body` or `file:<dir>` `labels.body_ref`). Mirror the `drop` style in Codex's `components/backends/services/elasticsearch/codex-cli/logs-apm.app@custom-codex_cli_rs.pipeline.json` (`{ "drop": { "if": "…" } }`), but discriminate on the **top-level `message`** field these docs carry: `ctx.message == 'claude_code.api_request_body' || ctx.message == 'claude_code.api_response_body'`. No other event type is affected. `SPEC/claude-code-telemetry.md` already updated — do **not** edit SPEC.
- [x] **Field-drop Claude Code tool-argument fields at ingest (`OTEL_LOG_TOOL_DETAILS`)** — this gate adds, on the events stream, `labels.tool_input` (+ `labels.tool_parameters`) on `claude_code.tool_result` and `labels.tool_parameters` on `claude_code.tool_decision`, carrying **verbatim tool arguments** (Bash `command` / `full_command`, file paths, Write/Edit content, search queries — see SPEC). In the per-agent **`logs-apm.app@custom-claude-code`** sub-pipeline (added by the prompt-redaction task above — **this task depends on it**), `remove` **`labels.tool_input`** and **`labels.tool_parameters`** with `ignore_missing: true`, so these sensitive fields are *absent* — identical to the `OTEL_LOG_TOOL_DETAILS=0` shape, NOT a `<REDACTED>` marker (deliberately unlike the prompt redaction: removing the field reproduces the unset state, so a client cannot expose tool arguments by setting the gate). **Scope: only those two argument fields.** Leave everything else this gate touches in place — notably `labels.server_name` on `claude_code.mcp_server_connection` (just the MCP server label, not sensitive) — and the gate-independent `*_size_bytes` / `tool_name` / decision fields. `SPEC/claude-code-telemetry.md` already updated — do **not** edit SPEC.
- [x] **Drop Claude Code tool-output events at ingest (`OTEL_LOG_TOOL_CONTENT`)** — `message: tool.output` (a new events-stream document — it lands there even though it is a `claude_code.tool.execution` span event) carries **verbatim tool output**: `labels.output` (Bash stdout), `labels.content` (Read file contents), `labels.diff` + `labels.file_path` (Edit) — file contents, command output, and diffs, none of it covered by the other ingest controls. In the per-agent **`logs-apm.app@custom-claude-code`** sub-pipeline (added by the prompt-redaction task above — **this task depends on it**), `drop` the document on `ctx.message == 'tool.output'` (same whole-doc `drop` mechanism as the raw API-body task — fold it into that `drop`'s `if` or add a sibling processor), so tool output **never lands** regardless of the gate. Whole-doc drop (not field-remove) because the entire document is gate-added — matching the `OTEL_LOG_TOOL_CONTENT=0` state where `tool.output` documents don't exist. `SPEC/claude-code-telemetry.md` already updated — do **not** edit SPEC.

Adopt ILM as the lab's retention mechanism (replacing the audit streams' DSL), expressed as Elasticsearch text assets, with **four policies** so each concern ages independently (audit `user_prompt` / audit `tool_call` / `telemetry.claude_code` / `telemetry.codex_cli_rs`; all hot→delete@3d for now). Each policy + its component template is **co-located in the concern that consumes it** — there is no shared `lifecycle` concern and no backend façade change. `SPEC/SPEC.md` "Lifecycle (ILM)", `SPEC/agent-audit.md`, and the `claude-code-elastic-audit` / `codex-cli-elastic-audit` stack READMEs (3-day ILM retention) are already updated — do **not** edit SPEC or those READMEs. Two tasks, the second depends on the first:

- [x] **(ILM 1/2) Rework the agent-audit index templates into thin compositions: extract mappings to a `@mappings` component template and move retention from DSL to a per-stream ILM policy.** Under `components/backends/services/elasticsearch/agent-audit/`, for **each** stream (`user_prompt`, `tool_call`) add three assets following the suffix convention in `import-elasticsearch-assets.{sh,ps1}`: (a) an ILM policy `*.ilm.json` (basename = policy name, e.g. `logs-agent_audit.user_prompt`) → `PUT _ilm/policy/<name>`, `hot` (`rollover`) → `delete` at `min_age: 3d`; (b) a lifecycle component template `*.component.json` (e.g. `logs-agent_audit.user_prompt@lifecycle`) → `PUT _component_template/<name>` setting **only** `index.lifecycle.name`; (c) a mappings component template `*.component.json` (e.g. `logs-agent_audit.user_prompt@mappings`) holding the stream's existing **strict** `template.mappings` verbatim. Then rewrite `logs-agent_audit.user_prompt.template.json` / `logs-agent_audit.tool_call.template.json` into **thin compositions** — `"composed_of": ["<stream>@mappings", "<stream>@lifecycle"]`, keeping `index_patterns`, `priority`, `_meta`, `data_stream: {}`, and **removing** both the inline `template.mappings` and the `template.lifecycle.data_retention` block. Extend the importer with the two new suffix handlers (`*.ilm.json` → `_ilm/policy`, `*.component.json` → `_component_template`), applied **before** index templates within a concern (order: ilm → component template → pipelines → templates → indices), each PUT idempotent + acknowledged-checked like the existing handlers. Because the audit `*.template.json` handler also re-syncs the stream mapping each run and the mappings are no longer inline, **rework that mapping-sync to apply the *resolved* composed mapping** (e.g. `POST _index_template/_simulate_index_template/<stream>` → `.template.mappings`) instead of reading `.template.mappings` from the IT file. **No façade change** — the `agent-audit` concern is already applied by the `elastic-audit` backend, and the new handlers run within it before the templates. Separate policies so `user_prompt` and `tool_call` can diverge later; both hot→delete@3d now. Load-bearing correctness: the two streams resolve the **same** strict mappings as before **and** their own `index.lifecycle.name` (no `data_retention`); a stray field is still rejected; an added field still re-syncs onto the live stream; a new/rolled backing index is ILM-managed by the matching policy; setup stays idempotent.
- [x] **(ILM 2/2) Attach a per-agent ILM policy to each agent's telemetry data streams via dedicated per-agent index templates.** Depends on ILM 1/2 (reuses its new `*.ilm.json` / `*.component.json` importer handlers). One telemetry policy **per agent**, covering that agent's logs+metrics+traces. Add a policy pair under each agent concern: `elasticsearch/claude-code/` → policy `telemetry.claude_code` + component template `telemetry.claude_code@lifecycle`; `elasticsearch/codex-cli/` → `telemetry.codex_cli_rs` + `telemetry.codex_cli_rs@lifecycle` (each CT sets only `index.lifecycle.name`). Then, because APM's shared per-signal templates (`logs-apm.app-*`, `metrics-apm.app-*`, `traces-apm-*`) prevent per-agent attachment via `@custom`, add **dedicated higher-priority compose-only index-template assets** in each agent concern — claude-code: `logs-apm.app.claude_code-*`, `metrics-apm.app.claude_code-*`, `traces-apm-agents_claude_code*`; codex-cli: the `codex_cli_rs` equivalents. Each: the per-agent `index_patterns`, a `priority` above APM's matching template, `data_stream: {}`, and `composed_of` = **APM's own managed component templates** (read live from `GET _index_template/<apm-template>` so its mappings/settings inherit verbatim) **plus** that agent's `…@lifecycle` component template **last** (so `index.lifecycle.name` wins). These carry **no** `template.mappings` and must **not** create a data stream or run a mapping-sync — the existing `*.template.json` handler does both, so add a distinct **PUT-only** handler/suffix (e.g. `*.index-template.json` → `PUT _index_template/<name>`, nothing else). No façade change (agent concerns are already applied per-stack). Load-bearing correctness: each per-agent pattern resolves **both** APM's mappings **and** that agent's lifecycle policy, while a non-agent service still resolves APM's default template with its original lifecycle — ILM lands on the agent streams only, never all of APM.

Refactor the agent-audit capture hooks into **humble entry scripts over an extracted per-agent library**, with **zero narrative comments** (tool directives like `# shellcheck` are fine). The entry reads as a named procedure; all logic moves to `hooks/lib/`; the explanation now lives in `SPEC/agent-audit.md` "Hook implementation contract" (already added — do **not** edit SPEC). Behaviour must be byte-identical: the canonical document, fail-open semantics, stdout-silence, and short-timeout delivery are unchanged, and the existing `verify-agent-audit` / `verify-tool-call-audit` scripts are the regression net. Two tasks, one per agent (Claude first as the canonical; Codex mirrors it):

- [x] **(humble hooks 1/2) Claude Code.** Extract all logic from `components/agents/claude-code/hooks/capture-user-prompt.{sh,ps1}` and `capture-tool-call.{sh,ps1}` into a per-agent library beside them (`hooks/lib/agent-audit.sh` + `hooks/lib/agent-audit.ps1`), sourced at runtime (the hook command runs the entry in place, so the sibling lib ships with it — **no `render-hook` change**). Each entry becomes a **comment-free humble script**: shebang, source the lib, a single `stream=user_prompt|tool_call` line, then a top-to-bottom sequence of verb-named steps (`require_config_arg`, `require_tools`, `load_delivery_config`, `require_stream_enabled`, `read_hook_payload`, `build_audit_document`, `deliver_document`; PowerShell `Assert-*` / `Import-*` / `Read-*` / `Build-*` / `Send-*`) — the two stream entries differ only by the `stream=` line. The library carries config-read, the named JSON helpers (`json::string_field` / `json::raw_value` / `json::escape` / `json::char_count` on sh; `ConvertFrom-Json` / `ConvertTo-Json` on ps1), Claude's `oauthAccount` identity extractor, the `stream`-dispatched document assembly, delivery, and the fail-open harness (guards short-circuit to a logged exit-0 skip). Preserve the invariants exactly: nothing to stdout, always exit 0, short-timeout POST, zero external deps. Load-bearing: `stacks/claude-code-elastic-audit/scripts/verify-agent-audit.{sh,ps1}` and `verify-tool-call-audit.{sh,ps1}` reach PASS with documents **byte-identical** to pre-refactor; `shfmt` / `shellcheck` and `Invoke-ScriptAnalyzer` (repo `PSScriptAnalyzerSettings.psd1`) clean.
- [x] **(humble hooks 2/2) Codex CLI.** Mirror Task 1 on `components/agents/codex-cli/hooks/capture-user-prompt.{sh,ps1}` and `capture-tool-call.{sh,ps1}` — same humble-entry shape, same comment-free rule, same `hooks/lib/` structure and step names. The only agent-specific difference is the identity extractor (Codex decodes its `auth.json` **JWT**, vs Claude's plain-JSON `oauthAccount`) and Codex's field paths; everything else (config read, JSON helpers, delivery, fail-open harness) matches Task 1's shape. Keep the per-agent library independent — the duplicated core across the two agents is accepted for now (see SPEC). Load-bearing: `stacks/codex-cli-elastic-audit/scripts/verify-agent-audit.{sh,ps1}` and `verify-tool-call-audit.{sh,ps1}` reach PASS with byte-identical documents; gates clean.

Commonize the agent-audit capture hooks **across agents and streams** — collapse the per-agent duplication into a **shared core + thin per-agent adapter**, and **unify each agent's two stream entries into a single `agent-audit.{sh,ps1}`** dispatched by `--stream` (8 entry files → 4). The only real variation left is the identity extractor, the provider constants, and one payload quirk. `SPEC/agent-audit.md` "Hook implementation contract" is already updated (one entry per agent + `--stream`; shared core/adapter under `components/agents/shared/agent-audit/`; three-way dispatch; full variable names; timeout constant; render points both hook events at the single entry) — do **not** edit SPEC. Behaviour stays byte-identical; the `verify-*` scripts are the regression net. Two tasks, split by shell:

- [x] **(hooks commonize 1/2) POSIX — shared core, per-agent adapter, single `--stream` entry.** Collapse the two near-identical `hooks/lib/agent-audit.sh` libraries (`components/agents/claude-code`, `codex-cli`) into a **single shared core** at `components/agents/shared/agent-audit/lib/agent-audit-core.sh` plus a **thin per-agent adapter** at each `components/agents/<agent>/hooks/lib/adapter.sh`. The core holds everything generic (config read, the `json::*` awk helpers, `jv_esc`/`jv_raw`, `load_delivery_config`, `require_*`, `read_hook_payload`, document assembly, `deliver_document`, the fail-open harness); the adapter supplies only the variation — `provider` / `agent_name` constants, `derive_runtime_identity` (Claude `oauthAccount` plain JSON vs Codex `auth.json` JWT + `AWK_ORG`), and the one payload quirk (Claude `user_prompt` has no `turn_id`). **Replace each agent's two `capture-<stream>.sh` files with one `components/agents/<agent>/hooks/agent-audit.sh`** that sources core + its adapter (paths relative to `$0`) and selects the stream from **`--stream <user_prompt|tool_call>`** (parsed alongside `--config`). **Update `render-hook.sh` / `render-hooks.sh`** so both hook events point at that single entry, differing only by the injected `--stream` (`UserPromptSubmit` → `user_prompt`, `PostToolUse` → `tool_call`); `hook.template.json`'s two-event structure is unchanged. Fold in the agreed polish, applied once in the unified core: (a) **parse `--stream` and pass it explicitly** to the steps that use it, with an explicit `require_stream` validating the value (no `SC2034` stream-global to disable); (b) **three-way dispatch** `user_prompt` / `tool_call` / else → logged fail-open skip (in `read_hook_payload` and `build_audit_document`), not a binary fallthrough; (c) **full variable names** (`escaped_prompt`, `account_id`, `prompt_length`, `input_raw`…; only loop vars stay terse); (d) **UPPER-case only for env-overridable globals** (`CLAUDE_CONFIG`, `CODEX_HOME`, the awk env vars), lowercase for internal globals; (e) keep the **`timeout_ms` fallback** (fail-open) but make its default a **single named constant synced to the shipped `agent-audit.conf` value (1000)**, not the stale `300`; (f) remove the lab-only **`(run setup.sh)`** hint, neutralize the `rendered hook command` wording, and put the stream in the log tag (`[agent-audit user_prompt]`). `SPEC/agent-audit.md` already updated — do **not** edit SPEC. Load-bearing: `verify-agent-audit.sh` + `verify-tool-call-audit.sh` for **both** `*-elastic-audit` stacks reach PASS with **byte-identical** documents; `shfmt` / `shellcheck` clean.
- [x] **(hooks commonize 2/2) PowerShell — shared core, per-agent adapter, single `-Stream` entry.** Mirror Task 1 on the `.ps1` side: a single shared core `components/agents/shared/agent-audit/lib/agent-audit-core.ps1` + per-agent `components/agents/<agent>/hooks/lib/adapter.ps1`, and **one `components/agents/<agent>/hooks/agent-audit.ps1`** (replacing the two `capture-<stream>.ps1`) dot-sourcing core + adapter and taking **`-Stream <user_prompt|tool_call>`** alongside `-Config`. **Update `render-hook.ps1` / `render-hooks.ps1`** (exec-form) so both events invoke the single entry with the matching `-Stream`. Same split (core = generic, incl. the `ConvertFrom-Json` / `ConvertTo-Json` document assembly; adapter = `provider` / `agent_name`, the runtime-identity function, payload quirk) and the same polish (a)–(f) as Task 1 in PowerShell idiom (Verb-Noun, `$Stream` passed to the steps, full names). `SPEC/agent-audit.md` already updated — do **not** edit SPEC. Load-bearing: `verify-agent-audit.ps1` + `verify-tool-call-audit.ps1` for both `*-elastic-audit` stacks reach PASS with byte-identical documents; `Invoke-Formatter` (repo `PSScriptAnalyzerSettings.psd1`) applied and `Invoke-ScriptAnalyzer` clean.

- [x] **(hooks polish) Tidy the commonized capture hooks — guard symmetry, de-leak the core, full naming, no PS shebang.** Refinements across both shells of the agent-audit hooks (`components/agents/shared/agent-audit/lib/agent-audit-core.{sh,ps1}`, each `components/agents/<agent>/hooks/agent-audit.{sh,ps1}` + `hooks/lib/adapter.{sh,ps1}`); **output stays byte-identical** (verify-* is the net). `SPEC/agent-audit.md` "Hook implementation contract" and `CONVENTIONS.md` are already updated — do **not** edit SPEC/CONVENTIONS.
  1. **Symmetric config guard.** sh: make `parse_args` parse-only (drop its `--config` presence check) and add a `require_config` guard parallel to `require_stream`; **move the config-file existence check** (`[ -f "$config_file" ]`) out of `load_delivery_config` into `require_config` (so the guard is "provided **and** exists"). ps1 already separates `Assert-Config` — move its `Test-Path` existence check from `Import-DeliveryConfig` into `Assert-Config` to match. Entry order both shells: `parse → require_stream → require_config → require_tools → load → …`; the loader keeps only the *contents* check (missing `elasticsearch.url` / `data_stream`).
  2. **Remove the `user_prompt_turn_id_supported` flag.** Delete it from both adapters (`adapter.sh` / `adapter.ps1`) and the gated branch in the core; in the `user_prompt` branch extract `turn_id` **unconditionally** like `tool_call` does (`json::string_field` / `NullIfEmpty` returns empty/`null` when absent → Claude still emits `turn_id: null`, byte-identical). This removes the last agent-specific knowledge from the core.
  3. **`org` → `organization`.** Rename `org_id` / `org_name` → `organization_id` / `organization_name` (sh) and `$orgId` / `$orgName` → `$organizationId` / `$organizationName` (ps1), for consistency with `account_*` and the full-names rule.
  4. **Variable-order sweep.** Ensure grouped initializations / assignments read in document order (account → organization, id → name → email) across both agents' adapters and the core (the `claude-code/hooks/lib/adapter.sh` identity-init line is already fixed — mirror it in `codex-cli` and the `.ps1` adapters).
  5. **Remove every PowerShell shebang.** Strip `#!/usr/bin/env pwsh` (and any `#!`-pwsh line) from **all** `.ps1` in the repo (hook entries, `render-*`, `setup-*`, `verify-*`, `ralph.ps1`, …) per the new CONVENTIONS rule.

  Load-bearing: `verify-agent-audit.{sh,ps1}` + `verify-tool-call-audit.{sh,ps1}` for **both** `*-elastic-audit` stacks reach PASS with byte-identical documents; `shfmt` / `shellcheck` and `Invoke-Formatter` (repo `PSScriptAnalyzerSettings.psd1`) + `Invoke-ScriptAnalyzer` clean.

- [x] **(tidy) Move claude-code's flat template files under `templates/` to match codex-cli.** `components/agents/claude-code/` keeps `agent-audit.template.conf`, `hook.template.json`, `mcp.template.json`, `otel.template.json` flat at the component root, while `codex-cli` groups them under `templates/` (and its render scripts already read `templates/…`). Create `components/agents/claude-code/templates/`, move the four files in, and update the path each render pair reads — `render-agent-audit.{sh,ps1}`, `render-hook.{sh,ps1}`, `render-mcp.{sh,ps1}`, `render-otel.{sh,ps1}` — from `$COMPONENT_DIR/<file>` to `$COMPONENT_DIR/templates/<file>` (the `.ps1` `Join-Path $ComponentDir '<file>'` likewise). Pure relocation, no behaviour change. Load-bearing: fresh `claude-code-elastic` and `claude-code-elastic-audit` setups render the same `.claude/` artifacts as before; gates clean.
- [x] **(tidy) Purge comments aggressively, project-wide, per CONVENTIONS "Comments and leanness".** Apply the repo's default-no-comment rule retroactively to **every** `.sh` and `.ps1` under `stacks/` and `components/`: strip header/banner comments, what-narration, section decoration, and drive-by notes — lean hard toward removal. Keep only (a) tool directives (`# shellcheck …`, PSScriptAnalyzer suppressions) and (b) the rare genuinely load-bearing **why** whose loss would mislead a future editor; where such a why runs more than a line (e.g. the protobuf-payload rationale in `smoke-test.sh`), relocate it to `SPEC/` or the README rather than keep prose inline. Comments don't affect runtime, so gates/verify can't catch knowledge loss — the durable rationale already lives in the spec layer (SPEC, the immutable PRD tasks), which is the net; when in doubt, cut. Load-bearing: `shfmt` / `shellcheck` and `Invoke-Formatter` + `Invoke-ScriptAnalyzer` stay clean; every stack's `smoke-test` / `verify-*` still PASS.

Add **managed-config placement** as an agent-scoped, opt-in capability per `SPEC/claude-code-managed-config.md` and `SPEC/codex-cli-managed-config.md` "Placement in this lab" (both already written — do **not** edit SPEC). It is **deploy-only and human-gated**: **no mechanical verification, no `--yes`** — placement is always interactive, **aborts on non-TTY/EOF**, **fails loud** on permission errors, and **never overwrites a file the lab does not own** (provenance via a sidecar marker; a mandatory teardown removes only lab-placed files). **Ralph must NOT execute placement against the host and must NOT wire it into any automated suite** — acceptance is static gates plus the documented interactive behaviour reviewed by inspection, never an automated run (the non-TTY abort guarantees an automated run places nothing). Three tasks (the per-agent two depend on the first):

- [x] **(managed 1/3) Shared placement + teardown primitive.** Add an agent-agnostic managed-config placement/teardown mechanism under `components/agents/shared/managed-config/` (mirroring `shared/agent-audit/`), parameterized by a per-agent manifest of `{ os → target path, files }`. Placement: prompts interactively showing the exact target path(s) and a diff against any existing managed file; **no `--yes`** and **aborts on non-TTY/EOF**; on a permission error **fails loud** with the path + the privileged/manual command; **never overwrites a file lacking the lab's sidecar provenance marker** (foreign / real-org file → hard refuse), refuses a different-stack lab marker ("run teardown first"), no-ops an identical same-stack placement, and writes/updates the sidecar marker (agent, stack, endpoint, timestamp) on success. Teardown: removes **only** files the marker confirms the lab placed (restoring the host), refuses foreign files, also interactive + `--yes`-less + fail-loud. Provide `.sh` + `.ps1`. **Do not execute against the host**; acceptance = `shfmt`/`shellcheck` + `Invoke-Formatter`/`Invoke-ScriptAnalyzer` clean and the no-overwrite / abort / fail-loud branches correct by inspection.
- [x] **(managed 2/3) Claude Code `managed-settings.json`.** Depends on managed 1/3. Add the Claude managed template and the per-agent manifest (the **single** `managed-settings.json` at the three platform paths in `SPEC/claude-code-managed-config.md`) plus `setup-managed` / `teardown-managed` façades wiring the shared primitive. The template enforces what Claude can — notably the telemetry `env` block (`CLAUDE_CODE_ENABLE_TELEMETRY=1` + the `OTEL_*` exporters); state honestly in template/prompt that these are **enforced** (highest precedence, user cannot override). Opt-in is a `--managed` flag / `setup.conf` key that runs the interactive placement — **not** wired into the automated `setup.sh` path. Update the relevant stack README(s) with the manual managed-config + teardown steps. Acceptance: static gates clean; **no host execution**.
- [x] **(managed 3/3) Codex CLI `requirements.toml` + `managed_config.toml`.** Depends on managed 1/3. Mirror managed 2/3 for Codex: a manifest covering **both** files at their platform paths (`SPEC/codex-cli-managed-config.md`) — `requirements.toml` (enforced: managed hooks, `allow_managed_hooks_only`, `[features].hooks`, sandbox/approval) and `managed_config.toml` (soft default; the Windows weak boundary). State honestly that **`[otel]`/telemetry is a managed *default*, not enforceable** (the asymmetry with Claude). `setup-managed` / `teardown-managed` façades + stack opt-in + README. Acceptance: static gates clean; **no host execution**.

Fixes for the managed-config placement scripts — **keep the shared design** (`components/agents/shared/managed-config/` core + per-agent `scripts/lib/managed-config-adapter.*`, `scripts/setup-managed.*`, `scripts/teardown-managed.*`, templates). For every task below: **do NOT execute placement against the host** (no mechanical verification — non-TTY abort); `SPEC/*-managed-config.md` and `CONVENTIONS.md` already state the rules, do **not** edit them; acceptance is `shfmt`/`shellcheck` + `Invoke-Formatter` (repo `PSScriptAnalyzerSettings.psd1`) + `Invoke-ScriptAnalyzer` clean plus correctness by inspection. The tasks are independent.

- [x] **(managed fix: endpoints) Honour the no-`/v1` invariant.** `setup-managed.{sh,ps1}` must take the **full per-signal OTLP endpoints** from the caller (like `render-otel`), not a base `--endpoint` it splits into `/v1/logs|traces|metrics`; build the `/v1/*` URLs in each stack's `setup.sh` (where the base lives) and pass them in. (SPEC.md: "no `/v1/<signal>` path construction in the agent".)
- [x] **(managed fix: ownership key) Drop `--stack`; key the overwrite guard on the endpoint.** A component taking a *stack name* is stack-aware against the repo's stack-agnostic convention; the meaningful value (the endpoint) is already passed. Remove `--stack` from the core/façades; the marker records agent / endpoint / placed_at / target; refuse-overwrite when an existing lab marker's **endpoint differs** ("this host already enforces <endpoint>; run teardown first"), no-op when identical, confirm+update when same endpoint with changed content. Teardown stays stack-agnostic (lab-marked → remove, foreign → refuse) — now consistent on both sides.
- [x] **(managed fix: Codex hook paths) Managed hooks must reference real deployed paths.** `codex setup-managed.sh` points managed `requirements.toml` at `$hooks_dir/agent-audit.{sh,ps1,conf}` and `managed_dir=$hooks_dir` (the repo component dir), but `agent-audit.conf` is not rendered there — a managed-enforced audit hook would fail-open with no config. Resolve: have `setup-managed` also materialise the conf and point at its real location, or document the required deployment layout, so the managed hook actually finds its config.
- [x] **(managed fix: shell naming) Lowercase non-env globals; spell names out.** In the `.sh` managed scripts, **lowercase** every non-env-overridable global (`SCRIPT_DIR`/`COMPONENT_DIR`/`TEMPLATE` → `script_dir`/`component_dir`/`template`; UPPER only for exported/env-overridable like `ES_URL`), and **avoid abbreviations** — full variable names (loop vars `i`/`k`/`v` exempt), including the cryptic `mc_` prefix; align with the hook style (`name::verb` functions, full variable names).
- [x] **(managed fix: PowerShell naming) Verb-Noun functions; spell names out.** In the `.ps1` managed scripts, rename every function to approved **Verb-Noun** — `McFail`/`McPlaceOne`/`McTeardownOne`/`McRemoveFile` → e.g. `Write-McFatal`/`Install-McManagedFile`/`Remove-McManagedFile` — and spell variable names out in full (no abbreviations beyond conventional loop vars; reconsider the `Mc` prefix), matching the hook scripts' convention.
- [x] **(managed fix: source contract) Define the façade↔adapter source handoff.** The adapter reads the rendered source path from a façade-set global whose name differs per agent (`mc_source` vs `mc_source_requirements`/`mc_source_managed_config`) and the core never defines it; standardise — e.g. the manifest function takes the rendered source path(s) as parameters, or one documented naming scheme.
- [x] **(managed fix: nits)** The marker is written *after* the file copy — if the copy succeeds but the marker write fails the file becomes "foreign" on the next run (self-lockout); guard or note. Omit `OTEL_EXPORTER_OTLP_HEADERS` when there are no headers rather than rendering `""`. Remove the trailing blank lines at the end of `managed-config-core.ps1`.
- [x] **(managed: Codex hook posture) `allow_managed_hooks_only = false` and single-source the hook tables.** In `components/agents/codex-cli/templates/requirements.template.toml` set **`allow_managed_hooks_only = false`** (currently `true` — the lab keeps user hooks coexisting; enforce hook *presence*, not exclusivity), keep `[features].hooks = true` and `managed_dir`/`windows_managed_dir`, and **remove the inline `[[hooks.*]]` tables**: `setup-managed.{sh,ps1}` renders `templates/hooks.template.toml` and **concatenates** it onto the rendered requirements (top-level fields + `[features]` first, the `[[hooks.*]]` arrays last, so the TOML parses) so the hook tables are single-sourced. `SPEC/codex-cli-managed-config.md` already states this posture — do **not** edit SPEC. Static gates; **no host execution**.

Introduce the **config-deployment model** from `SPEC/config-deployment.md` (already written — do **not** edit SPEC): a self-contained bundle deployed to a `(target, scope)`, materialized per deployment so a component edit no longer cross-affects live stacks. Split `setup.sh` into `setup-backend` + `setup-config`, add `--scope local|managed` (default `local`) + `--target`. The default `setup.sh` (no flags) must stay end-to-end equivalent (smoke/verify green). Staged:

- [x] **(deploy 1/4) Split `setup-backend` / `setup-config`; add `--scope` / `--target`.** Decompose each stack's `setup.sh` into `setup-backend` (compose up + ES/Kibana asset imports) and `setup-config` (deploy the agent bundle to a target), with `setup.sh` the composition forwarding `--scope local|managed` (default `local`) and `--target <dir>`. `--scope managed` routes config to the existing managed placement (`setup-managed`); `setup.sh --scope managed` still brings the backend up; **managed-only without a backend is `setup-config --scope managed`**. No materialize change yet — `--scope local` with no `--target` reproduces today's stack-local deployment. Mirror: the existing per-stack `setup.{sh,ps1}` + the agent `setup-telemetry`/`setup-audit` façades. Verify: every stack's `smoke-test`/`verify-*` reach PASS unchanged; gates clean.
- [x] **(deploy 2/4) Materialize the bundle (self-containment).** Change `setup-config` (local scope) from referencing hook scripts in-place under `components/` to **materializing** them — copy `agent-audit.{sh,ps1}` + the shared core + the per-agent adapter (plus `agent-audit.conf`, mcp, settings/registration) into the target's `.claude/`/`.codex/`, and repoint the hook `command` at the materialized scripts — so the deployment is self-contained and editing a component source no longer changes a live stack. Update the `verify-*` scripts (they drive the rendered hook) to the materialized paths. The shared hook core stays a single source in the repo, copied per deployment. Verify: a fresh deploy references nothing back under `components/` at runtime; `smoke-test`/`verify-*` PASS with byte-identical audit documents.
- [x] **(deploy 3/4) `local --target <dir>` to an arbitrary project.** With materialize in place, allow `setup-config --scope local --target <dir>` to deploy a self-contained bundle into an arbitrary directory (a user's real project) pointing at the lab backend's endpoints. README documents the use ("see what your everyday work emits") and the sensitive-content caveat (per `SPEC/config-deployment.md`); the scripts do **not** emit the caveat. Verify: deploying into a scratch dir yields a runnable, self-contained `.claude/`/`.codex/`; gates clean.
- [x] **(deploy 4/4) Managed audit hooks: materialize scripts (gated on a host confirmation).** Extend `managed` scope to also materialize the hook scripts to a host location and point the managed config at them — Codex into its `managed_dir`, Claude into a `hooks/` dir beside `managed-settings.json`. **Gated on a one-time manual host check** that an absolute Claude hook `command` path works (macOS space / Windows `Program Files` — record the result); until confirmed, `managed` stays config-only (telemetry). Per `SPEC/config-deployment.md` "Managed materialize is staged" and the `SPEC/claude-code-managed-config.md` caveats. **No automated host execution** (managed is human-gated).

- [x] **Rename the `codex-cli` concern/component/stack/doc name to `codex`.** The agent is the OpenAI **Codex** family — CLI (`service.name: codex_cli_rs`) and Desktop / app-server (`service.name: codex-app-server`) share one config engine and managed-config layer — so the organizational name is `codex`, not `codex-cli`. Done in-conversation (spec + implementation), behaviour-neutral. **Three token classes, treated differently:**
  1. **Organizational name → `codex`:** dirs `components/agents/codex-cli` and `components/backends/services/{elasticsearch,kibana}/codex-cli`; stacks `codex-cli-elastic{,-audit}` → `codex-elastic{,-audit}` (incl. compose `name:` and all path/concern/import-arg references); docs `SPEC/codex-cli-{telemetry,managed-config}.md` → `SPEC/codex-{telemetry,managed-config}.md`; agent-identity literals — the audit adapter `agent_name`, the `.managed` provenance `agent=` marker, and the `agent_audit.agent.name` value — `codex-cli` → `codex`.
  2. **Real `service.name` tokens preserved verbatim:** `codex_cli_rs` (CLI) keeps its asset filenames, the `@custom-codex_cli_rs` sub-pipelines, and the `agents_codex_cli_rs` trace namespace; `codex-app-server` (Desktop) is untouched.
  3. **Surface-specific Kibana IDs/titles kept:** the `codex-cli-*` saved-object ids and "Codex CLI — *" data-view titles denote the **CLI surface** (like the `codex_cli_rs` filenames) and stay; the dir holding them moved to `kibana/codex/`. A second Desktop-surface view set is added later.

  Also fixed a stale doc fact: the `codex-elastic` README stated `service.name: codex-cli` (actually `codex_cli_rs`). Completed PRD tasks and `reports/` retain their historical `codex-cli` references (immutable). The full two-surface reframe of `SPEC/codex-telemetry.md` (documenting `codex-app-server` alongside `codex_cli_rs`) and the Desktop ingest coverage (the `@custom-codex` diamond + per-surface assets) are deferred to a follow-up. Verified: both renamed stacks `docker compose config -q` pass; a repo-wide grep finds no organizational `codex-cli` outside the preserved Kibana ids / `reports/` / closed PRD tasks; `codex_cli_rs` (103 occurrences) untouched. Behaviour-neutral (paths/names only) — full `smoke-test`/`verify-*` re-run is deferred to the next `down -v && up` under the new `codex-elastic` project name.

- [x] **(Codex Desktop coverage — logs) Diamond the codex app-logs pipeline and widen the logs template to both surfaces.** Codex Desktop (`service.name: codex-app-server`) emits the same app-logs schema as the CLI (`codex_cli_rs`) but was unhandled — tool I/O landed **unredacted** and the stream missed the lab's ILM/mappings — because the lab's ES assets were keyed to `codex_cli_rs` only. Refactor the `logs-apm.app@custom` dispatch into a **diamond** so every Codex surface shares one logic body: the per-`service.name` sub-pipelines become thin **forwarders** (`logs-apm.app@custom-codex_cli_rs` and new `logs-apm.app@custom-codex-app-server`, each `{ "pipeline": { "name": "logs-apm.app@custom-codex" } }`) to a single shared **`logs-apm.app@custom-codex`** that holds the drop-streaming-deltas + redact-`arguments`/`output`/`prompt` logic (moved verbatim from the old `…-codex_cli_rs` body). The router (`shared/logs-apm.app@custom`) is unchanged — it still dispatches by `{{service.name}}`, now resolving the Desktop forwarder too. Widen the logs index template pattern to **`logs-apm.app.codex_*-*`** so one template + ILM governs both physical streams (`codex_cli_rs` + `codex_app_server`) — **physical streams stay separate** (no merge); the template is single-sourced. Family-rename (lab, reset-resets-conflicts): `logs-apm.app.codex_cli_rs.index-template.json` → `logs-apm.app.codex.index-template.json`, and the per-agent ILM `telemetry.codex_cli_rs{-policy,@lifecycle}` → `telemetry.codex{-policy,@lifecycle}` (all 3 signal templates' `composed_of` + the lifecycle component's `index.lifecycle.name` updated; SPEC.md ILM table updated). SPEC.md "APM `@custom` pipelines" invariant updated to document the diamond (`router → @custom-<service.name> → @custom-<agent>`). **Deferred to their own increments:** metrics + traces Desktop coverage (their templates keep `codex_cli_rs`-only patterns and names until widened — traces also needs a reroute-namespace decision for `codex-app-server`), and the two-surface reframe of `SPEC/codex-telemetry.md`. Verified: all asset JSON valid; a non-destructive `_ingest/pipeline/_simulate` of the shared `-codex` logic redacts a `codex-app-server` sample doc's `arguments`/`output`/`prompt` to `[REDACTED]`. Full live e2e (a Desktop session's logs landing redacted in the wildcard-covered stream with the lab ILM) deferred to the next telemetry-stack run.

- [x] **(Codex Desktop coverage — metrics) Widen the metrics template to both surfaces.** Metrics need **no pipeline** — they are already per-service (dataset = `service.name`, so `metrics-apm.app.codex_cli_rs-default` and `metrics-apm.app.codex_app_server-default` are naturally isolated, nothing to reroute) and carry no tool I/O / prompt text (nothing to redact); the lab's only metrics asset is the per-agent index template that supplies ILM + APM mappings. So Desktop coverage is the template alone: widen `metrics-apm.app.codex_cli_rs.index-template.json` to pattern **`metrics-apm.app.codex_*-*`** and family-rename it to **`metrics-apm.app.codex.index-template.json`** (it already composes `telemetry.codex@lifecycle`). Physical streams stay separate. Data-stream names (`codex_cli_rs` / `codex_app_server`) are unchanged, so no doc edits. Verified: JSON valid, pattern resolves both `codex_*` metrics streams. Live e2e deferred to the next telemetry-stack run. Remaining for full Desktop parity: traces (reroute diamond + an `agents_codex_app_server` namespace decision) and the two-surface reframe of `SPEC/codex-telemetry.md`.

- [x] **(Codex Desktop coverage — traces) Diamond the traces pipeline with per-surface reroute; split the physical trace streams.** Codex Desktop traces (`service.name: codex-app-server`) landed in the service-agnostic `traces-apm-default` (un-isolated, missing the lab ILM). Diamond the `traces-apm@custom` dispatch like logs, but **asymmetrically**: the common **drop** (streaming spans `receiving`/`handle_responses` — same span names on both surfaces) goes in the shared **`traces-apm@custom-codex`**, while the **reroute is per-surface** (different namespaces) and lives in each forwarder. Physical streams **stay split** (user decision): CLI → `agents_codex_cli_rs`, Desktop → `agents_codex_app_server`. **Ordering is load-bearing: `reroute` is a terminal processor — any processor after it is skipped** (confirmed live via `_simulate`: `reroute`→`drop` does not drop; `drop`→`reroute` does), so each forwarder calls the shared drop **first**, then reroutes **last**: `traces-apm@custom-codex_cli_rs` = `[ pipeline→traces-apm@custom-codex, reroute agents_codex_cli_rs ]`, new `traces-apm@custom-codex-app-server` = `[ pipeline→traces-apm@custom-codex, reroute agents_codex_app_server ]`. Re-entry stays loop-free (a rerouted doc re-runs the router; the second reroute to the same namespace is a no-op). Widen the traces index template to pattern **`traces-apm-agents_codex_*`** and family-rename to **`traces-apm-agents_codex.index-template.json`** (already composes `telemetry.codex@lifecycle`) so both per-surface trace streams inherit the lab ILM + APM trace mappings. Verified: all traces asset JSON valid; `_simulate` confirms a `receiving` span drops and a normal span reroutes to `traces-apm-agents_codex_app_server`. Deferred: a surface-specific Kibana data view for Desktop traces (`traces-apm-agents_codex_app_server*`) — Desktop traces are already visible via the cross-agent **AI Agents — Traces** view (`traces-apm-agents_*`); and the two-surface reframe of `SPEC/codex-telemetry.md`. Live e2e deferred to the next telemetry-stack run with a Desktop session.

- [x] **(Codex Desktop coverage — Kibana) Make the Codex Kibana objects surface-agnostic.** Policy: **CLI vs Desktop is not surfaced to users** — the Kibana layer presents one "Codex" agent spanning both surfaces. In `components/backends/services/kibana/codex/{data-views,saved-searches}.ndjson`: rename ids `codex-cli-*` → `codex-*` (data views + 15 saved searches + every cross-`references[]`/`meta.index`), titles "Codex CLI — *" → "Codex — *". **Re-point the 3 data views to both surfaces** — `metrics-apm.app.codex_*`, `logs-apm.app.codex_*`, `traces-apm-agents_codex_*` — and **widen the embedded ES|QL `FROM` clauses** the same way (`FROM logs-apm.app.codex_*`, `FROM traces-apm-agents_codex_*, logs-apm.app.codex_*`); some Codex saved searches are ES|QL-mode and hard-code the index. No saved search filters on `service.name`, so widening introduces no surface leak. Sync the stack README's data-view title references ("Codex CLI — *" → "Codex — *"). **Version-validity audit (0.137-authored → 0.142 current):** no search uses the now-split `codex.websocket_event` (clean); the `target=codex_otel.trace_safe` + `event_name=codex.tool_result|user_prompt` discriminators still exist at 0.142. **Two suspect classes need live 0.142 confirmation (not yet run — telemetry stack down):** the `*-log-only-*` searches (discriminate on `service.framework.name == codex_otel.log_only` — unconfirmed at 0.142) and the span.name-based trace searches (`try_run_sampling_request` etc. — span names churn across versions). The two `(Deprecated)` searches are intentional, not version-broken. Verified: all NDJSON lines valid; no residual `codex-cli` / `codex_cli_rs` / "Codex CLI". The live saved-search validation + the broader two-surface `SPEC/codex-telemetry.md` reframe remain deferred to the next telemetry-stack run.

- [x] **Rename the `claude-code` namespace to `claude`** (mirror of the Codex rename), so the agent/family name is `claude` — Claude Code (`service.name: claude-code`) and Claude Desktop (`service.name: claude-code-desktop`, confirmed live) are two surfaces of one `claude` agent. **Critical difference from Codex: the org name `claude-code` is identical to the CLI `service.name`, and `claude_code.*` are physical message values Claude emits** — so this was a **surgical, per-token** rename, not a blind sed. **Renamed (organizational → `claude`):** component dirs `components/agents/claude-code` and `components/backends/services/{elasticsearch,kibana}/claude-code`; stacks `claude-code-elastic{,-audit}` + `claude-code-otelcol-elastic` → `claude-elastic{,-audit}` / `claude-otelcol-elastic`; docs `SPEC/claude-code-{telemetry,managed-config}.md` → `claude-{telemetry,managed-config}.md`; audit adapter `agent_name` `'claude-code'` → `'claude'`; ILM `telemetry.claude_code{-policy,@lifecycle}` → `telemetry.claude…`; index-template file/template names `*-apm.app.claude_code` / `traces-apm-agents_claude_code` → `…claude`; concern/import args, compose `name:`, path refs (incl. Windows `\` paths), Kibana ids `claude-code-*` → `claude-*` and titles "Claude Code — *" → "Claude — *", and the 5-stacks/README Agent column "Claude Code" → "Claude". **Preserved (physical — emitted by Claude / dispatch keys):** `service.name` `claude-code` and `claude-code-desktop`; pipeline **names** `logs-apm.app@custom-claude-code` / `traces-apm@custom-claude-code` (router dispatches by `{{service.name}}`); index **patterns** `*-apm.app.claude_code*`, trace namespace `agents_claude_code`, `message: claude_code.*` values; and descriptive "Claude Code" product prose / `claude-code-guide` / heading anchors. Note `logs-apm.app.claude_code*` already matches the Desktop dataset `claude_code_desktop` (prefix), so templates/ILM cover Desktop; only the redaction/reroute **pipeline diamond** for `claude-code-desktop` remains (deferred with the rest of Desktop telemetry coverage + Saved-Search rebuild). Verified: all NDJSON/JSON valid; all three renamed stacks `docker compose config -q` pass; remaining `claude-code` occurrences are physical-only (service.name / `@custom-` pipeline refs / product prose) in three docs. Behaviour-neutral rename — full `smoke-test`/`verify-*` re-run deferred to the next `down -v && up` under the new `claude-elastic` project name.

- [x] **(Claude Desktop coverage — logs) Diamond the Claude app-logs pipeline for both surfaces.** Claude Desktop emits `service.name: claude-code-desktop` (confirmed live); the router dispatched it to `logs-apm.app@custom-claude-code-desktop`, which did not exist, so Desktop's force-redaction/drop defence never ran (prompt is source-redacted by the client default, but a client turning the `OTEL_LOG_*` gates on would have leaked on Desktop). Diamond it like Codex: move the drop-raw-bodies/tool-output + redact-prompt + field-drop-tool-args logic into a shared **`logs-apm.app@custom-claude`**, and make `logs-apm.app@custom-claude-code` a thin forwarder to it, plus a new `logs-apm.app@custom-claude-code-desktop` forwarder. **No template/ILM change needed**: the `logs-apm.app.claude_code*` pattern already prefix-matches the Desktop dataset `claude_code_desktop-default` (unlike Codex, where `codex_cli_rs`/`codex_app_server` share no prefix). Verified: all three pipelines valid and installed live (200); `_simulate` of the shared logic and of the `…-claude-code-desktop` forwarder by name both redact prompt to `<REDACTED>`, remove `labels.tool_input`, and drop `tool.output` on a `claude-code-desktop` doc. Live from the next Desktop session. Metrics need nothing (same prefix-match on `metrics-apm.app.claude_code*`, no pipeline); traces (reroute diamond → `agents_claude_code_desktop`) are the remaining Desktop-coverage piece.

- [x] **(Claude Desktop coverage — logs + metrics index templates) Widen the `-*` patterns to cover the Desktop dataset.** Correction to the logs task above, which wrongly claimed no template change was needed: the logs and metrics index templates matched `…app.claude_code-*` (hyphen), which does **not** match the Desktop dataset `claude_code_desktop-default` (the char after `claude_code` is `_`, not `-`) — confirmed live, the Desktop streams resolved to APM's default `*-apm.app@template` (APM default ILM), not the lab template. Widen both patterns to **`…app.claude_code*-*`** (keep the `-<namespace>` hyphen — mirrors Codex's `codex_*-*` — so it matches `claude_code-default` **and** `claude_code_desktop-default` without being over-loose). Traces are **not** touched here (trace data streams are `traces-apm-agents_claude_code` with no `-default` suffix — a different shape, handled in the traces increment). Verified live (after clearing the stale pre-rename `logs/metrics-apm.app.claude_code` templates): `_simulate_index` now resolves `telemetry.claude-policy` for **both** the CLI (`claude_code-default`) and Desktop (`claude_code_desktop-default`) logs and metrics streams. Existing Desktop streams re-bind on next rollover / `down -v && up`. Metrics need no pipeline (no redaction/reroute); the logs pipeline diamond shipped separately.

- [x] **(Claude Desktop coverage — traces) Diamond the traces pipeline with per-surface reroute.** Same pattern as Codex, but Claude's shared op is a **redact** (interaction-root `labels.user_prompt` → `<REDACTED>`, forced regardless of `OTEL_LOG_USER_PROMPTS`), not a streaming-span drop. Move it into a shared **`traces-apm@custom-claude`**; make `traces-apm@custom-claude-code` a forwarder `[ pipeline→traces-apm@custom-claude, reroute agents_claude_code ]` and add `traces-apm@custom-claude-code-desktop` = `[ pipeline→…-claude, reroute agents_claude_code_desktop ]`. **reroute is terminal → it must come last** (shared redact first via the pipeline processor, reroute last). Physical trace streams stay split: CLI → `agents_claude_code`, Desktop → `agents_claude_code_desktop`. **No traces template change** (this is where traces differ from logs/metrics): trace data streams are `traces-apm-agents_<ns>` with **no `-default` suffix**, so the existing `traces-apm-agents_claude_code*` pattern prefix-matches the Desktop namespace `agents_claude_code_desktop` directly. Verified live: 3 pipelines valid + installed (200); `_simulate` of the `…-claude-code-desktop` forwarder by name redacts `user_prompt` and reroutes to `traces-apm-agents_claude_code_desktop`; after clearing the stale pre-rename `traces-apm-agents_claude_code` template, `_simulate_index` resolves `telemetry.claude-policy` for both `agents_claude_code` and `agents_claude_code_desktop`. Completes Claude Desktop telemetry coverage (logs + metrics + traces). Live from the next Desktop session (existing default-routed Desktop spans stay put).

- [x] **(Codex — preemptive coverage of the mcp-server surface) Add `codex-mcp-server` forwarders.** `codex mcp-server` (Codex run as an MCP server so another agent drives it — exposes `codex()`/`codex-reply()`) is a **third Codex surface** (`service.name: codex-mcp-server` → dataset `codex_mcp_server`), distinct from `codex_cli_rs` (CLI) and `codex-app-server` (Desktop); observed live once. It was uncovered by the codex per-service pipelines. Add `logs-apm.app@custom-codex-mcp-server` (forwarder → shared `logs-apm.app@custom-codex`) and `traces-apm@custom-codex-mcp-server` (`[ pipeline→traces-apm@custom-codex (drop), reroute agents_codex_mcp_server ]`), mirroring the Desktop forwarders. **No template/ILM change**: `logs/metrics-apm.app.codex_*-*` and `traces-apm-agents_codex_*` already prefix-cover `codex_mcp_server` / `agents_codex_mcp_server`. **Unverified against live data**: `codex mcp-server` telemetry could not be reproduced — driving it via MCP Inspector and Claude Code both fail the initialize handshake, matching known upstream bugs ([openai/codex #6664](https://github.com/openai/codex/issues/6664) hang; [#7155](https://github.com/openai/codex/issues/7155) Windows stderr → transport closed); the origin of the one observed `codex_mcp_server` stream (likely Codex Desktop's internal mcp-server use) is unknown. Coverage assumes the surface emits the standard Codex OTel schema (same binary, different entry point). Verified: both pipelines valid + installed live (200); `_simulate` by name redacts `arguments`/`output`/`prompt` and reroutes a normal span to `traces-apm-agents_codex_mcp_server` (streaming span dropped). Defensive / behaviour-neutral — effective only if/when the surface emits.

- [x] **(backport from `agent-audit`) Composite `.managed` marker endpoint (`telemetry=<logs>;audit=<es>`).** Backport of the dist's `marker_endpoint` refactor (only that commit — the dist's ILM production-retention change is Cloud-specific and deliberately **not** ported). The `.managed` provenance/ownership marker keyed on a single endpoint that was **overloaded** — telemetry deploys stored the OTLP logs URL, audit-only deploys reassigned `logs_endpoint=$es_url` so the marker held the ES URL instead — meaning a telemetry home and an audit home on the same host had **indistinguishable** ownership keys, and the overload obscured which of the two a marker described. Replace with a **composite** endpoint `telemetry=<logs_endpoint>;audit=<es_url>` set explicitly in each `setup-managed` (both fields always recorded, empty side blank), so place()/teardown() ownership and the marker describe the full deploy. Shared core (`managed-config-core.sh`): add the `marker_endpoint` global (default `''`); `write_marker`, the place-guard, the teardown prompt, and the "no endpoint provided" check read `marker_endpoint` instead of `logs_endpoint` (guard/teardown locals renamed `marker_endpoint`→`existing_endpoint` to compare stored-vs-current). The `.ps1` core already threaded `McEndpoint`, no change. All 4 `setup-managed.{sh,ps1}` (codex + claude) set the composite and drop the audit-URL overload. **Lab-only fix beyond the dist:** the dist left the new `marker_endpoint=` assignment tripping shellcheck **SC2034** (set here, read across the `source=/dev/null` core-lib boundary); the lab adds a targeted `# shellcheck disable=SC2034` with rationale so the gate stays clean (candidate to propagate back to the dist). Verified: shellcheck / shfmt / `bash -n` clean on the 3 `.sh`; both `.ps1` parse + PSScriptAnalyzer clean. Behaviour-neutral except the richer marker string; existing markers re-key on the next `setup-managed`/`teardown` cycle.

- [x] **(fix) Restore Claude's Linux managed-settings path to `/etc/claude-code`.** The `claude-code`→`claude` rename over-reached: `managed-config-adapter.{sh,ps1}` had its Linux managed root changed from `/etc/claude-code` to `/etc/claude`, but that is **Claude Code's real fixed product path** (`/etc/claude-code/managed-settings.json`) — an org rename must not touch it, and the tell was that macOS (`/Library/Application Support/ClaudeCode`) and Windows (`C:\Program Files\ClaudeCode`) kept the `ClaudeCode` product token while only Linux was rewritten. On Linux the client would have read `/etc/claude-code/` while the tool placed under `/etc/claude/`, so managed config silently would not apply (never caught — the pilot verification was Windows-only). Revert the path in both adapters and in the docs that quote it (`SPEC/claude-managed-config.md` table, `SPEC/agent-audit.md` `hooks/` staging example, `stacks/claude-elastic/README.md` table). The audit `agent_name`/marker stays `claude` (that is the lab's own identity label, correctly renamed — only the OS-fixed path was wrong). Surfaced while porting the rename to the `agent-audit` distribution, where the same fix was applied (the dist deliberately keeps `/etc/claude-code`). Verified: no `/etc/claude` (non-`-code`) token remains; shellcheck + shfmt clean; ps1 parse + PSScriptAnalyzer clean.

- [x] **(callers) Add a Spring Boot one-shot caller: trace-context propagation + per-app attribution.** New top-level concept **`callers/`** (SPEC.md repository shape + CONVENTIONS tech stack updated): a calling *application* that launches an agent as a subprocess to exercise the **agent's** telemetry from the client side — it emits no telemetry of its own and never changes `service.name`, so the standard pipelines/templates/views apply unchanged. First member **`callers/spring-boot/`**: Spring Boot 4.1 / Java 25, non-web (`spring-boot-starter` only), an **agent-agnostic** `AgentLaunchRunner` (`CommandLineRunner` + `ExitCodeGenerator`) that fabricates a W3C `TRACEPARENT=00-<traceId>-<spanId>-01`, sets `OTEL_RESOURCE_ATTRIBUTES=app.name=<name>`, then `ProcessBuilder`-launches the selected agent in its stack dir (so that stack's telemetry env / traces beta apply), waits, and exits with the child's code. Per-agent bits (argv, default stack working-dir, extra env) live in an `Agent` enum selected by `caller.agent`; **only `claude` is wired today** (`claude -p`, `claude-elastic`) — a second agent (e.g. Codex `codex exec` + `CODEX_HOME`) is a new enum constant. All knobs under `caller.*` (`agent`/`app-name`/`prompt`/`command`/`working-dir`), overridable at launch. Run via `./mvnw spring-boot:run`; Maven wrapper is `only-script` (no committed jar, pins 3.9.16). Result: Claude's spans join the caller's `trace.id` (the per-run identifier), and every Claude signal carries `labels.app_name` (auto-mapped keyword — no ES change). No per-run `app_id` — `trace.id` already identifies the run. Implemented directly (not via Ralph); `mvn compile` clean and the wrapper bootstraps on JDK 25.

- [x] **(callers/spring-boot) Mint the trace id via Micrometer Tracing instead of fabricating it.** The caller now runs real **Micrometer Tracing** (as a modern Spring service does) rather than hand-rolling a W3C id: the runner opens a span (`Tracer.nextSpan().start()` inside a `SpanInScope`, so the trace context is active in the thread) and reads the **W3C `traceparent`** from it via the `Propagator`, then hands that to the agent. A `CommandLineRunner` gets no auto span (unlike a web request / `@Scheduled`), so the span is opened explicitly. Deps are the **Boot 4 granular OTel modules** `spring-boot-opentelemetry` + `spring-boot-micrometer-tracing-opentelemetry` + `micrometer-tracing-bridge-otel` — **no OTLP metrics registry and no span exporter**, so the app still exports nothing: the full `spring-boot-starter-opentelemetry` was tried first and rejected because it pulls `micrometer-registry-otlp`, which auto-publishes metrics to `localhost:4318` (confirmed via a DI smoke). `management.tracing.sampling.probability=1.0` (traceparent flag `01` so the agent samples + exports its children) and `propagation.type=W3C` (guarantees the `traceparent` carrier key). Removed the `SecureRandom`/`HexFormat` fabrication. Verified: `mvn compile` clean; a bogus-agent DI smoke reaches the `caller.agent` error with **zero** OTLP publish attempts (Tracer/Propagator wired, nothing exported). Java baseline for `callers/` bumped to 25 (README/CONVENTIONS).
