# Control plane — backend endpoints scripts/provision.{sh,ps1} target.
# (elasticsearch.url is also reused by the Elasticsearch MCP.)
elasticsearch.url=http://localhost:9200
kibana.url=http://localhost:5601
