# telemetry.otel_collector.api_key: an APM/OTLP API key. When set, the agent sends
# `Authorization: ApiKey <key>` on its OTLP exports (see SPEC/config-deployment.md).
telemetry.otel_collector.api_key=<key>
