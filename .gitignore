.DS_Store
*.swp
*.swo
.idea/

.claude/
.codex/
**/setup.local.conf

# Bare `.mcp.json` would over-ignore committable project MCP configs — scope to the generated Claude-stack ones.
/stacks/claude-code-*/.mcp.json

# reports/ is intentionally tracked (Ralph's HTML history) — do not add it here.
