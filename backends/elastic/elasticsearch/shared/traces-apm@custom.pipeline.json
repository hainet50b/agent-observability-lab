{
  "description": "APM traces @custom router: dispatches to the matching traces-apm@custom-<service.name> sub-pipeline.",
  "processors": [
    { "pipeline": { "name": "traces-apm@custom-{{service.name}}", "ignore_missing_pipeline": true } }
  ]
}
