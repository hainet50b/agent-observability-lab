{
  "description": "Codex Desktop (codex-app-server) traces forwarder: shared drop, then reroute to agents_codex_app_server (reroute last — it is terminal).",
  "processors": [
    { "pipeline": { "name": "traces-apm@custom-codex" } },
    { "reroute": { "namespace": "agents_codex_app_server" } }
  ]
}
