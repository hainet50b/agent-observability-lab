[package]
name = "agent-config"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
sha2 = "0.10"
zip = { version = "5", default-features = false, features = ["deflate"] }
