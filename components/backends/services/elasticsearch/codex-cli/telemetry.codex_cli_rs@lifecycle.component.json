{
  "template": {
    "settings": {
      "index.lifecycle.name": "telemetry.codex_cli_rs"
    }
  },
  "_meta": {
    "description": "Attaches the telemetry.codex_cli_rs ILM policy (composed last so it overrides APM's default lifecycle).",
    "managed_by": "agent-observability-lab"
  }
}
