{
  "template": {
    "settings": {
      "index.lifecycle.name": "telemetry.claude_code"
    }
  },
  "_meta": {
    "description": "Attaches the telemetry.claude_code ILM policy (composed last so it overrides APM's default lifecycle).",
    "managed_by": "agent-observability-lab"
  }
}
