{
  "description": "APM app-logs @custom router: dispatches to the matching logs-apm.app@custom-<service.name> sub-pipeline.",
  "processors": [
    { "pipeline": { "name": "logs-apm.app@custom-{{service.name}}", "ignore_missing_pipeline": true } }
  ]
}
