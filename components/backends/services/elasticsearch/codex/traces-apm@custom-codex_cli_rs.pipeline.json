{
  "description": "Codex CLI (codex_cli_rs) traces forwarder: shared drop, then reroute to agents_codex_cli_rs (reroute last — it is terminal).",
  "processors": [
    { "pipeline": { "name": "traces-apm@custom-codex" } },
    { "reroute": { "namespace": "agents_codex_cli_rs" } }
  ]
}
