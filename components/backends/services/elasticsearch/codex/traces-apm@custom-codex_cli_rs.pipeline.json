{
  "description": "Codex CLI traces sub-pipeline: drops streaming spans and reroutes to traces-apm-agents_codex_cli_rs.",
  "processors": [
    { "drop": { "if": "ctx.span?.name == 'receiving' || ctx.span?.name == 'handle_responses'" } },
    { "reroute": { "namespace": "agents_codex_cli_rs" } }
  ]
}
