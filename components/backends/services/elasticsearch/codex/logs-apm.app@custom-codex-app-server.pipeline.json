{
  "description": "Codex Desktop / app-server (service.name codex-app-server) app-logs forwarder: dispatched by the logs-apm.app@custom router, forwards to the shared logs-apm.app@custom-codex logic so Desktop redacts/drops identically to the CLI.",
  "processors": [
    { "pipeline": { "name": "logs-apm.app@custom-codex" } }
  ]
}
