{
  "description": "Shared Claude traces logic: redact the interaction-root user prompt (forced, regardless of OTEL_LOG_USER_PROMPTS).",
  "processors": [
    { "set": { "field": "labels.user_prompt", "value": "<REDACTED>", "override": true, "if": "ctx.labels?.user_prompt != null" } }
  ]
}
