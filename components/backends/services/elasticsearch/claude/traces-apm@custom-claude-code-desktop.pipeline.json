{
  "description": "Claude Desktop (service.name claude-code-desktop) traces forwarder: shared redact via traces-apm@custom-claude, then reroute to agents_claude_code_desktop (reroute last — it is terminal).",
  "processors": [
    { "pipeline": { "name": "traces-apm@custom-claude" } },
    { "reroute": { "namespace": "agents_claude_code_desktop" } }
  ]
}
