{
  "description": "Claude Desktop (claude-code-desktop) traces forwarder: shared redact, then reroute to agents_claude_code_desktop (reroute last — it is terminal).",
  "processors": [
    { "pipeline": { "name": "traces-apm@custom-claude" } },
    { "reroute": { "namespace": "agents_claude_code_desktop" } }
  ]
}
