{
  "description": "Claude Code traces logic: redact the interaction-root user prompt (forced, regardless of OTEL_LOG_USER_PROMPTS), then reroute to agents_claude_code (reroute last — it is terminal).",
  "processors": [
    { "set": { "field": "labels.user_prompt", "value": "<REDACTED>", "override": true, "if": "ctx.labels?.user_prompt != null" } },
    { "reroute": { "namespace": "agents_claude_code" } }
  ]
}
