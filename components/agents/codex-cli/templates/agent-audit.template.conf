# capture.<stream>: enabled (true|false), content (plaintext|redacted|encrypted)
capture.user_prompt.enabled=@@CAPTURE_USER_PROMPT_ENABLED@@
capture.user_prompt.content=@@CAPTURE_USER_PROMPT_CONTENT@@
capture.tool_call.enabled=@@CAPTURE_TOOL_CALL_ENABLED@@
capture.tool_call.content=@@CAPTURE_TOOL_CALL_CONTENT@@

# seal: applies when content=encrypted
seal.recipients_file=@@SEAL_RECIPIENTS_FILE@@
seal.key_id=@@SEAL_KEY_ID@@
seal.compress_min_bytes=1024

# elasticsearch: url/api_key/timeout_ms shared; data_stream is per stream
elasticsearch.url=@@ES_URL@@
elasticsearch.api_key=@@ES_API_KEY@@
elasticsearch.timeout_ms=@@ES_TIMEOUT_MS@@
elasticsearch.data_stream.user_prompt=logs-agent_audit.user_prompt-default
elasticsearch.data_stream.tool_call=logs-agent_audit.tool_call-default
